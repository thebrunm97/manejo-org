# 🗂️ Débitos Técnicos & Pendências — pmo-bot-go

> Documento formal de acompanhamento, consolidado a partir de `state.md` e `estado_tarefa.md`.
> Última atualização: **2026-09-01**

---

## Como usar este documento

- Cada item tem um **ID** único (`DT-XX`) para referência em commits, PRs e mensagens (`git commit -m "DT-01: rotaciona service_role key"`).
- Status possível: `🔴 A Fazer` · `🟡 Em Andamento` · `🟢 Concluído` · `⚪ Bloqueado/Decisão Pendente`
- Ao mover um item de coluna, atualize também a seção **Histórico** no final do arquivo.
- Este arquivo substitui o rastreio informal que vinha sendo feito via `state.md` / `estado_tarefa.md` — mantenha-o como fonte única de verdade para débitos.

---

## 🔴 A Fazer

| ID | Item | Categoria | Prioridade | Arquivo(s) |
|----|------|-----------|------------|------------|
| DT-51 | **Evolution devolve 500 ao baixar mídia — 403, causa externa (mídia expirada).** 6 jobs de áudio morreram assim, todos com `status code 403`, mais recente 2026-08-22, todos após esgotar as 3 retentativas — ou seja, **não é falha transitória** e mais retentativa não resolve. Hipótese principal: a mídia expira nos servidores do WhatsApp e o `rawPayload` armazenado deixa de ser suficiente para baixá-la. **Correção da nota anterior:** o log do `evolution-go` **é acessível** via `docker logs pmo-prod-stack-evolution-go-1` nesta mesma máquina (confirmado ao investigar o DT-52) — não está fora de alcance, só não foi lido antes. **Distinto do DT-52:** aquele foi `client disconnected` (sessão morta pelo loadtest), não 403 (mídia expirada) — duas causas diferentes atrás do mesmo sintoma de superfície. **Mitigação já aplicada (DT-50):** o produtor passa a ser avisado em vez de receber silêncio. **Possível correção de fundo:** baixar a mídia no momento do webhook, e não quando o worker pega o job — hoje pode haver minutos de fila entre os dois, e é nessa janela que a mídia expira. Note que isso interage com o DT-42: baixar antes significa reter o áudio antes de saber se ele será processado. | Confiabilidade | Média | `internal/adapter/evolution/adapter.go`, `internal/queue/media_worker.go` |
| DT-46 | **Funções `SECURITY DEFINER` sem checagem de autorização, e defeitos vizinhos.** Levantado ao versionar as funções órfãs (DT-21). Das 9 `SECURITY DEFINER` do lote, **7 não checam nada** — sem `auth.uid()`, sem `is_admin`, sem checagem de papel — e **todas têm `EXECUTE` para `anon`**: `get_dashboard_stats`, `get_propriedade_metrics`, `increment_usage_stats`, `validate_file_extension`, `registrar_atividade_pmo`, `criar_infraestrutura_pmo` (3 sobrecargas). Só `get_admin_user_details` (checa `is_admin`) e `get_recent_bot_activities` (filtra por `auth.uid()`) se protegem. Três também **não fixam `search_path`** (`get_propriedade_metrics`, `get_traceability_data`, `registrar_atividade_pmo`) — `SECURITY DEFINER` sem `search_path` fixo é vetor clássico de escalada, pois o chamador controla a resolução de nomes. **NÃO foram corrigidas durante a sincronização, de propósito**: alterar comportamento ali faria o staging deixar de refletir a produção, que era o defeito sendo consertado. A correção precisa ser aplicada nos DOIS ambientes de uma vez. **Achados vizinhos no mesmo lote:** (a) a sobrecarga de 4 argumentos de `match_chunks` filtra por `k.pmo_id`, coluna que **não existe** em `knowledge_chunks` — como o corpo é plpgsql, o Postgres não resolve nomes na criação e ela só estoura quando chamada; corrigir adicionando a coluna ou removendo a sobrecarga; (b) `is_chemical_input` é marcada `IMMUTABLE` mas chama `unaccent()`, que é `STABLE`; (c) `criar_infraestrutura_pmo` tem 3 gerações convivendo e provavelmente 2 estão mortas; (d) `get_traceability_data` devolve `endereco_completo` da propriedade para chamador anônimo — aqui o acesso anônimo é legítimo (rastreabilidade por QR na embalagem), mas endereço de produtor rural é mais do que a rastreabilidade exige; (e) `farm_documents` tem RLS ligado e **zero policies**, então só `service_role` lê, apesar do `GRANT SELECT` para `anon`/`authenticated`. Parente do DT-18. | Segurança | 🔥 Alta | `supabase/migrations/20260823110000_sync_prod_orphan_functions.sql` |
| DT-47 | **Remover código morto do banco de produção.** Identificado ao versionar as órfãs do DT-21 e deliberadamente **não** transportado para o staging. (a) `event_trigger_fn()` — corpo é literalmente `BEGIN -- Add logic here END;`, stub de tutorial, e alimenta um event trigger de nome genérico `event_trigger_name` em `ddl_command_end`; (b) `custom_access_role_protection()` — nenhum trigger ligado, e é duplicata **mais fraca** de `trg_prevent_self_promotion` (não trata `service_role`), o que a torna uma armadilha: quem a encontrar pode pensá-la ativa. Depois de removidas, os dois bancos ficam idênticos também em contagem. | Limpeza | Baixa | Supabase Dashboard (produção) |
| DT-48 | **Decidir o sentido staging → produção.** O trabalho do DT-21 cobriu só prod → staging. No sentido inverso há objetos que existem apenas no staging e em nenhum arquivo: a tabela `rag_run_judgments` e as funções `upsert_arena_models`, `get_rastreabilidade_publica`, `maintain_single_latest_judgment`, `set_updated_at_rag_run_judgments`. Parecem experimentação de RAG; promover para produção ou descartar exige saber se ainda são usados — por isso ficaram fora. **Item à parte e mais incisivo:** o staging tem uma **sobrecarga obsoleta de `rpc_registrar_compra_insumo` com 12 argumentos, sem `propriedade_id` e sem `idempotency_key`**, anterior ao trabalho de idempotência (DT-00/DT-09). A produção tem só a de 17 argumentos. Uma chamada que resolva para a sobrecarga velha gravaria **sem chave de idempotência**, reabrindo a duplicação que o DT-09 fechou. Avaliar remover do staging. **Nota:** `get_rastreabilidade_publica` pode ser a sucessão de `get_traceability_data` (ver DT-46d); se for, a produção está rodando a versão antiga. | Manutenção/DB | Média | Supabase (staging) |
| DT-02 | Remover ou marcar como `deprecated` o método `InsertFarmDocument` (órfão, grava só na coluna legada 3072d/Gemini) | Código Legado | Média | `internal/supabase/client.go` |
| DT-03 | Corrigir filename hardcoded (`"audio.ogg"`) no pipeline legado de áudio | Código Legado | Média | `internal/state/fsm.go`, `media_worker.go` |
| DT-04 | Descomissionar interface legada `llm.LLMProvider` (FSM) em favor de `ports.LLMProvider` | Código Legado | Média | `internal/state/`, `internal/llm/` |
| DT-05 | ~~Atualizar/padronizar model IDs e tabela de custos~~ — **RESOLVIDO** (primário migrado no DT-32; tabela de custos substituída pelo catálogo multi-fornecedor em DT-43). Resta apenas limpar IDs defasados nos utilitários de benchmark (`cmd/tester/`, `fallback_simulation_test.go`), que não afetam produção. | RAG/Modelos | Baixa | `cmd/tester/` |
| DT-06 | Remover diretório `scratch/` e node_modules do laboratório de embeddings offline (Transformers.js) | Limpeza | Baixa | `scratch/` |
| DT-13 | Suporte opcional a Botões Interativos da Evolution API para confirmação de rascunhos HITL (fallback automático para texto livre) | UX / Webhook | Baixa | `internal/adapter/evolution/`, `internal/webhook/` |
| DT-18 | **ÉPICO**: Migrar escrita direta do `pmo-frontend` para RPCs `SECURITY DEFINER` antes de aplicar REVOKE global de grants. Auditoria confirmou que `pmo-frontend` grava direto em tabelas via SDK do Supabase usando anon/authenticated (arquivos: `cadernoService.ts`, `propriedadeService.ts`, `talhaoService.ts`, `pmoService.ts`, `TabelaDinamica.tsx`, `VegetalImportDialog.tsx`, `Secao9.tsx`). A migração `20260816030000_revoke_broad_grants.sql.pending` está pronta mas NÃO deve ser aplicada até essa migração ser concluída, ou ela quebra essas gravações. | Segurança/Frontend | 🔥 Alta | `pmo-frontend/src/...` |
| DT-38 | **PRAZO — Migrar a stack para VPS antes de ~2026-09-05.** Hoje a produção roda na máquina local do desenvolvedor (Docker Desktop/WSL2), o que já custou caro: foi a origem do clock drift (DT-26), impede qualquer pipeline de deploy (DT-30) e derruba o serviço sempre que a máquina desliga. O responsável viaja para Moçambique em ~2 semanas e o MVP precisa rodar sem depender do desktop. Provedor provável: Hostinger. Escopo mínimo: provisionar VPS Linux, instalar Docker + Compose, transferir `.env.prod` por canal seguro (NUNCA versionado nem colado em terminal — ver DT-01), subir a stack, **reconectar a instância do WhatsApp (exige novo QR e é o passo mais arriscado)**, apontar webhook/DNS, configurar backup do volume `piper_data` e do estado do `evolution-go`, e garantir `restart: always` + boot automático. Ganho colateral: em Linux nativo o NTP funciona por padrão, eliminando a classe de problema do DT-26; e um host sempre ligado destrava o DT-30. **⚠️ CAPACIDADE MEDIDA EM 2026-08-22 — a VPS de 2 vCPUs é apertada.** Do pipeline inteiro, o **Piper (TTS) é o único passo que consome CPU local**; transcrição, LLM e envio são todos limitados por rede. Ou seja, a capacidade da VPS é essencialmente a capacidade do Piper. Medido com o container já limitado a `cpus: 1.0` (mesmo teto da VPS), via `scripts/simular_carga_tts.sh`: **tempo ≈ 5,2s fixos + 0,037s por caractere** (50 chars→7,1s · 150→10,9s · 300→15,2s · 600→27,2s). Sob concorrência, 2 requisições simultâneas levam ~42s contra ~20s de uma só. **Descoberta relevante: concorrência NÃO aumenta vazão** — com 1 vCPU, 2 workers apenas dividem a mesma CPU (≈170 áudios/hora nos dois casos), dobrando a latência individual sem ganho algum. Considerar reduzir AIWorkers para 1 na VPS: mesma vazão, metade da espera. **Alavanca testada e DESCARTADA:** trocar por voz mais leve não ajuda — `pt_BR-edresson-low` (21,3s) não foi mais rápido que `pt_BR-faber-medium` (20,3s); o gargalo é a síntese em si, não o tamanho do modelo. **Alavancas que funcionam:** (a) encurtar as respostas — é o fator dominante e linear; (b) não sintetizar quando o produtor prefere texto (**DT-29**), que elimina a carga em vez de otimizá-la; (c) mais vCPUs. Some-se o teto do tier gratuito da Groq: **2.000 transcrições/dia**. Atenuante importante: o texto já é enviado ANTES do áudio (DT-28), então o produtor recebe a resposta em poucos segundos e o áudio chega depois — a espera de 15-40s degrada o complemento, não a resposta. **✅ DESBLOQUEADO em 2026-09-01 — DT-45 concluído**, chave da Evolution rotacionada (ver DT-45 em Concluído). O bloqueio real (credencial viva exposta publicamente) não existe mais; o `.env.prod` a transferir para a VPS já carrega o valor novo. **Adendo 2026-08-23 — teste de capacidade isolado local (`docker-compose.captest.yml`, DT-53):** `cmd/loadtest_piper` contra o Piper sozinho, mesmo teto `cpus: 1.0`/`1G` do prod, fora de VPS real — mesma metodologia que já gerou os números acima, não é validação independente em hardware de nuvem. Resultado: 1 worker ≈10,0s; 2 workers ≈20,5s (quase dobra — confirma que concorrência não aumenta vazão); 5 workers com spread de 27,4s (26,8s–54,2s), classificado pelo próprio script como **CPU Starvation confirmado**. Mede o Piper "nu": em produção o `tts.ConcurrencyLimiter` (DT-54) já capa em 1 requisição simultânea e degrada para texto no timeout, então o pior caso medido aqui não deveria se repetir tal e qual sob o roteador real. | DevOps / Prazo | 🔥 Alta | `docker-compose.prod.yml`, infraestrutura |
| DT-41-STREAM | **Detectar stall por time-to-first-token (streaming)** — refinamento futuro do DT-41. A correção aplicada elimina os retries inúteis, mas ainda gasta 25s antes de reconhecer o stall. Só o TTFT distingue "modelo pensando" de "conexão morta" *durante* a chamada; por latência total isso é impossível, pois há respostas legítimas em 17s, 19s, 22s e 23s (verificado na amostra). Exige migrar `CallGoogle` de `GenerateContent` para `GenerateContentStream`, com montagem incremental da resposta e das tool calls — mudança de risco moderado no caminho crítico, por isso não feita junto. | Performance | Média | `internal/gemini/client.go` |
| DT-44 | **Falso positivo do filtro de PII em sequências numéricas.** Observado ao validar o DT-39: a string `DT391787418354` foi persistida como `DT[CPF ocultado]4` — o filtro tratou a sequência de dígitos como CPF. Achado com dois lados: confirma que `filter_pii.go` **está ativo sobre o conteúdo persistido** (informação relevante e positiva para o DT-42), mas também mostra que ele redige por padrão numérico sem validar o dígito verificador do CPF. Em produção isso corromperia dados legítimos do produtor — códigos de lote de rastreabilidade, notas fiscais, identificadores de talhão e números de registro têm exatamente esse formato. Corrigir validando o DV antes de redigir, e cobrir com testes de lote/NF que devem passar intactos. | Dados / Segurança | Média | `internal/guardrails/filter_pii.go` |
| DT-40 | **Markdown do WhatsApp renderizado cru no painel web.** O bot formata para o padrão do WhatsApp (`*negrito*`, `**texto**`), e o Monitor ao Vivo exibe os asteriscos literalmente (`**Dicas de ouro**`, `*Consulta Técnica:*`). O texto é o mesmo dos dois lados, mas a convenção de formatação não é. Converter na exibição do painel (WhatsApp-markdown → HTML), sem alterar o que é enviado ao produtor. | Frontend | Baixa | `pmo-frontend/src/pages/` (Monitor ao Vivo) |
| DT-42 | **LGPD — anonimização e minimização antes de enviar dados a terceiros.** Hoje o pipeline envia a fornecedores externos o conteúdo bruto das mensagens do produtor (telefone, localização, biometria vocal). **Status de 2026-08-24:** As diretrizes foram consolidadas no documento `docs/LGPD_GUIDELINES.md`, que separa os dados em duas camadas: **Camada Operacional** (onde descartaremos áudios brutos e payloads em 7 dias, e mensagens sem vínculo probatório em 90 dias) e **Camada de Conformidade** (para evidências que demandam retenção de 3 anos via Portaria MAPA 52/2021). **Próximos Passos (Código):** 1. Criar `certification_records` e `plot_geometry_versions` no banco. 2. Implementar policies/cron jobs no Supabase para enforcing dos TTLs. 3. Finalizar lógica em `internal/guardrails/filter_pii.go` para pseudonimizar os dados antes da injeção no LLM e ocultação completa de biometria vocal (o LLM só deve receber texto). | Jurídico / Segurança | 🔥 Alta | `internal/guardrails/filter_pii.go`, `internal/gemini/`, `supabase/migrations/` |
| DT-19 | Investigar drift histórico de migrações anteriores à Fase 2.2. O `supabase migration list --linked` revelou pares com nomes dessincronizados entre local e remoto (datas 20260525, 20260526, 20260609, 20260610). Não bloqueou o deploy, mas indica que o projeto já tinha histórico de renomeação. Investigar se é apenas dessincronia de nome ou se representa schema realmente divergente entre ambientes. | Manutenção/DB | Média | `supabase/migrations/` |
| DT-21 | ~~**Separacao de Ambientes (Staging vs Prod)**: criar um segundo projeto Supabase~~ — **O PROJETO JÁ EXISTE** desde 2026-08-19 (`pmo-staging` / `srboqpxrzejtxjfgodnc`). O texto anterior deste item mandava fazer algo já feito, e foi usado como justificativa para não testar em staging. **O item real é MANTER O STAGING FIEL.** Diagnóstico de 2026-08-23: 19 funções (23 com sobrecargas) e 3 tabelas existiam só na produção e em nenhum arquivo de migration. A mais grave era `claim_next_message_job` — sem ela o worker não pega job da fila, ou seja, **o bot não rodava contra o staging**. Faltava também o trigger `ensure_role_protection`, controle que impede auto-promoção a admin: presente na produção, ausente no staging. **RESOLVIDO em 2026-08-23** pelas migrations `20260823110000` e `20260823110100`; paridade verificada por digesto md5 das assinaturas e dos corpos normalizados (`dd4e1a94c385dc7294add3389f8879ca`, 74 funções idênticas nos dois). **CAUSA RAIZ, que continua aberta:** schema criado fora do fluxo de arquivos. Descobri o mecanismo na própria sessão — `apply_migration` via API/MCP **gera timestamp próprio** em vez de usar a versão do arquivo, então as migrations de 22/08 ficaram como `20260822170422` no remoto contra `20260822150000` no arquivo local. Não é descuido de quem aplicou: é comportamento da ferramenta. O histórico do staging foi reconciliado com os nomes dos arquivos; **o da PRODUÇÃO não foi** e precisa de `supabase migration repair --status applied` nas três de 22/08, senão um `db push` futuro tenta reaplicá-las. | DevOps / DB | 🔥 Alta | `supabase/migrations/`, Supabase Dashboard |
| DT-37 | **Filtro de ferramentas por intent não filtra nada** — ⚠️ **REBAIXADO: o dado do DT-33 mostrou que cortar ferramentas NÃO resolve os timeouts** (chamadas sem ferramentas são MAIS lentas que as com 29). Continua válido como economia de **custo por token** (~7.400 tokens desperdiçados por chamada), não como correção de performance. ATENÇÃO ao cortar: a matriz de uso real registrou `registrar_colheita` e `registrar_limpeza` sendo invocadas sob intent **RAG** — remover DBWrite de RAG quebraria registros legítimos, exatamente o risco que motivou medir antes. Apenas **7 das 29** ferramentas foram usadas na amostra, mas uma única simulação não prova que as outras 22 sejam dispensáveis. `GetToolsForIntent` inclui `CategoryRAG + CategoryDBWrite + CategoryDBRead` tanto para `RAG` quanto para `DATABASE`, ou seja, **29/29 ferramentas** nos dois casos (CHAT leva 25/29). Medido em bancada (`go test ./internal/mcp/ -run PayloadSize -v`): **29.594 bytes ≈ 7.400 tokens** só de definição de ferramentas, enviados em **cada turno** e **cada retry**. Uma pergunta "qual a previsão do tempo?" carrega `registrar_compra_insumo` (2,3KB), `registrar_venda`, `registrar_colheita`, `cadastrar_propriedade`… Principal suspeito dos timeouts que persistiram após a troca do modelo. **Achado colateral:** a descrição de `calcular_recomendacao_adubacao` contém fragmentos de *system prompt* colados por engano (regras numeradas "# 7. NO TECHNICAL IDs", "# 8. EXTRAÇÃO DE CONTEXTO", "# 9. LÓGICA DE ÁREA TOTAL"), inflando o payload e poluindo o contexto do modelo. | Performance / Custo | 🔥 Alta | `internal/mcp/server.go:143`, `internal/mcp/tools_registry.go:17` |
| DT-33 | **Medir a causa real dos timeouts de LLM antes de trocar de modelo**. ⚠️ **PRIMEIRA MEDIÇÃO JÁ REFUTOU A HIPÓTESE INICIAL**: após migrar do canal preview para o GA, a telemetria registrou `motivo_do_fallback=timeout modelo_primario=gemini-3.1-flash-lite tentativas=3` **duas vezes seguidas** (2026-08-22 09:43). Ou seja, o modelo GA também estoura o prazo — o canal preview **não** era a causa, confirmando a orientação de medir antes de concluir. Investigar as causas ainda não descartadas: prompt extenso (29 ferramentas são enviadas a cada turno), legislação inteira no contexto, ausência de streaming, deadline de 30s por turno no orquestrador possivelmente curto demais para a carga real. Orientação original do time: | Observabilidade | 🔥 Alta | `internal/gemini/`, `internal/telemetry/`, `internal/queue/` |
| DT-33-DETALHE | Orientação de método registrada em 2026-08-22: com ~47% de timeout, não se deve declarar o modelo inadequado antes de saber se a falha está no modelo, na API, na rede ou no **desenho do prompt** (prompt extenso, legislação inteira no contexto, documentos sem pré-processamento, chamadas encadeadas, concorrência, ausência de streaming). A instrumentação `motivo_do_fallback` do DT-32 é o primeiro passo, mas falta registrar por chamada: `request_id, model, input_tokens, output_tokens, latency_ms, time_to_first_token, status_code, timeout_stage, retry_count, retrieved_chunks, prompt_version, cost_estimate`. Falta também aplicar **deadline por etapa** (retrieval 2s, classificação 5s, geração 20s, validação JSON 3s, fallback 15s) em vez de um teto único, para identificar QUAL etapa estoura. Métrica-alvo: **custo por caso corretamente resolvido**, não custo por chamada. | Observabilidade | 🔥 Alta | `internal/gemini/`, `internal/telemetry/`, `internal/queue/` |
| DT-34 | **Roteador de modelos por complexidade** (desenho definido em 2026-08-22). Hoje todas as tarefas usam o mesmo modelo, o que é caro para triagem e fraco para casos complexos. Desenho alvo: `gemini-3.1-flash-lite` para classificação de intenção, extração de entidades, triagem e resumo; `gemini-3.7-flash` para interpretar múltiplos dispositivos normativos, analisar exceções, confrontar documentos e produzir relatório preliminar; **motor de regras determinístico + auditor humano** para decisão final de conformidade, vigência de norma, substância autorizada e cálculo de prazo — nunca delegados ao LLM. O roteamento deve usar critérios objetivos (mais de uma norma aplicável, presença de exceções, conflito entre documentos, ausência de evidência, baixa confiança na recuperação), não a autoavaliação do modelo. Distinguir fallback de **infraestrutura** (mesmo modelo, outro provedor) de fallback de **capacidade** (modelo mais forte). | Arquitetura / Custo | Média | `internal/state/`, `internal/llm/`, `internal/gemini/` |
| DT-29 | **Preferência de formato de resposta por produtor**: hoje o formato é decidido **por mensagem** (espelha a entrada: mandou áudio → responde áudio), não por pessoa. Não existe nenhuma coluna de preferência em `profiles` (verificado no banco). Decisão de produto tomada em 2026-08-22: o produtor deve poder escolher entre `audio`, `texto` e `ambos` por **três** caminhos — (1) comando no WhatsApp ("modo texto"), (2) seletor no painel do `pmo-frontend`, (3) botões interativos no primeiro contato (casa com o DT-13). Escopo: migration adicionando `preferencia_resposta` em `profiles`; leitura em `queue/response_mode.go` (`ShouldRespondWithAudio`), que já é o ponto central de decisão; handler do comando no webhook; UI no frontend. **Pré-requisito já concluído**: a entrega áudio+texto do DT-28. **⚡ BACKEND CONCLUÍDO EM 2026-08-23.** Migration `20260823100000` adiciona `profiles.preferencia_resposta` (`texto` | `audio` | `automatico`, NULL = nunca escolheu). `ports.ResolveResponseModeFor` e `Job.ShouldRespondWithAudioFor` passam a considerar a preferência, com precedencia: modo já resolvido a montante > preferência > campos legados > espelhamento. A preferência vence os campos legados de propósito — senão um job enfileirado antes do deploy faria quem acabou de pedir "modo texto" receber áudio mesmo assim. Comando no WhatsApp implementado em `state/response_preference_command.go`, interceptado antes de qualquer chamada de LLM (interpretar "modo texto" com um modelo gastaria cota e segundos para reconhecer duas palavras fixas), e a confirmação sai NO formato escolhido. O comando só dispara com a mensagem inteira, nunca por substring: aceitar trecho faria "não quero áudio de outra pessoa no meu talhão" reconfigurar o produtor em silêncio (coberto por teste). **DECISÃO DE PRODUTO REVISADA:** o default é `automatico` (espelhamento), não `texto`. Texto como padrão economizaria mais CPU, mas cobra na acessibilidade e cobra de quem menos pode pagar — quem manda áudio frequentemente o faz porque ler e escrever é custoso, e com áudio em opt-in essa pessoa precisaria LER uma instrução para descobrir o recurso. **FALTA:** (a) seletor no `pmo-frontend`; (b) botões interativos no primeiro contato (casa com DT-13); (c) aplicar a migration no Supabase — o código é seguro sem ela (a leitura falha, loga e cai no espelhamento), então dá para deployar em qualquer ordem. **Achado colateral corrigido:** `LogResponseModeDecision` computava a origem da decisão DEPOIS de o pipeline marcar o modo como explícito, então toda decisão era registrada como `explicit` e a métrica `ResponseModeResolutionTotal` ficava cega. Sem isso não haveria como medir a adoção da preferência, que é justamente o número que falta para dimensionar a VPS (DT-38). | UX / Produto | Média | `supabase/migrations/`, `internal/queue/response_mode.go`, `internal/webhook/`, `pmo-frontend/src/` |
| DT-30 | **Não existe pipeline de deploy do container** — causa raiz do DT-25. O `.github/workflows/deploy_production.yml` roda apenas `supabase db push` (migrations); ele **nunca** buildou ou publicou a imagem do `pmo-bot-go`. Ou seja, mesmo funcionando não teria evitado a defasagem. Além disso ele está quebrado: falha em 16s com `Missing value for flag --db-url` porque o secret `PROD_DB_URL` não está configurado no repositório (run `32539826317`), e o checkout emite `fatal: No url found for submodule path 'evolution-go-source' in .gitmodules`. Agrava: **todos** os demais workflows (`Tests & Coverage`, `Backend E2E`, `E2E`) também estão vermelhos há semanas. Complicador de arquitetura: a stack de produção roda localmente via Docker Desktop/WSL2, então um runner do GitHub não alcança o host — resolver exige registry + pull no host, runner self-hosted, ou assumir o deploy como manual e documentado. **Confirmado e agravado em 2026-08-23, com consequência prática:** `evolution-go-source` era um gitlink (`160000 commit 89eaae33...`) sem entrada em `.gitmodules` — `git submodule status` devolvia `fatal: no submodule mapping found`, e o diretório não tinha `.git` próprio nenhum. Não era só o clone do CI que quebrava: **qualquer edição feita dentro de `evolution-go-source/` nesta máquina ficava fora de qualquer controle de versão**, git nem enxergava que os arquivos tinham mudado. Aconteceu na prática ao implementar o Estágio 3 do DT-53 (três edições em `whatsmeow.go`/`instance_service.go` + dois arquivos de teste): ficou tudo em disco, nada rastreável, sem histórico. **Investigação de causa raiz:** o gitlink foi criado direto no commit `ffe4af3` (2026-04-06), sem `.gitmodules` correspondente — nunca funcionou, desde o dia em que foi criado. O diretório é uma cópia vendorizada do repositório público `github.com/EvolutionAPI/evolution-go` (confirmado pelo `go.mod` e pelos badges do README), sem nenhum `.git` próprio. Ele carrega um `.gitmodules` **interno** referenciando `whatsmeow-lib` (`https://github.com/EvolutionAPI/whatsmeow.git`), e `whatsmeow-lib/.git` existia — mas era só um arquivo-ponteiro órfão de 38 bytes (`gitdir: ../.git/modules/whatsmeow-lib`, apontando pra um diretório de dados que não existe). **Não havia nada recuperável.** ✅ **RESOLVIDO (a parte do gitlink) em 2026-08-23:** removido o gitlink quebrado do índice (`git rm --cached`), removidos o ponteiro órfão e um binário compilado solto (`evolution-go-source/evolution-go`, build artifact não coberto pelo `.gitignore` interno do fork), e `evolution-go-source/` passou a ser versionado como diretório normal deste repositório — decisão deliberada de não reconstruir um submódulo de verdade agora (exigiria criar/popular um fork remoto próprio, decisão maior e não urgente, do responsável). **O resto do DT-30 continua aberto**: o pipeline de deploy em si ainda não existe; o checkout do CI deve parar de falhar pelo submódulo ausente, mas isso não foi verificado num run real ainda. | DevOps | 🔥 Alta | `.github/workflows/`, `docker-compose.prod.yml` |
| DT-22 | **Schema Drift Crítico (Local vs Prod)**: O ambiente remoto (Staging/Prod) possuía alterações não versionadas em migrations locais: (1) colunas extras em `profiles`, (2) FK nativa para `pmos`, (3) RLS recursivo infinito na policy de Admin e (4) coluna `canteiro_id` + FK ausentes em `caderno_campo_canteiros`. A falta de paridade quebrou testes locais. Criadas migrations corretivas (finais `212500`, `213000` e `214500`) de forma estritamente idempotente para reestabelecer o rastreio oficial sem quebrar Prod. | Manutenção/DB | 🔥 Alta | `supabase/migrations/` |


## ⚪ Bloqueado / Decisão Pendente

| ID | Item | Decisão Necessária | Opções |
|----|------|---------------------|--------|
| DT-07 | Futuro do `rag_ingest.py` (Docling) | Aposentar em favor do `cmd/ingestor` (Go) ou manter como pré-processador isolado? | (a) Migrar tudo para Go / (b) Manter híbrido |
| DT-08 | Política de acesso ao painel Knowledge Ops | Reabrir para produtores ou manter restrito ao admin? | (a) Abrir para produtores / (b) Restrito a admin |

## 🟡 Em Andamento

| ID | Item | Estado |
|----|------|--------|
| DT-58 | **Cadastro 100% via WhatsApp (Onboarding)** — extrair os dados iniciais do produtor no chat, removendo o atrito de web/PWA. | **DECIDIDO em 2026-08-24: opção (b), Extração One-Shot Estruturada**, com confirmação por botões. Motivo da escolha: o estado da FSM vive em memória (`history.Manager`, TTL 45min, `map[phone]*Conversation`) e some em restart/deploy — um slot-filling longo (opção *a*) perderia o cadastro pela metade e exigiria persistir estado fora da memória, trabalho que a (b) não pede. WhatsApp Flows (opção *c*) exigiria tocar `evolution-go-source`, mesma mecânica de rebuild que o DT-53 já adiou. **O alvo já existe e é pequeno:** a RPC `setup_initial_profile(p_user_id, p_nome, p_propriedade_nome, p_area_ha, p_talhao_nome)` cria Profile+Propriedade+Talhão numa transação e já é usada pelo portal web (`pmo-frontend/src/services/onboardingService.ts`) — são **4 campos** do produtor. **O bloqueio real é outro:** `profiles_id_fkey` é `FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE` (verificado no schema vivo), e um produtor só-WhatsApp não existe em `auth.users`. Por isso o DT-58 depende do DT-59: quem cria esse usuário é o Go, via Admin API. **Já entregue (2026-08-24):** `CreateAuthUserByPhone`, `DeleteAuthUser` e `SetupInitialProfile` em `internal/supabase/auth_admin.go`. **Falta:** o fluxo em si, em `internal/state/fsm.go:196` — hoje o número desconhecido recebe `"❌ WhatsApp não vinculado. Vincule via portal web."` e o fluxo morre. Preservar o comando `CONECTAR <código>` (`LinkDeviceToWeb`), que continua sendo o caminho de quem já tem conta. Reusar `SendButton` (`internal/ports/whatsapp.go:18`, já usado no HITL em `tool_pipeline.go:96`) para a confirmação. **Cuidado de idempotência:** se o usuário for criado no `auth` e a RPC seguinte falhar, sobra órfão e a próxima mensagem tentaria criar outro — daí o `DeleteAuthUser` como compensação. |
| DT-59 | **Centralização do Backend em Go (Evitar Vendor Lock-in)** — frontend acessa o banco direto via SDK do Supabase, descentralizando as regras de negócio em relação ao bot. | **DECIDIDO em 2026-08-24: opção (a), na forma de GATEWAY — não remoção do PostgREST.** O Go vira a porta de entrada (valida JWT, autoriza, audita) e chama **as mesmas RPCs que já existem**. **Por que não ao pé da letra:** "cancelar a dependência do PostgREST" contradiz o **ADR-002 "Fat Database" (Aceito)**, que decide lógica transacional em PL/pgSQL com Go como *thin backend* — e que já avaliou e aceitou o lock-in do Postgres, com mitigação documentada (funções em SQL padrão). Reescrever as 37 RPCs em Go jogaria fora a atomicidade que o ADR-002 elenca como benefício central. O problema real do DT-59 é outro e é legítimo: **quem** pode chamar as RPCs. Isso o gateway resolve sem contradizer o ADR. **Dimensão medida:** 51 arquivos do frontend tocam o SDK, 35 tabelas/views via `.from()`, 37 RPCs via `.rpc()`. **Precedente que evita greenfield:** `/api/v1/admin/knowledge/*` (9 rotas, gin) já é chamado pelo frontend via `VITE_API_URL`. **✅ Fatia 1 CONCLUÍDA (2026-08-24)** — ver DT-61, já em produção. **✅ Fatia 3 CONCLUÍDA (2026-08-24, ainda não deployada):** achado crítico ao implementar — as 10 RPCs alvo (`create_talhao`, `update_talhao`, `delete_talhao`, `create_caderno_registro`, `update_caderno_registro`, `delete_caderno_registro`, `rpc_update_propriedade`, `create_pmo`, `update_pmo`, `delete_pmo`) são `SECURITY DEFINER` e derivam o dono do registro de `auth.uid()` (confirmado em `supabase/migrations/20260818140000_create_domain_mutation_rpcs.sql`: `IF v_user_id IS NULL THEN RAISE EXCEPTION 'Não autorizado'`). Chamar com a chave de serviço faria `auth.uid()` vir NULL e toda chamada morrer — por isso `internal/gateway/rpc_proxy.go` **encaminha o JWT do próprio produtor** ao PostgREST (nunca a chave de serviço), com um allowlist fechado de nomes de RPC (404 para qualquer nome fora dele, antes de tocar o banco). `auth.uid()` resolve exatamente como resolve hoje quando o frontend chama direto — o gateway não é uma segunda autorização, é um encaminhamento auditado da mesma. Rota: `POST /api/v1/rpc/:name`, protegida por `RequireAuth` (qualquer produtor autenticado, não só admin). 4 testes cobrindo allowlist, encaminhamento do token certo, recusa sem auth e repasse intacto de status/erro da RPC. **Frontend:** `goApiRpc()` novo em `services/goApiClient.ts`, migrado como referência em `talhaoService.ts` (`create`/`delete`) — as outras 8 chamadas (`update_talhao`, os 3 de `caderno_registro`, `rpc_update_propriedade`, os 3 de `pmo`) continuam indo direto ao PostgREST, mesmo padrão, ainda não trocadas. **ADR-009** escrito, registrando que o gateway complementa o ADR-002 em vez de supersedê-lo. **Falta:** deploy (o código está commitado, não no ar); migrar as 8 chamadas restantes com teste de tela real antes de cada troca — mesma lição do DT-31, build/vet/teste verde não substitui uso real; leitura pura (dashboards, `get_dre_mensal`, rastreabilidade pública) fica no PostgREST por ora, não é onde está o risco. **Atenção ao ADR-003 (Offline-First, Aceito):** `useSyncEngine.ts` sincroniza direto com o Supabase — toda rota migrada precisa que o motor de sync aponte para o novo destino, ou registros offline param de subir em silêncio; não verificado ainda. |
| DT-61 | **Rotas administrativas do Go estavam sem autenticação nenhuma.** Achado em 2026-08-24 ao dimensionar o DT-59. O middleware global era só RequestID/CORS/Logger/Recovery e `r.Group("/api/v1/admin")` não recebia nada: 9 rotas abertas, entre elas `DELETE /knowledge/documents/:id` e `POST /knowledge/playground/rag` (gasta crédito de LLM por chamada), mais `POST /admin/reload-knowledge`. O próprio frontend documentava o buraco (`// Note: In a real scenario, attach the JWT token here`). Só não vazou porque a stack roda em localhost — o corte para VPS (DT-38) acabaria com essa proteção acidental. | **✅ CONCLUÍDO em 2026-08-24** (é a fatia 1 do DT-59). `internal/middleware/auth.go`: verificação de JWT do Supabase por **JWKS/ES256** — o projeto usa chaves assimétricas, não o segredo HS256 legado (confirmado no endpoint real), então **não entrou segredo novo no `.env`**, basta o `SUPABASE_URL`. Cache do JWKS com rebusca única em `kid` desconhecido (rotação; o projeto já publica 2 chaves) e intervalo mínimo entre rebuscas para o `kid` inválido não virar vetor de DoS. Algoritmo fixado em ES256 (fecha confusão de algoritmo — `alg=none` e HS256-com-chave-pública). `RequireAdmin` lê `profiles.role`, **não** o claim `role` do token: o Supabase manda `"authenticated"` para todo usuário logado, inclusive produtor comum. A RPC `is_admin` do frontend não serve ao backend porque resolve por `auth.uid()`, nulo com chave de serviço. Falha fechada em todos os caminhos. **CORS corrigido junto:** `Allow-Origin: *` com `Allow-Credentials: true` é combinação inválida pela especificação — não protegia nem funcionava; agora vem de `ALLOWED_ORIGINS`. **Frontend:** duas chamadas mandavam `Bearer ${localStorage.getItem('token')}`, chave que o Supabase nunca escreve (a sessão mora em `sb-<ref>-auth-token`) — o header ia com `null` e nunca funcionou; centralizado em `services/goApiClient.ts` lendo `supabase.auth.getSession()`. 9 testes novos. **Falta:** a prova de ponta (`curl` sem header → 401 contra o servidor rodando de verdade), que exige subir a stack. |
| DT-33 | Medir a causa real dos timeouts de LLM | **Instrumentação COMPLETA no ar, aguardando tráfego.** Já emitidos: `event=llm_call` (status, turno, modelo, `latency_ms`, `req_bytes`, ferramentas, msgs de histórico, `input_tokens`, `output_tokens`, `tool_calls`), `event=llm_fallback` (`motivo_do_fallback` em vocabulário controlado) e `event=tool_invoked` (intent × ferramenta). Cobertura fechada em 2026-08-22: além do laço do orquestrador, foram instrumentados o **atalho de CHAT** (que envia ZERO ferramentas e é o grupo de controle natural da hipotese de carga) e o **nivel do provider** em CallGoogle, que captura de uma vez os 6 pontos que chamam o LLM — inclusive o **roteador de intencao**, ja observado levando **19s para classificar Ola, bom dia com zero ferramentas**, dado que contraria a hipotese de que so a carga explica os timeouts. Agregador em scripts/analisar_telemetria.sh, que acumula em .telemetria_acumulada.log para sobreviver a recriacao de container. ATENCAO: evitar docker compose up --build ate a analise, pois recriar o container descarta seus logs. |
| DT-37 | Filtro de ferramentas por intent não filtra nada | **Parcialmente resolvido.** Removidos os fragmentos de system prompt vazados na descrição de `calcular_recomendacao_adubacao` (784 bytes, ~200 tokens — ganho real mas pequeno, 2,6%). O corte de verdade depende do DT-33: apertar `GetToolsForIntent` sem dado é arriscado, pois se o roteador classificar "registra 10kg de tomate" como RAG e RAG tiver perdido as ferramentas de escrita, o registro falha em silêncio. `event=tool_invoked` foi adicionado justamente para montar a matriz intent × ferramenta e permitir o corte com base em uso real. |
| DT-64 | **Onboarding via WhatsApp (DT-58) travava em produção para número desconhecido, por quatro causas empilhadas.** Achado ao investigar por que o bot não respondeu ao áudio de um produtor real (2026-08-24): (1) `HandleOnboarding` pulava a pergunta "você já tem cadastro?" sempre que a mensagem tinha ≥5 palavras (`pareceConterDados`), e o LLM de extração às vezes "alucinava" um nome a partir de perguntas de domínio, travando o fluxo numa confirmação de cadastro errada; (2) o interceptor global de HITL (`handleHITLResponse`, `internal/webhook/handler.go`) sequestrava qualquer "SIM"/"NÃO" de número **sem perfil**, respondendo sempre "não encontrei rascunho pendente" antes da mensagem chegar no onboarding — reproduzido ao vivo com uma segunda produtora real, que nunca conseguiu vincular a conta por e-mail por causa disso; (3) o "Ultra-Fast Greeting Guard" (`internal/state/fsm.go`) interceptava saudações puras ("oi", "boa tarde") **antes** de checar se o número tinha cadastro, também travando o onboarding na primeira mensagem; (4) o `PIIScrubber` (guardrail de entrada) trocava o e-mail digitado por `[Email ocultado]` justamente no estado `aguardando_email`, quebrando a checagem de e-mail válido — o próprio fluxo pedia o dado sensível e o guardrail o apagava antes de chegar na lógica. | **Os quatro corrigidos e deployados em produção em 2026-08-24:** (1) extração ganhou um campo `eh_cadastro` decidido pelo próprio LLM (`internal/state/onboarding.go`), só avança para confirmação quando a mensagem é de fato dado de cadastro; (2) `handleHITLResponse` agora retorna cedo (sem tratar) quando o telefone não tem perfil (`internal/webhook/handler.go`); (3) o greeting guard passou a checar profile antes de disparar (`internal/state/fsm.go`); (4) `ai_worker.go` pula o `PIIScrubber` quando o estado da FSM é `aguardando_email`/`aguardando_otp_email`, resolvendo o telefone com `ResolvePhone` (mesma chave usada por `SetFSMState`) para casar o estado certo. Validado ponta a ponta em produção via webhooks sintéticos (número de teste fictício, sem afetar produtores reais) — cadeia completa até `otp_enviado` funcionando. **Achado colateral no mesmo teste, ainda em aberto:** o template de e-mail "Magic Link or OTP" do Supabase Auth só tinha `{{ .ConfirmationURL }}` (manda link clicável), não `{{ .Token }}` (código de 6 dígitos) — o bot pede pro produtor digitar um código que nunca existia. **Corrigido no template** (adicionado `{{ .Token }}`, testado e confirmado recebendo código). **Bloqueador real que sobra:** o projeto usa o serviço de e-mail **padrão/embutido do Supabase** (`noreply@mail.app.supabase.io`), documentado pela própria Supabase como *dev-only*, "best-effort", com rate limit baixo por projeto (não por destinatário) — 4 envios de teste em ~15min já foram suficientes para a cota parar de entregar. Resolve configurando **SMTP customizado** (Project Settings → Auth → SMTP Settings) com um provedor como Resend/SES/Postmark, o que exige um domínio próprio verificado (registros SPF/DKIM). **Decisão do responsável (2026-08-24): domínio fica para quando a VPS (DT-38/DT-45) for provisionada** — registrado aqui para não represar o DT-58 esperando por isso; o onboarding funciona ponta a ponta hoje para quem NÃO precisa vincular por e-mail (cadastro novo direto), só o caminho "já tenho conta, vincular por e-mail" fica bloqueado até o SMTP próprio existir. |
| DT-53 | **Self-healing do heartbeat.** O heartbeat de `sendHeartbeat` detectava `WhatsApp DISCONNECTED` a cada 60s mas nunca reagia — só logava e gravava em `bot_status`. Confirmado por DOIS incidentes reais no mesmo dia (2026-08-23): DT-52 (24min, `StreamReplaced` engolido em silêncio pelo fork) e um segundo incidente descoberto e diagnosticado ao vivo durante esta própria sessão (**36min35s**, 17:29–18:05, DNS transitório + auto-restart de tiro único do `evolution-go` + `connected=false` impedindo religar no boot — um QUARTO caminho de falha distinto do DT-52, mesmo sintoma). | **Estágios 0, 1, 2 e 3 implementados e testados. Deploy (rebuild da imagem do evolution-go) fica pra janela do DT-38**, por decisão deliberada (DT-30/DT-25/DT-55 já mostraram essa mecânica de rebuild falhar silenciosamente — não vale arriscar fora da janela que já vai reconstruir a stack inteira). **Achado e resolvido durante o Estágio 3, ver DT-30:** o código do Estágio 3 vive em `evolution-go-source/`, que era um gitlink quebrado sem `.gitmodules` — as edições ficaram em disco, sem controle de versão algum, até o gitlink ser removido e o diretório passar a ser versionado como pasta normal deste repositório (mesmo commit que entrega este Estágio 3). **Desvio importante do plano original:** a investigação achou que `POST /instance/reconnect` (o que estava planejado) **recusa exatamente o caso que se quer curar** — `ensureClientConnected` devolve `client disconnected` quando o cliente existe no pool com o socket morto, e `ReconnectClient` nunca chega a ser chamado. A ação real usa `POST /instance/forcereconnect/:id` (rota admin, mas a chave do bot já alcança — confirmado ao vivo). **Estágio 0** (`internal/ports/notifier.go`, `internal/notify/`): Telegram (push) + e-mail (fallback) em cascata, com cooldown por chave que fura na recuperação, tudo atrás de `notify.Disparar` (goroutine própria, `context.WithoutCancel`, `recover`, nunca bloqueia nem derruba quem chamou — mesma lição do defeito (c) do DT-32). **Estágio 1** (`internal/selfheal/`): máquina de estados pura (`classificar`) distinguindo `caida` (recuperável) de `deslogada`/`nao_autorizada` (nunca age, só alerta) via o campo `Jid` de `/instance/info` — a trava central contra QR automático, testada isoladamente. `Healer.HealOnce` aplica graça (2 sondagens, dá margem ao auto-restart nativo do evolution-go), backoff (2/5/15/30min), teto de 4 tentativas por incidente, cooldown de 6h antes de reabrir um novo lote, e teto móvel de 10 tentativas/24h. Trata corretamente o falso negativo conhecido do `forcereconnect` (dorme 2s e devolve 500 mesmo quando a reconexão real vai funcionar em ~10s) — decide sempre por nova sondagem, nunca pelo status HTTP do POST. **Estágio 2** (`internal/webhook/handler.go`, `ExtractEventType`): o bot já recebia webhooks de `CONNECTION` e os descartava (`ParseWebhook` devolve `nil` pra tudo que não é mensagem) — agora um evento `Disconnected/ConnectFailure/LoggedOut/Connected` acorda a sondagem em segundos em vez de esperar até 60s, sem furar a graça. `QRCode` é tratado à parte como **disjuntor**: desliga o healer permanentemente na execução corrente e alerta uma única vez — terceira trava contra QR, complementar às duas primeiras (`Gateway` sem método `Connect`, e o check de `Jid` em `classificar`). **Dois bugs reais achados pelos próprios testes, corrigidos antes de reportar sucesso:** uma corrida nos testes (checagem rodando antes da goroutine assíncrona do alerta terminar, corrigida com sinal por canal) e um bug de lógica de verdade — depois do cooldown pós-esgotamento expirar, o código resetava o estado mas nunca chegava a tentar de novo, só devolvia; ficava preso até o próximo tick. **Flags:** `SELF_HEAL_ENABLED` (default `false`) e `SELF_HEAL_DRY_RUN` (default `true` quando ligado — percorre toda a máquina de estados e dispara todo alerta, mas nunca chama `forcereconnect`; é o modo de estreia contra a sessão real, risco zero). **Para ativar de verdade:** configurar `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` (ou SMTP) e `SELF_HEAL_ENABLED=true` no `.env.prod`, rebuild do `pmo-bot-go` (não do fork). Build limpo, `go vet` limpo, suíte nova (selfheal + notify + evolution) rodada 5× seguidas sem flakiness, suíte completa do projeto sem novas falhas. **Estágio 3** (`evolution-go-source/pkg/whatsmeow/service/whatsmeow.go`, `pkg/instance/service/instance_service.go`): **F1** — `Reconnect` deixou de checar `ensureClientConnected` antes de delegar a `ReconnectClient`, que já trata todos os estados sozinho (o gatekeeper era o motivo de `/instance/reconnect` não funcionar pro caso comum). **F2** — o `return` que matava a sessão em silêncio num soluço de rede (a causa exata do DT-56) virou retentativa limitada (5/10/20/40/80s) via `retryConnect`, e a desistência final persiste `disconnect_reason` e dispara webhook `ConnectFailure` via `reportarFalhaDeConexao` — sem isso o self-heal do bot só descobriria pelo poll de 60s. **F3** — `StreamReplaced` (a causa do DT-52) deixou de só logar e dar `return`: agora persiste estado e dispara webhook, nas três listas de categoria `CONNECTION` (`CallWebhook` e as dela duas filas globais AMQP/NATS). Deliberadamente **só reporta, nunca reconecta sozinho** — evita duas autoridades de reconexão brigando pela mesma sessão (ping-pong). **F4 não implementado** (opcional no plano original — `AllowQR` em `ClientData` fecharia de vez a terceira trava contra QR; fica pra depois, o disjuntor por webhook do Estágio 2 já cobre esse caso na prática). Testado com fakes de `WhatsmeowService`/`InstanceRepository`/`Producer` (5 testes nos dois pacotes) — `retryConnect` em si não é testável sem um `*whatsmeow.Client` real, registrado como limitação honesta. Build/vet nativo neste ambiente Windows não cobre o binário inteiro (um pacote não-relacionado, `sendMessage`, exige cgo que não está configurado aqui — o Dockerfile já usa `CGO_ENABLED=1`, isso é só limitação do shell local); os dois pacotes tocados compilam, passam vet e testam limpos isoladamente. **Falta:** desligar o dry-run do Estágio 1 depois de observar uma classificação correta ao vivo, e então rebuild+deploy na janela do DT-38. |

## 🟢 Concluído

| ID | Item | Concluído em | Observações |
|----|------|---------------|-------------|
| DT-67 | **Isolamento multi-tenant nas tools MCP era convenção de quem escreve o handler, não propriedade do dispatcher — e havia um caso furado no financeiro.** Levantado em 2026-09-01 ao revisar a arquitetura do agente. O buraco real: nada no tipo nem no fluxo impedia um handler de derivar o tenant dos **argumentos do LLM** em vez do perfil da sessão — era o que `handleConsultarBalancoFinanceiro` fazia, lendo `args["propriedade_id"]` vindo do modelo em vez de `profile.PropriedadeAtivaID`, contra uma RPC autenticada com a service_role key (ignora RLS). | 2026-09-01 | **Item (1)** (já registrado antes desta entrada consolidar tudo): `handleConsultarBalancoFinanceiro` parou de ler `propriedade_id` de `args`. **Item (2):** `ToolHandler` (`internal/mcp/server.go`) trocou `profile *supabase.Profile` por `tenant TenantCtx` (`PmoID`, `UserID`, `PropriedadeID`, `Telefone`) — um handler que só enxerga `TenantCtx` não tem mais como ler um id de tenant de outro lugar que não seja a sessão validada. Nova função `buildTenantCtx(profile) (TenantCtx, error)` centraliza validação (profile nulo / `PmoAtivoID == 0`) e construção, chamada por `CallTool`/`CallToolWithGuard` antes de qualquer handler rodar; as assinaturas públicas desses dois métodos não mudaram. Os ~29 `if profile == nil` redundantes nos handlers (ruído identificado na análise original, não a vulnerabilidade em si) desapareceram como efeito colateral da troca de tipo. Achado e corrigido de passagem no mesmo padrão: `tools_memory.go` declarava `pmo_id`/`phone_number` no schema JSON da tool sem o handler jamais os usar. Caso especial preservado via `context.Value` estreito (`withTenantUpdater`/`tenantUpdaterFromContext`, `internal/mcp/server.go`): `handleCadastrarPropriedade` precisa mutar o profile compartilhado em memória para que chamadas seguintes na mesma resposta do LLM já vejam a nova propriedade ativa sem reconsultar o banco. **Item (3):** `internal/mcp/tenant_guard_test.go`, dois testes novos — `TestNoHandlerReadsTenantIDsFromArgs` varre via `go/ast` todo `tools_*.go` não-teste do pacote em busca de um índice literal `args["pmo_id"\|"pmo_ativo_id"\|"propriedade_id"\|"propriedade_ativa_id"\|"user_id"\|"telefone"]`, tornando a garantia verificável para handlers futuros, não só os corrigidos agora — validado injetando deliberadamente uma leitura proibida num handler e confirmando que o teste falha, depois revertendo; `TestAllRegisteredToolsAreWellFormed` itera `GetAllMCPTools()` garantindo nome, handler e unicidade de cada ferramenta registrada. **Item (4):** `RegisterTool` (`internal/mcp/server.go`) passou a aplicar `wrapWithMiddleware` incondicionalmente — antes só envolvia o handler quando `tool.Options != nil`, deixando 16 das 29 ferramentas sem suporte a `dry_run`. **Verificação:** `go build ./internal/mcp/...`, `go vet ./internal/mcp/...` e `go test ./internal/mcp/...` (27 testes, só os de integração contra `DATABASE_URL` pulados) verdes. **Relação com o DT-66:** aquele item apontava que a suíte de multitenancy não testava isolamento de fato; vários testes ali precisaram ser reescritos nesta correção porque testavam uma checagem (`if profile == nil` no handler) que deixou de existir — a validação real hoje só vive em `buildTenantCtx`. |
| DT-62 | **View `view_conversas_recentes` fazia bypass de RLS — telefones e mensagens de todos os produtores expostos a `anon`.** Achado numa auditoria de segurança do schema Supabase (2026-08-24). A view rodava com as permissões do dono (`postgres`), que ignora RLS por padrão mesmo no Postgres 15+ a menos que marcada com `security_invoker = true`, e tinha `GRANT SELECT` para `anon` — qualquer chamador anônimo via PostgREST lia a lista completa de conversas, bypassando a policy "Admins select all messages" que protege a tabela `messages` de origem. | 2026-09-01 | Nova migration `20260901140000_fix_view_conversas_recentes_rls_bypass.sql`: `ALTER VIEW ... SET (security_invoker = true)` (a view passa a rodar com a RLS de quem chama, não do dono — um autenticado não-admin já cai para zero linhas, igual uma consulta direta a `messages`) e `REVOKE ALL ... FROM anon` (remove o acesso anônimo por completo; o Monitor ao Vivo, única consumidora, já exige admin logado). Validado em local: `anon` passou de ler a lista inteira para `42501 permission denied`; `service_role` (bypassa RLS por padrão) confirmado ainda lendo a view corretamente com uma fixture de teste inserida e removida na hora. **Aplicado em produção** (`hejewayflbuemnffrhae`) no mesmo dia com confirmação explícita do responsável, e reconfirmado ao vivo contra o endpoint REST real com a chave pública atual: `anon` agora recebe `401 permission denied`; `pg_class.reloptions` confirma `security_invoker=true`; grants confirmam `anon` sem nenhum privilégio na view e `authenticated` mantendo `SELECT` (necessário para o admin logado continuar usando o painel). |
| DT-69 | **Achado ao escrever o teste real do DT-66: `consultar_balanco_financeiro` retornava receitas/despesas/saldo sempre zerados em produção, silenciosamente, desde ~2026-08-16.** `rpc_registrar_transacao_com_rateio` passou a normalizar `tipo` com `LOWER(p_payload->>'tipo')` a partir de `20260816000000_add_idempotency_to_mutations.sql` (mantido em `20260824140000_revoke_dt65_gen1_rpc_grants.sql`), então toda despesa/receita gravada desde então ficou minúscula (`'despesa'`/`'receita'`). `rpc_get_balanco_ia` (`20260526060000_create_rpc_balanco_ia.sql`) nunca foi atualizada e seguia comparando `tipo = 'DESPESA'`/`tipo = 'RECEITA'` maiúsculo — nenhuma linha gravada depois de 16/08 batia com o filtro, então o "CFO de bolso" respondia sempre com balanço zerado, sem erro algum. | 2026-09-01 | Nova migration `20260901130000_fix_balanco_ia_tipo_case_mismatch.sql`, recriando `rpc_get_balanco_ia` com `LOWER(tipo) = 'despesa'`/`'receita'` nas três agregações (receitas, despesas, top 3 despesas por categoria) — cobre tanto as linhas novas (minúsculas) quanto qualquer linha antiga gravada em maiúsculo. Confirmado ao vivo contra o Supabase local: inserida uma despesa de R$123.456,78 via `rpc_registrar_transacao_com_rateio`, `rpc_get_balanco_ia` retornava `despesas: 0` antes da correção e `despesas: 123456.78` depois. **Aplicado em produção** (`hejewayflbuemnffrhae`) no mesmo dia, via MCP do Supabase (`apply_migration`, escopado só a esta função — não um `db push` genérico, para não arrastar outras migrations locais pendentes/divergentes do remoto). Verificado após aplicar: `pg_proc.prosrc` da função em produção contém `LOWER(tipo)`. Checado também que nenhuma transação financeira foi gravada em produção desde 16/08 até o momento da correção — ou seja, o bug nunca chegou a esconder um balanço real de nenhum produtor, só teria afetado a primeira consulta a partir de agora. |
| DT-66 | **A suíte de "testes de multitenancy" não testava isolamento entre tenants, e `MULTITENANCY.md` declarava validação sem esse lastro.** `TestIsolation_CrossPMOWrite_ArgsInjectionIgnored` montava um mapa de args com `pmo_id: 999` e nunca chamava nenhum handler; `TestRead_WithRealDB_PMOACannotSeePMOBData` comparava só `profileA.PmoAtivoID != profileB.PmoAtivoID` — nenhum dos dois de fato lia/escrevia um dado de um tenant para verificar se vazava para outro. | 2026-09-01 | Novo `internal/mcp/cross_tenancy_real_postgres_test.go`, `TestFinanceiroBalanco_CrossTenantIsolation_RealPostgreSQL`: registra uma despesa real (R$123.456,78, ano 2099 para isolar de dados reais) sob a propriedade 1 pelo handler de produção `handleRegistrarDespesa`, depois confirma — pelo handler de produção `handleConsultarBalancoFinanceiro` — que a propriedade 2 não a enxerga (controle negativo, `despesas == 0`) e que a propriedade 1 enxerga exatamente o valor gravado (controle positivo — sem ele, um teste que sempre lesse zero passaria mesmo com o pipeline quebrado, que é exatamente o defeito que este teste descobriu de verdade: ver **DT-69**). Segue o padrão de `internal/queue/message_buffer_real_postgres_test.go` (DT-68): roda contra `supabase start` local por padrão, aceita `SUPABASE_TEST_URL`/`SUPABASE_TEST_SERVICE_KEY` para staging, e **falha (não pula) sem Postgres real disponível** — o oposto do padrão antigo de "pula sem `DATABASE_URL`" que era parte do próprio problema do DT-66. Reaproveita propriedades/pmos já existentes no seed (id 1 e 2) para não lidar com a FK de `propriedades.user_id` para `auth.users`. `MULTITENANCY.md` atualizado: exemplos de código trocados de `*supabase.Profile` para `TenantCtx` (refletindo o DT-67), checklist de code review ganhou os dois itens novos de teste, e a alegação "validado" agora aponta para este teste real em vez da suíte antiga. |
| DT-01 | Rotacionar `service_role` key do Supabase (crítico pois a tabela `mutation_drafts` depende de service_role e trafega dados sensíveis). Durante a Fase 2.2, o `SUPABASE_ACCESS_TOKEN` também foi exposto em texto plano e revogado. | 2026-09-01 | Supabase migrou para um sistema de chaves novo (`sb_publishable_...`/`sb_secret_...`, substituindo os antigos `anon`/`service_role` JWT) — gerada uma `secret key` nova no dashboard de produção (`hejewayflbuemnffrhae`), confirmada funcionando (`Blacklist carregada: 3 itens`, dado real via a chave nova). Bot confirmado saudável via `docker logs` depois de cada etapa. **`service_role` legada desativada** na aba "Legacy anon, service_role API keys" do mesmo painel — sem qualquer erro subsequente, como esperado, já que o código já usava só `SUPABASE_KEY` (a chave nova). `pmo-bot-go/.env` (dev local) não foi atualizado, só importa para quem rodar o bot localmente fora do Docker. **Nota da Sessão original**: adotar o hábito para toda a equipe de NUNCA colar segredos em comandos visíveis no terminal (usar prompts interativos ou variáveis lidas com input mascarado). |
| DT-45 | 🛑 **BLOQUEADOR DO DT-38 — a chave da Evolution API estava em texto puro em `cmd/test_audio/main.go:20`, num repositório público, desde 2026-07-30** (commit `163ce12`) — credencial viva da instância de produção do WhatsApp, ~5 semanas de exposição até a rotação. | 2026-09-01 | **(a) e (b):** chave rotacionada (`EVOLUTION_API_KEY`/`GLOBAL_API_KEY` em `.env.prod`, valor hexadecimal puro — a primeira tentativa com um `$` no valor foi cortada pelo Docker Compose ao interpretar como variável dentro do `env_file`, corrigido gerando um valor sem símbolos), containers `pmo-bot-go` e `evolution-go` recriados e confirmados saudáveis (WhatsApp reconectou sozinho, sem QR novo). Achado e corrigido no mesmo fluxo: **DT-57** (vazamento do token nos logs do `evolution-go`, que reapareceria a cada rotação futura). **(c) — histórico do git — decisão explícita: NÃO reescrever.** Antes de decidir, confirmado que as duas worktrees ativas no repositório (`claude/modest-jang-88f9cb`, investigação de dedup; `claude/magical-shamir-a00988`, seed.sql/testes) tinham terminado e não dependiam do histórico permanecer como está — não era isso que pesou contra o rewrite. O que pesou: a chave já está rotacionada e inválida, então o que resta no histórico não é mais um segredo explorável, só registro; `git filter-repo` não está instalado; é repositório público no GitHub, onde um rewrite bem-sucedido não garante remoção de caches do GitHub nem de forks; e existem múltiplas outras branches locais (`feat/*`, `dt-*/*`) mais o `claude/magical-shamir-a00988` recém enviado ao GitHub, todas divergiriam de `main` após o rewrite. Decisão: manter o histórico como está — a rotação já eliminou o risco real. **`pmo-bot-go/.env` (dev local) não foi atualizado** com a chave nova, o que só importa para quem rodar o bot localmente fora do Docker. **Deixou de bloquear o DT-38.** |
| DT-57 | **`evolution-go` imprimia a API key de produção em texto puro no log a cada desconexão** — `whatsmeow.go` (7 ocorrências de `"UserInfo cache cleared for token: %s"`/`"Clearing instance cache - Token: %s"`, não só as 2 linhas identificadas originalmente), mais um vetor não documentado até então: o logger padrão do GORM (`&gorm.Config{}` em `config.go`) imprime a query SQL inteira com parâmetros interpolados sempre que uma consulta não encontra registro — e o fallback de auto-cura de token em `GetInstanceByToken` (`instance_service.go`) dispara exatamente esse caso a **cada rotação de chave**, vazando o valor novo no `docker logs` mesmo com toda a cautela de nunca digitá-lo em outro lugar (achado ao vivo durante a rotação real do DT-45/DT-01 nesta sessão). | 2026-09-01 | Dois fixes em `evolution-go-source`: (1) `newSafeGormLogger()` em `config.go`, configurando `IgnoreRecordNotFoundError: true` nos dois `gorm.Open` de `CreateUsersDB` — suprime o log da query só para o caso "não encontrado" (o próprio fallback de auto-cura), mantendo warnings de queries lentas e erros genuínos; (2) helper `maskToken()` em `whatsmeow.go`, aplicado nas 7 ocorrências que logavam o token cru (mais uma do que o item original documentava), preservando um prefixo de 8 caracteres para correlacionar linhas do mesmo token em debugging sem expor o valor inteiro. Teste unitário novo (`mask_token_test.go`, 5 casos incluindo limites de tamanho). `go build`/`go vet` limpos nos dois pacotes tocados (`pkg/config`, `pkg/whatsmeow/service` — o build do módulo inteiro segue com a limitação pré-existente de cgo do pacote `webp` em ambiente Windows local, já documentada no DT-53, sem relação com esta correção). **Deployado**: imagem reconstruída e `evolution-go` recriado em produção — reconectou sozinho, sem QR, confirmado saudável via `docker logs`. Não foi possível reproduzir ao vivo o caminho exato do vazamento pós-fix (o restart não disparou o fallback de auto-cura, já que o token já estava correto no banco desde a rotação anterior) — a correção é validada por revisão de código + teste unitário + build/vet limpos, não por observação direta do log suprimido em produção. |
| DT-68 | **Mensagens picotadas geram uma resposta por fragmento** — o produtor escreve como fala ("plantei alface hoje" / "no talhão 3" / "umas 200 mudas" em ~4 segundos) e cada mensagem virava um job independente na `message_queue`, com raciocínio de IA e resposta próprios (3× custo de LLM/tool calls/TTS, confusão para o produtor, risco de FSM registrar um plantio incompleto). | 2026-09-01 | Implementado o dreno de coalescência em `claim_next_message_job`: sob o mesmo advisory lock por telefone (`pg_try_advisory_xact_lock`), agrupa fragmentos `ai_pending` elegíveis do mesmo produtor num único turno antes do AI Worker processar. Plano completo em [PLAN-message-buffer-coalescing.md](./PLAN-message-buffer-coalescing.md). **Achado real de concorrência, pego pelo próprio teste e corrigido antes de ir a produção:** a primeira versão da RPC escolhia o "pai" por `SKIP LOCKED` sem noção de telefone — dois workers concorrentes podiam vencer sobre linhas diferentes da MESMA rajada, fatiando um turno de 5 fragmentos em 2-3 "pais" (medido: 5 workers × 5 fragmentos → 3 pais). Corrigido trocando a unidade de contenção de linha para telefone; histórico completo da versão que falhou documentado de propósito no comentário da migration `20260901120100_add_message_queue_drain_on_claim.sql`. **Validação:** 6/6 execuções estáveis contra Postgres local (`-count=1`, sem cache) e 5/5 contra staging real (`srboqpxrzejtxjfgodnc`) via testes parametrizáveis (`SUPABASE_TEST_URL`/`SUPABASE_TEST_SERVICE_KEY`, novo em `message_buffer_real_postgres_test.go`) — dois problemas de higiene de teste corrigidos no caminho (colisão de `msg_id` fixo com índice único aplicado por tarefa paralela; contaminação cruzada entre subtestes por telefone reciclado). **Rollout em produção** (`hejewayflbuemnffrhae`), com confirmação explícita do responsável em cada etapa de risco: schema aplicado (inerte, confirmado via `docker logs` que o bot seguiu saudável), depois o binário publicado em duas fases — primeiro com `MESSAGE_BUFFER_WINDOW=0` (kill-switch, para validar o deploy sem mudar comportamento), depois com os valores reais (`4s`/`12s`). Dois efeitos colaterais descobertos e resolvidos sem incidente: editar `.env.prod` recria também o `evolution-go` (mesmo `env_file`) — reconectou sozinho ao WhatsApp duas vezes, sem QR novo, sessão persistida no volume `evolution_data`; e o classificador de auto mode bloqueou um `docker compose up` repetido, passando na segunda tentativa após reconfirmação em chat. **Pendente, não bloqueante:** observar `message_buffer_parts_per_turn`/`merged_total`/`added_latency_seconds` por um dia de uso real antes de considerar o rollout plenamente encerrado (seção 6.5 passo 5 do PLAN). Achados fora de escopo sinalizados como tarefas separadas, não corrigidos aqui: `seed.sql` quebrado contra o schema atual, e dois testes real-Postgres pré-existentes (`mutation_drafts`, idempotência de cotas) que nunca tinham rodado sua lógica de verdade neste ambiente. |
| DT-65 | RPCs `SECURITY DEFINER` de mutação (geração `*_arg`) sem checagem de posse, IDOR explorável via PostgREST | 2026-08-24 | Corrigido em três frentes, aplicadas primeiro em staging (`pmo-staging`) e validadas antes de ir para produção (`pmo-inteligente`): (1) `registrar_atividade_pmo`, `rpc_registrar_compra_insumo`, `rpc_registrar_operacao_campo` — só o bot as chamava, então `REVOKE ALL FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role`, mesmo padrão já usado em `create_basic_profile` (`20260824130000_create_basic_profile_rpc.sql`); (2) `criar_infraestrutura_pmo` (3 sobrecargas) e `rpc_registrar_cota_produtor` — órfãs, sem chamador vivo no bot nem no frontend, bloqueadas sem `GRANT` a ninguém (destino final fica com o DT-46c); (3) `rpc_registrar_transacao_com_rateio` — única com dois chamadores reais (bot via `service_role` **e** frontend via `financeiroService.ts`, que usa a sessão do próprio usuário), teve o corpo reescrito para derivar `user_id` de `auth.uid()` e validar posse de `propriedade_id` quando o chamador não é `service_role`, em vez de confiar no `user_id` do payload. Drift do DT-48 tratado à parte: staging tinha uma sétima sobrecarga de `rpc_registrar_compra_insumo` (12 args, sem idempotência) que não existe em produção, revogada só lá. Verificado ao vivo: `has_function_privilege` confirmando os grants nos dois ambientes, teste negativo (`authenticated` chamando RPC do grupo 1 → `42501 permission denied`), teste do grupo 3 com `propriedade_id` alheio e `user_id` forjado no payload → rejeitado com `"Propriedade inválida ou não pertence ao usuário"`, e teste positivo real em produção via WhatsApp (colheita, compra de insumo e despesa financeira registradas com sucesso pelo bot depois da migration). Migration: `supabase/migrations/20260824140000_revoke_dt65_gen1_rpc_grants.sql`. |
| DT-63 | **Canal de alerta do self-heal (DT-53 Estágio 0) estava mudo em produção — `canal=nenhum` em todo alerta.** Descoberto ao investigar duas quedas reais do WhatsApp percebidas pelo responsável (2026-08-24, ~14:36–14:55): logs confirmaram DNS transitório do Docker (`127.0.0.11:53 i/o timeout`) derrubando o WebSocket do `evolution-go` em loop por ~19min, com `telemetry event=self_heal_alert` disparando corretamente (`severidade=critico` às 14:41) — só que sem `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` no `.env.prod`, o alerta nunca saía do container. O serviço voltou sozinho (auto-restart de tiro único do `evolution-go`, mesmo padrão do DT-56), mas ninguém foi notificado — só se percebeu por teste manual. | 2026-08-24 | Criado bot no Telegram (`@manorg_bot` via BotFather) e obtido `chat_id` via `GET /getUpdates`. Adicionadas `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` ao `.env.prod`, `pmo-bot-go` recriado (`docker compose -f docker-compose.prod.yml up -d --force-recreate pmo-bot-go`) para reler o env — confirmado saudável nos logs pós-restart. **Nota de segurança:** o token do bot passou em texto puro pelo chat da sessão; recomendado revogar via BotFather (`/revoke`) e trocar por um novo assim que possível. **Atualização no mesmo dia — `SELF_HEAL_ENABLED=true` ligado** a pedido do responsável: heartbeat confirmado enviando o alerta de teste ponta a ponta (`SELF_HEAL_TEST_ALERT=true` numa subida, log `status=ok latency_ms=888`, depois removido do `.env.prod` para não repetir a cada restart). **Segue em `SELF_HEAL_DRY_RUN=true` (default)** — o Estágio 1 já roda a máquina de estados e emitiria o alerta de uma queda real, mas ainda não chama `forcereconnect` de verdade; desligar o dry-run é decisão à parte, deliberadamente represada até observar uma classificação correta ao vivo (ver `DT-53`). **Ping de uptime criado junto** (`cmd/server/main.go`, funções `pingDeUptime`/`formatarDuracaoPing`, chave nova `ports.ChavePingUptime`): manda uma confirmação "online há Xh" pelo Telegram em marcos de 1h/3h/6h/12h/24h após o boot, repetindo a cada 24h depois disso — sinal positivo periódico, independente do alerta de queda (que continua disparando a qualquer momento via `ChaveWhatsAppCaiu`). `go build ./...` limpo; sem teste unitário dedicado (a função é só agendamento sobre `notify.Disparar`, já coberto). |
| DT-60 | **Ordem de montagem do system prompt sabotava o cache implícito do Gemini.** `BuildSystemInstruction` concatenava os blocos DINÂMICOS primeiro (contexto do usuário com talhões, memórias vetoriais top-3, intent do turno) e só depois o conteúdo estável (`.md` de identidade/regras, OKF, instrução de RAG). Como o Gemini 2.5+ casa cache por **prefixo comum** entre chamadas consecutivas, o prefixo nunca se repetia — nem o cache gratuito e automático tinha chance de bater, antes mesmo de qualquer Context Caching explícito entrar em jogo. | 2026-08-24 | Ordem invertida em `internal/state/prompt_manager.go`: estável primeiro (`<IDENTIDADE_E_REGRAS_LLM>` → `<REGRAS_NEGOCIO_OKF>` → `<INSTRUCAO_RAG>`), dinâmico depois (intent + contexto do usuário), e `<MEMORIA_DO_PRODUTOR>` por último por ser a fatia mais volátil. Nenhuma string literal ou condicional mudou — só a ordem de concatenação. Alvo do ganho: o par estável de ~29,6KB de ferramentas (medido no DT-37) + ~12–18KB de prompt/OKF, hoje reenviado a cada turno e cada retry. **Nota de numeração:** o commit `f460c77` saiu rotulado `refactor(DT-59)` por engano — DT-59 já pertencia à centralização do backend em Go. O histórico não foi reescrito (pior que o erro); o comentário no código foi corrigido para DT-60 e o registro passa a ser este. **Ainda não medido em tráfego real:** a telemetria que provaria o ganho é a do DT-37/`CostWithCache`, entregue no mesmo dia — falta rodar com volume para ver `cached_tokens` subir. |
| DT-54 | **Roteador de TTS híbrido com limite de concorrência e fallback gracioso para texto (Fase 1).** O Piper processava requisições concorrentes sem controle algum, arriscando CPU starvation numa VPS de poucos vCPUs — exatamente o que o teste de capacidade do DT-38/DT-53 comprovou (5 requisições simultâneas: spread de 27,4s). Não havia também camada de roteamento entre cache, TTS local e TTS em nuvem — só uma chamada direta ao Piper. **Nota de proveniência:** feito por fora desta sessão (agente Antigravity, commit `c7aca18`, 2026-08-23) e só descoberto ao revisar o repositório durante o DT-53. Os dois documentos que a IA gerou como registro (`PLAN-tts-hybrid-architecture.md`, `WALKTHROUGH-tts-hybrid-architecture.md`) caíam silenciosamente em regras já existentes do `.gitignore` (`PLAN-*.md`, `*ARCHITECTURE.md`) e nunca seriam versionados apesar da intenção declarada de serem "a versão permanente" — o código estava seguro (já commitado), só a narrativa ficava órfã. Conteúdo absorvido aqui e os dois arquivos removidos, para não conviver duas fontes de verdade. | 2026-08-23 | Nova interface `ports.Synthesizer` (`Synthesize(ctx, req) (AudioArtifact, error)`, cancelável por `context.Context`). `tts.Router` encadeia Cache → Local (Piper) → Cloud → Fallback texto. `tts.ConcurrencyLimiter` decora o Piper com fila de capacidade 1 (`chan struct{}`) — quem aborta a espera é o contexto com timeout do chamador, não o limitador, evitando fila infinita. Ao esgotar o timeout, propaga `ErrSynthesizerSaturated` até `Delivery`/`Worker`, que ignora o áudio sem travar a thread e completa a resposta só em texto, com contador Prometheus `tts_fallback_starvation_total` e log `⚠️ TTS Fallback triggered due to starvation` para a degradação não ficar invisível na operação. Injeções ajustadas em `fsm.go`, `ai_worker.go`, `delivery.go`, `utils.go`, `main.go`; `MediaWorker` migrado de `ports.TTSProvider` para `ports.Synthesizer`. Testes unitários cobrindo os 4 ramos do roteador (caminho feliz + falhas em cascata) e a fila unifilar do limitador; suíte `go test ./...` completa passando. Também removido o mapeamento de porta `5000:5000` do `piper` em `docker-compose.prod.yml` — **mas o container em produção ainda está rodando com a porta antiga mapeada**, porque não foi recriado após a edição; ver **DT-55**. |
| DT-55 | **Container de produção do Piper expunha a porta 5000 publicamente** — o `docker-compose.prod.yml` já não mapeava porta pro serviço `piper` desde o DT-54 ("higiene pós-teste de carga"), mas o container em execução continuava com `0.0.0.0:5000->5000/tcp` porque editar o YAML não recria sozinho um container já em pé. Piper não tem autenticação própria: enquanto exposto, qualquer host que alcançasse a porta 5000 desta máquina sintetizava áudio de graça, consumindo a mesma CPU que o produtor precisa. Achado ao montar o teste de capacidade do DT-53 (conflito de porta contra o `docker-compose.captest.yml`). Mesma classe de defeito do DT-25 (edição no arquivo sem efeito prático até recriar o container). | 2026-08-23 | `docker compose -f docker-compose.prod.yml up -d piper` recriou o container aplicando a config atual. Confirmado via `docker ps`: porta agora aparece só como `5000/tcp` (sem publicação no host). Verificado que o resto da stack não foi afetado — `pmo-bot-go` continuou reportando `Heartbeat: WhatsApp CONNECTED` nos logs durante e depois da recriação, já que a comunicação `pmo-bot-go` → `piper` usa a rede Docker interna por nome de serviço, independente de porta publicada no host. |
| DT-56 | **Incidente: WhatsApp fora do ar 36min35s (17:29:09–18:05:44), causa distinta do DT-52.** Cronologia: `[Client/Socket ERROR] failed to read frame header: EOF` → `evolution-go` detecta `Disconnected` e dispara auto-restart → `Already logged in with JID: 5534...` (sessão válida, sem risco de QR) → `Failed to connect: ... dial tcp: lookup web.whatsapp.com on 127.0.0.11:53: i/o timeout` — falha de DNS **transitória** dentro do Docker. O auto-restart do `evolution-go` é **tiro único**: `StartClient` loga a falha e retorna sem retentativa (`whatsmeow.go:517-519`), então um soluço de DNS matou a sessão permanentemente. Um `docker restart pmo-prod-stack-evolution-go-1` sozinho **não bastou** — o boot do `evolution-go` só religa instâncias com `connected=true` no banco (log: `Found 0 connected instances`), e a coluna tinha ficado `false`. Foi preciso um `POST /instance/reconnect` manual pra religar de fato. **Achado colateral de segurança:** o `evolution-go` imprime a API key de produção em texto puro no log a cada desconexão (`whatsmeow.go:1641`, `UserInfo cache cleared for token: <chave>`) — registrado à parte como **DT-57**. | 2026-08-23 | Diagnosticado ao vivo cruzando `docker logs` do `evolution-go` e do `pmo-bot-go` durante a própria sessão de implementação do DT-53 (o incidente aconteceu enquanto o self-heal ainda não estava pronto — prova de necessidade em tempo real). Recuperado com `POST /instance/reconnect` (autorizado pelo responsável, já que o classificador de auto mode bloqueia `docker exec`/chamadas HTTP contra produção por padrão). Confirma **quatro** caminhos de falha distintos já mapeados no board para o mesmo sintoma de superfície (WhatsApp fora do ar sem reação): `StreamReplaced` silencioso (DT-52), auto-restart de tiro único (este incidente), `POST /instance/reconnect` recusando o caso comum (achado ao planejar o DT-53), e `connected=false` impedindo religar no boot (este incidente). Motivou a implementação dos Estágios 0-2 do **DT-53** na mesma sessão. |
| DT-49 | **26 registros de caderno de campo perdidos por vocabulário.** A RPC `rpc_registrar_operacao_campo` valida `tipo_arg` contra sete valores e recusa o resto. Quatorze dos quinze call sites passam literal válido; um passava `ext.Atividade` cru, texto livre do LLM. Sintoma em produção: 26 mensagens com "❌ Erro no Registro: Tipo de operação inválido: aplicação de composto orgânico" — um assistente de produção ORGÂNICA recusando aplicação de composto orgânico. | 2026-08-23 | `domain.NormalizeTipoOperacao` traduz por palavra-chave, seguindo o padrão do `ClassifyHITLResponse` (DT-11). **Verbos antes de substantivos**, descoberto por teste: "lavei as caixas de colheita" caía em Colheita porque a palavra descrevia as CAIXAS, não a atividade. Preserva a distinção compostar (Compostagem) vs aplicar composto (Manejo). **Não chuta quando não reconhece** — pergunta ao produtor, porque arquivar uma venda como manejo corromperia prova perante certificadora. O texto original continua em `metodo_aplicacao`. |
| DT-50 | **Jobs presos para sempre em processamento, e silêncio no dead letter.** `claim_next_message_job` marca o job como em processamento e só o próprio worker o tira dali; se o worker morre (deploy, crash, container reiniciado), ninguém devolve, e o `Claim` só busca `pending`/`ai_pending`. Medido: **8 jobs presos, o mais antigo desde 2026-06-07**. Somado a isso, 6 jobs de áudio morreram por erro 500 do Evolution sem que o produtor fosse avisado de nada. | 2026-08-23 | `queue.StuckJobReaper`, ticker em Go (mesma decisão do DT-42 sobre não usar `pg_cron`). Job de IA volta para `ai_pending`, não `pending`, para não refazer transcrição e gastar cota da Groq de novo. Primeira varredura na SUBIDA, que é quando existem órfãos do processo que acabou de morrer. Regra de dead letter unificada em `IsDeadLetter`/`MaxRetryAttempts`, antes duplicada. No dead letter de mídia o produtor recebe pedido para reenviar ou escrever, em vez de silêncio. |
| DT-52 | **Incidente: `cmd/loadtest.go` (versão antiga, solta na raiz) derrubou a sessão real do WhatsApp por 24 minutos.** Cronologia (2026-08-23, horário local): **13:20:44** — rajada de 70 webhooks `STRESS_*` chega em `/webhook/evolution`, fabricando mensagens de áudio com ID inexistente e o telefone real (`5534997317545`), disparada por alguém rodando o `loadtest.go` antigo (fora do git, com o default perigoso `-url http://localhost:8080/...`, que nesta máquina É a produção real — DT-38). **13:20:48** — o `evolution-go` tenta baixar mídia para essas mensagens fantasmas; a sessão do `whatsmeow` nunca as recebeu de verdade, o protocolo Signal dessincroniza (`Unable to get or create message keys: received message with old counter`), e a Meta derruba a conexão com `StreamReplaced`. **13:20:49 – 13:44:46** — zero tentativa de reconexão; o heartbeat do bot reporta `WhatsApp DISCONNECTED` a cada minuto sem que nada aja — a lacuna de self-healing já registrada no DT-30. Qualquer produtor real que mandasse mensagem nesse intervalo não recebia resposta nenhuma. **13:44:46** — restart manual do container (`docker restart pmo-prod-stack-evolution-go-1`). **13:44:50** — reconectado, `Successfully authenticated`, sem necessidade de novo QR. | 2026-08-23 | **Diagnosticado e mitigado na hora**, cruzando `docker logs` do `evolution-go` com a fila do Supabase. Correção estrutural em `cmd/loadtest`: `-url` perdeu o valor padrão (obrigava a escrever o alvo, mas isso sozinho não bastava — a URL de produção era o único alvo que funciona nesta máquina, então seria digitada de qualquer forma); a correção real é `confirmarAlvo`, que exige o operador digitar `CONFIRMO` depois de ler um aviso citando este incidente, e só pula a pergunta para alvos reconhecidos como staging/CI. README reescrito recomendando `cmd/loadtest_piper` (não toca o WhatsApp) como a escolha padrão para medir capacidade. Os três arquivos soltos na raiz que causaram o incidente (`loadtest.go`, `loadtest_piper.go`, `payload_audio_stress.json` — nunca versionados, com o telefone real) foram removidos; a cópia segura já estava versionada em `cmd/loadtest/`. **Nota:** o restart resolveu porque a sessão em si não corrompeu, só a conexão — se tivesse corrompido, o board já registrava (DT-38) que reconectar por QR é o passo mais arriscado da migração para VPS; este incidente é evidência de primeira mão de por que isso é levado a sério. **Ainda em aberto:** DT-30 continua sem self-healing — se isto acontecer sem ninguém olhando o log na hora, o bot fica fora do ar indefinidamente. |
| DT-00 | Fase 2.1 — Idempotência e migrações no PostgreSQL Staging | 2026-08-16 | Validada com testes unitários e de integração real |
| DT-09 | Two-Phase Commit com tabela `mutation_drafts` no PostgreSQL e TTL de 45min | 2026-08-17 | Fase 2.2 deployada em staging com sucesso (2026-08-16). As 3 migrações (000000, 010000, 020000) confirmadas idênticas entre local e remoto via `supabase migration list --linked`. |
| DT-10 | Tool única polimórfica `propose_batch_mutations(operations: [...])` | 2026-08-17 | Unio discriminada por `type` (`caderno_campo`, `compra_insumo`, `transacoes_com_rateio`, `cotas_produtores`) |
| DT-11 | Normalização determinística de respostas de confirmação HITL por palavras-chave | 2026-08-17 | `ClassifyHITLResponse` antes de chamada a LLM com fallback robusto |
| DT-12 | Substituição atômica via `create_or_supersede_mutation_draft` e terminalidade de rascunhos com erro | 2026-08-17 | Prevenção de race condition sob `FOR UPDATE` e índice único parcial `idx_mutation_drafts_one_pending` |
| DT-25 | **Container de produção defasado (stale build)** — imagem buildada em 2026-08-16 vs. último commit em 2026-08-21 (`1b1b341`). O binário não tinha 5 dias de fixes. Sintoma: `consultar_previsao_tempo` falhando 100% com `parâmetro 'propriedade_id' é obrigatório`, string que já não existia no código-fonte — o que confirmou o diagnóstico. | 2026-08-22 | Rebuild executado (`docker compose -f docker-compose.prod.yml up -d --build pmo-bot-go piper`). Imagem agora 2026-08-22T12:03Z. Levou junto **DT-27** (SendVoice multipart) e **DT-28** (TTS agnóstico + Piper + áudio&texto). Verificado em produção: bot loga `🔊 [TTS] Provider: Piper (auto-hospedado) voz=pt_BR-faber-medium`, container `piper` de pé (Uvicorn na :5000) e síntese testada ponta a ponta (138KB de WAV válido, magic bytes `RIFF....WAVE`). **Causa raiz não resolvida** — rastreada em `DT-30`: não há pipeline que faça deploy do container, então a defasagem volta a acontecer. |
| DT-31 | **TTS lia formatação em voz alta + áudio perdido por timeout**. Validação em produção do DT-28 revelou três defeitos: (1) o texto ia cru para o motor, que narrava "asterisco asterisco Consulta Técnica" e o nome de cada emoji (⏳, 🌿, ☀️); (2) a resposta longa da previsão do tempo falhava com `context deadline exceeded` — o WAV de 554KB estourava o timeout de 30s; (3) o áudio era enviado ANTES do texto, então uma síntese de 25s deixava o produtor sem resposta nenhuma nesse intervalo, e sem nada quando o TTS falhava. | 2026-08-22 | (1) Criado `utils.SanitizeForSpeech` — remove emoji/pictogramas, marcadores de lista, ênfase, cabeçalhos, blocos de código e URLs, preservando letras acentuadas, dígitos, pontuação e `° º ª %`. A mensagem **escrita** mantém a formatação intacta; só o áudio usa a versão limpa. 11 casos de teste. (2) Piper passou a devolver **MP3 em vez de WAV**: medido 92KB contra 554KB (~6x menor) na mesma resposta; timeouts elevados para 120s (contexto) e 150s (cliente HTTP). (3) Ordem invertida para **texto primeiro, áudio depois**, nos dois caminhos (`queue/delivery.go` e `state/utils.go`), com teste de invariante de ordem. Corrigido também o comentário do contrato em `ports.TTSProvider`, que afirmava erroneamente que o texto chegava pré-sanitizado. |
| DT-32 | **Cadeia de fallback do Gemini estava morta em três camadas** (~47% das dúvidas terminando em "erro interno"). (a) `CallGoogle` fixava `c.Config.Model` internamente e **ignorava** o modelo recebido por parâmetro — ao "escalar", `withFallback` voltava a chamar o mesmo primário que acabara de falhar; evidência: a URL do erro citava o primário enquanto o log dizia estar no fallback. (b) O modelo de fallback `gemini-1.5-flash` **foi descontinuado** — confirmado com 0 ocorrências em `/v1beta/models`; estava também hardcoded em `DescribeImage` (descrição de imagem quebrada em silêncio) e como default em `main.go`. (c) A escalada herdava o **contexto já expirado** pelo primário e retornava em ~0,55ms sem abrir conexão — medido pelos timestamps de log (`09:27:04.843084` → `09:27:04.843639`). | 2026-08-22 | (a) `CallGoogle` passou a receber `modelName` obrigatório, propagado em todos os call sites. (b) Primário migrado do canal *preview* para o GA `gemini-3.1-flash-lite`; default morto removido de `main.go` (vazio agora é intencional: escala para **outro provedor**, OpenRouter, que é o que resolve falha de infraestrutura); `OPENROUTER_MODEL` alinhado ao **mesmo** modelo (`google/gemini-3.1-flash-lite`, verificado como existente na OpenRouter). (c) Cada tentativa recebe deadline próprio de 25s via `context.WithoutCancel`, para a escalada não nascer sem prazo. Adicionado vocabulário controlado `FallbackReason` (`timeout`, `rate_limit`, `erro_5xx`, `requisicao_invalida`, `outro`) emitido como `telemetry event=llm_fallback motivo_do_fallback=...`, separando falha de **infraestrutura** de falha de **capacidade**. 9 testes. **Nota de método:** a troca do preview pelo GA é uma hipótese ainda não confirmada como causa dos timeouts — a instrumentação foi adicionada justamente para medir isso em vez de supor (ver DT-33). |
| DT-35 | **Output Judge bloqueava respostas legítimas como `ALUCINACAO_DADOS`** (falso positivo). O `buildJudgePrompt` só marcava `FONTE_RAG=sim` para ferramentas cujo nome contivesse `document`/`rag`, e a regra do juiz só mandava relaxar quando `FONTE_RAG=sim`. Resultado: `consultar_previsao_tempo` executava de fato contra a API meteorológica, mas o juiz recebia `FONTE_RAG=não`, via números específicos (16°C, UV 7,4, evapotranspiração 5,88mm) e acusava alucinação — bloqueando **toda** resposta de previsão do tempo e notificando um especialista à toa. Verificado nos logs que a ferramenta rodou (`09:43:58 handleConsultarPrevisaoTempo executado`) antes do bloqueio (`09:44:02 Judge BLOQUEADA`). **Bug secundário achado pelo próprio teste de regressão:** a checagem usava `strings.Contains(t, "rag")` minúsculo, mas a ferramenta real é `ConsultarLeiOrganica_RAG` — então `FONTE_RAG` era **sempre** "não", deixando o juiz rigoroso demais também nas consultas RAG legítimas. | 2026-08-22 | Introduzido o sinal `FONTE_EXTERNA`, marcado por qualquer ferramenta de consulta executada (prefixo `consultar`, RAG ou documentos), separado do `FONTE_RAG` específico de documentos. Ferramentas de **escrita** (`registrar_*`) deliberadamente não contam como fonte. A regra `ALUCINACAO_DADOS` passou a declarar que dados retornados por qualquer ferramenta em `[FERRAMENTAS USADAS]` são fonte legítima — essas ferramentas foram realmente executadas — e que previsão do tempo não é "registro da fazenda do produtor". Comparação agora case-insensitive. 4 testes de regressão. |
| DT-36 | **Unidades e datas lidas como abreviação no áudio**. Com o pipeline já funcionando ponta a ponta, o motor lia "28°C" como "vinte e oito grau cê" e "(22/08)" como "vinte e dois barra zero oito". Tecnicamente correto, mas soa robótico para quem ouve no campo. | 2026-08-22 | `SanitizeForSpeech` ganhou `expandUnitsForSpeech` (°C/°F/°, mm, mm/dia, cm, km, km/h, kg, ha, m², %) e `expandDatesForSpeech` (dd/mm e dd/mm/aaaa → "22 de agosto"). A expansão roda **antes** de `stripSymbols`, que descartaria `°` e `%` sem deixar leitura; a ordem das regras é significativa (`mm/dia` antes de `mm`). Mês inválido é deixado intacto, evitando estragar razões numéricas. 13 testes novos, incluindo a mensagem real de previsão ponta a ponta. Três asserções anteriores foram atualizadas: afirmavam que `°C` e `%` permaneciam como símbolo, o que era a intenção antiga. |
| DT-26 | **Clock drift** — o sidecar `clockwork` tentava corrigir o relógio de dentro do contêiner e logava "setting time" a cada 30s, mas o offset só crescia (+4,85s → +5,46s em 3 minutos): o relógio autoritativo é o do Windows e o Hyper-V TimeSync reimpõe esse horário na VM, desfazendo o ajuste. O log dava falsa sensação de saúde e mascarou a causa por dias — foi este drift que derrubou 100% dos downloads de mídia do WhatsApp (403) na manhã de 2026-08-22. | 2026-08-22 | **Causa raiz: o serviço `W32Time` do Windows estava PARADO.** A configuração estava correta (`time.windows.com`), mas o serviço é *trigger-started* por ingresso em domínio e a máquina não está em domínio — o gatilho nunca disparava e o Windows jamais sincronizava. Corrigido no host (`Set-Service W32Time -StartupType Automatic` + `Start-Service` + `w32tm /resync /force`), executado manualmente pelo responsável por exigir Administrador. Validado: `Status: Running`, `StartType: Automatic` (sobrevive a reboot), `Camada 5 sincronizado por SNTP`, sondagem a cada 1024s. Drift medido após a correção: **média 0,403s, máximo 0,424s** — dentro da tolerância. O `clockwork` foi reescrito de corretor para **sentinela**: só mede (`ntpd -w`), sem `privileged` nem `SYS_TIME`, e a classificação por limiar saiu do YAML para `scripts/analisar_telemetria.sh`. |
| DT-33 | **Medir a causa real dos timeouts de LLM** — hipótese inicial (canal *preview* do modelo) e hipótese secundária (payload de 29 ferramentas) ambas **REFUTADAS PELO DADO**. | 2026-08-22 | Instrumentação em 3 níveis (`llm_call` no orquestrador, `llm_provider_call` no adapter cobrindo os 6 call sites, `tool_invoked` para a matriz intent×ferramenta) + simulador de carga (`scripts/simular_carga.sh`) + agregador (`scripts/analisar_telemetria.sh`). **Amostra: 121 chamadas.** Conclusões: **(1)** A distribuição é **bimodal** — 69% abaixo de 3s, 85% abaixo de 10s, e **7% travam e são mortas pelo timeout**, com **ZERO chamadas na faixa 24–24,9s**. Essa lacuna é a assinatura de **conexão travada**, não de processamento lento: carga computacional produziria distribuição contínua (chamadas em 15s, 20s, 23s), não um vão. **(2)** O payload **não** explica: chamadas **SEM ferramentas** (roteador, payload mínimo) tiveram média **6.477ms e 23% acima de 10s**, contra **3.749ms e 9%** das chamadas **COM 29 ferramentas** (44KB) — as leves são MAIS lentas que as pesadas. Com payload idêntico (44KB), a latência variou de 789ms a 35.261ms (45x). **(3)** O fix do fallback (DT-32) está funcionando: 20 escaladas para a OpenRouter, **0% de erro para o usuário final** — antes essas 7% viravam "erro interno". Ação restante movida para DT-41 (fail-fast em stall). |
| DT-41 | **Conexão travada era retentada 3x no mesmo endpoint mudo** — custo de ~79s por stall. `isOverloadedError` tratava `context deadline exceeded` como erro transitório de sobrecarga, então um stall disparava a política de retry do primário: 25s (trava) + 2s + 25s (trava) + 2s + 25s (trava) **antes de sequer tentar outro provedor**. Repetir a mesma requisição no endpoint que acabou de ficar mudo por 25s é justamente o que não resolve stall — e contraria a orientação do time de que falha de infraestrutura se resolve trocando de **provedor**, não repetindo no mesmo. | 2026-08-22 | Criado `isStallError`, separado de `isOverloadedError`: stall (deadline/`Client.Timeout`) **escala imediatamente** para a OpenRouter, enquanto 429/503/`overloaded` — que são o servidor declarando carga — mantêm o retry com backoff, onde ele faz sentido. Custo do stall cai de **~79s para ~25s**, afetando os 7% de chamadas medidos no DT-33. 8 casos de teste, incluindo regressão explícita de que timeout não volta a ser classificado como overload. **Nota de método:** a proposta original deste débito era baixar o `attemptTimeout` para 12s; ao inspecionar a faixa de 8–25s antes de aplicar, verificou-se que existem respostas legítimas em 17s, 19s, 22s e 23s — o corte mataria 7 das 13 chamadas dessa faixa. A separação limpa que se supunha não existe (o vão real é só de 23,3s a 24,9s), e a proposta foi descartada em favor desta. Refinamento por TTFT segue em DT-41-STREAM. |
| DT-43 | **Tabela de custos por LLM era ficção** — preços digitados à mão num `switch`, só de Gemini, e **custo ZERO para qualquer modelo não reconhecido**. Como o sistema passou a escalar para a OpenRouter, todo o gasto do fallback ficava invisível. Os valores também estavam defasados: cobrava `gemini-3.1-flash-lite` a US$0,075/US$0,30 por 1M quando o real é US$0,250/US$1,500 — subestimando entrada em 3,3x e saída em 5x. | 2026-08-22 | Criado o pacote `internal/pricing` com catálogo **multi-fornecedor** (319 modelos com suporte a tool calling, de Google, Anthropic, OpenAI, Meta, DeepSeek, Qwen, Mistral e outros), gerado por `cmd/pricing-refresh` a partir de fonte pública e verificável, embutido via `go:embed` e consultado offline. Modelo desconhecido passa a receber estimativa **conservadora (cara)**, nunca zero: subestimar custo desconhecido induz decisão errada de arquitetura, superestimar apenas provoca conferência. O `Lookup` tolera as variações de nome entre fornecedores (`gemini-x` vs `google/gemini-x`, sufixos de variante). Inclui `Compare(entrada, saida, n)`, que ordena por perfil de uso real — a ordem muda conforme a proporção entrada/saída, servindo de insumo objetivo ao roteador do DT-34. **Ganho de resiliência:** o refresh captura `expiration_date`, e `cmd/pricing-refresh -check` falha se um modelo em uso estiver ausente ou expirando — exatamente o cenário que quebrou a cadeia de fallback quando o `gemini-1.5-flash` foi descontinuado sem aviso. **Limitação registrada:** `pricing.overrides` (preço de contexto longo e janelas de horário) não está modelado; o custo assume condições padrão. 7 testes. |
| DT-39 | **Monitor ao Vivo exibia 21:00 em toda mensagem e listava a conversa fora de ordem.** A coluna `timestamp` de `public.messages` era `text`, sem DEFAULT, e o struct `MessageInsert` do bot **não tinha campo de timestamp** — nunca foi enviado. No frontend, `new Date(msg.timestamp)` com NULL vira epoch, que em UTC-3 renderiza **21:00 de 31/12/1969**: uma hora fabricada, com aparência de real. **Segundo defeito, mais grave:** tanto a consulta do painel quanto a view `view_conversas_recentes` ordenam por essa coluna; com ela nula, a ordem das mensagens e a escolha da "última mensagem" de cada conversa eram **indefinidas**. Um painel de auditoria fora de sequência é pior que não ter painel. | 2026-08-22 | Migration `20260822150000`: coluna convertida para `timestamptz`, `DEFAULT now()` como defesa em profundidade, índice `(phone, timestamp DESC)` e a view recriada com `NULLS LAST`. O bot passa a enviar o instante explicitamente (`MessageInsert.Timestamp`) — o que importa para auditoria é a hora do evento, não a do INSERT, que ocorre numa goroutine assíncrona e pode atrasar. No frontend, `formatarHoraMensagem` exibe `--:--` em vez de fabricar horário. **Decisão deliberada:** as 307 linhas históricas seguem NULL. Preenchê-las com `now()` "limparia" a tela, mas fabricaria dado de auditoria num sistema que serve de prova perante certificadora — admitir a lacuna é mais honesto. **Percalços registrados:** (a) o levantamento inicial concluiu "100% NULL"; a verificação antes de aplicar mostrou 30 linhas COM valor, em dois formatos distintos, todos testados como convertíveis; (b) a primeira tentativa falhou e reverteu inteira — a view dependia da coluna (`cannot alter type of a column used by a view`), exigindo DROP/CREATE e restauração explícita dos 20 GRANTs. Verificado após aplicar: tipo correto, DEFAULT ativo, 337 linhas e os 30 timestamps preservados, view funcionando e grants restaurados. |
| DT-20 | Hotfix IDOR Crítico: `setup_initial_profile` mitigado | 2026-08-17 | Vulnerabilidade de sequestro fechada (Staging). Nota: Auditoria manual de baixo volume (N=6), sem cruzamento sistemático de created_at/duplicidade — aceitável devido à volumetria, mas não é garantia formal. |
| DT-28 | **TTS agnóstico + voz natural + resposta em áudio E texto**. (a) A voz robótica vinha de `tts.NewGoogleOrchestrator()` hardcoded em `main.go` — endpoint **não-oficial** do Google Tradutor. (b) O pacote `internal/tts` era um `Orchestrator` único com `switch` por `Provider string`, acoplando a lógica de negócio ao fornecedor: 40+ assinaturas recebiam `*tts.Orchestrator` concreto. (c) A resposta era *ou* áudio *ou* texto, deixando sem saída o produtor que não pode ouvir. | 2026-08-22 | Criada a fronteira `ports.TTSProvider` (mesma estratégia de `LLMProvider`): `GenerateSpeech(ctx, text) (audio, mimeType, err)` + `Name()`. `internal/tts` dividido em `openai_compat.go` (cobre Piper, Groq e OpenRouter — todos falam `/v1/audio/speech`), `google_translate.go` (legado) e `factory.go` (`NewFromEnv`, único ponto que conhece fornecedor concreto). Todas as assinaturas migradas para a interface — os erros de "import não usado" que surgiram foram a prova do desacoplamento. Adotado **Piper auto-hospedado** (serviço no compose + volume `piper_data`, voz `pt_BR-faber-medium`, gratuito e sem cota). Entrega passou a ser **áudio + texto**, com o texto como canal garantido e o áudio best-effort enviado uma única vez (não se repete em retry). Adicionado `sniffAudioMIME` para rejeitar HTTP 200 com corpo não-áudio. Cobertura: 8 testes em `internal/tts` + 5 em `internal/queue/delivery_test.go`. **Requer rebuild (ver DT-25).** |
| DT-27 | **`SendVoice` incompatível com o contrato do `evolution-go`** — bot nunca respondia em áudio. O adapter enviava JSON com `media: "data:audio/ogg;base64,..."` + `mediatype` (contrato da Evolution API v2 em Node), mas o fork `evolution-go` usado aqui expõe `/send/media` com dois caminhos: (a) JSON exigindo `url` (`data.Url`, consumido via `http.Get` — não aceita base64 nem data URI) e (b) multipart aceitando os bytes em `file` + `type`. Resultado: todo envio retornava `400 {"error":"URL is required"}` e caía no fallback para texto. | 2026-08-22 | Fix: `SendVoice` reescrito para `multipart/form-data` com campos `number`, `type=audio` e `file` (bytes decodificados do base64). O `evolution-go` converte para Opus automaticamente e envia com `PTT=true`, então o parâmetro `isPtt` passou a ser redundante nesse caminho. Compila e passa `go vet`. **Requer rebuild da imagem para ter efeito em produção (ver DT-25).** |
| DT-23 | **Sidecar `clockwork` (NTP) quebrado silenciosamente** — causa raiz de "hora errada" + 100% dos downloads de áudio do WhatsApp falhando com 403. `ntpdate` foi removido dos repositórios do `alpine:latest` (`apk add ntpdate` falhava sempre, erro engolido por `2>/dev/null \|\| true`). Sem correção de drift, o relógio do host derivou (2.94s medido às 07:15:52 do dia do incidente) e a Evolution API passou a rejeitar a assinatura das chamadas à WhatsApp API com 401/403, derrubando todo job de download de áudio para dead-letter após 3 tentativas. | 2026-08-22 | Fix: `command` do serviço `clockwork` em `docker-compose.prod.yml` trocado para usar o applet `ntpd` embutido no busybox do Alpine (`busybox ntpd -n -q -p time.google.com`), que não depende de nenhum pacote via `apk` e não silencia erros. Container recriado em 2026-08-22 e o `ntpd` voltou a executar de fato. **PORÉM: conclusão parcial** — a correção não persiste (o relógio segue ~4s atrasado). O download de mídia voltou a funcionar (403 → 200), mas o drift não foi eliminado. Continuação rastreada em **DT-26**. Ver também `state.md` item 9. |

---

## 🎯 Próxima Frente Ativa

> **Mudança de prioridade decidida em 2026-08-24:** **DT-58** (cadastro 100% via WhatsApp) e **DT-59** (centralização do backend em Go) assumiram a frente ativa, a pedido do responsável. A cadeia da VPS descrita abaixo (`DT-45` → `DT-38`, prazo ~2026-09-05) foi **conscientemente despriorizada** — não esquecida. Registrado aqui de propósito: o prazo de 2026-09-05 continua de pé no calendário e ninguém o cancelou; o que mudou foi a ordem de ataque. Se a data se aproximar sem o corte para VPS, a decisão a rever é esta.
>
> **Ordem de execução acordada para a nova frente:**
> 1. ✅ **Fatia 1 — autenticação no Go** (`DT-61`, concluída em 2026-08-24). Era pré-requisito das outras duas *e* fechava um buraco aberto.
> 2. **Fatia 2 — DT-58**, o fluxo de onboarding em `fsm.go:196`. As peças de banco (`CreateAuthUserByPhone`, `SetupInitialProfile`) já estão prontas.
> 3. **Fatia 3 — DT-59**, o grupo `/api/v1/*` para as rotas de escrita, mais o `ADR-009`.
>
> **Atualização 2026-09-01:** o `DT-45` (chave da Evolution exposta em repositório público desde 2026-07-30) foi rotacionado — não é mais exposição ativa. O parágrafo abaixo descreve o estado em que essa decisão de despriorização foi tomada, mantido por registro histórico.

---

**Cadeia real de bloqueio da VPS (`DT-38`, prazo ~2026-09-05) — despriorizada em 2026-08-24, mantida aqui para referência:**
1. ✅ **DT-45** — concluído em 2026-09-01 (rotacionar a chave da Evolution exposta publicamente desde 2026-07-30). Era o único item que o documento amarrava como pré-requisito **real** do DT-38.
2. **DT-38** — corte real (reconexão do WhatsApp via novo QR, o passo mais arriscado): pré-requisito de segurança já satisfeito, segue dependendo só de decidir provisionar a VPS.

**🔥 Alta que NÃO bloqueiam a VPS** — prioritários, mas independentes de qual máquina roda o container: `DT-01`, `DT-46`, `DT-42`, `DT-18`, `DT-21` (residual). Atenção especial ao **DT-30**: parece pré-requisito mas é o contrário — é a VPS que desbloqueia o DT-30 ("um host sempre ligado destrava o DT-30", texto do próprio DT-38), não o inverso.

**Pode ser feito hoje, sem esperar o DT-45:** teste de capacidade isolado numa VPS enxuta descartável, com `cmd/loadtest_piper` (não toca WhatsApp nem a chave real) para validar contra hardware de nuvem de verdade os números já medidos localmente no DT-38 (~5,2s + 0,037s/caractere, 1 vCPU, Piper como único gargalo de CPU). Isso é dimensionamento, não é o corte de produção — não precisa do DT-45 nem do DT-38 andarem primeiro.

**`DT-53`** (self-healing) fica mais crítico depois do DT-38 fechar, não antes — ver observação no próprio item.

---

## 📜 Histórico de Mudanças

| Data | Alteração |
|------|-----------|
| 2026-09-01 | **DT-62 concluído e movido para 🟢** — `view_conversas_recentes` deixava qualquer chamador anônimo (sem sessão) ler telefone e última mensagem de todos os produtores, porque a view rodava com as permissões do dono (`postgres`) e ignorava a RLS de `messages`. Corrigido com `security_invoker = true` na view + `REVOKE ALL ... FROM anon`, validado em local (anon passou de ler tudo para `42501`) e **aplicado em produção** com confirmação explícita, reconfirmado ao vivo contra o endpoint REST real (`anon` → `401`, `authenticated` mantém acesso para o Monitor ao Vivo). |
| 2026-09-01 | **DT-66 concluído e movido para 🟢** — novo `TestFinanceiroBalanco_CrossTenantIsolation_RealPostgreSQL` prova isolamento de verdade (escreve despesa real numa propriedade, confirma pelo handler de produção que outra propriedade não a vê), corrigindo a suíte antiga que nunca lia/escrevia dado real. `MULTITENANCY.md` atualizado para refletir `TenantCtx` (DT-67) e apontar para o teste real em vez da alegação sem lastro. **Achado no processo e corrigido como DT-69:** `rpc_get_balanco_ia` comparava `tipo` em maiúsculo contra dados gravados em minúsculo desde 2026-08-16 — `consultar_balanco_financeiro` retornava zero para tudo em produção, silenciosamente, há mais de duas semanas. Corrigido via nova migration (`20260901130000_fix_balanco_ia_tipo_case_mismatch.sql`), validada em local e **aplicada em produção** (`hejewayflbuemnffrhae`) no mesmo dia com confirmação explícita do responsável, via MCP do Supabase escopado só a esta função. Confirmado que nenhuma transação real foi gravada em produção entre 16/08 e a correção — o bug nunca chegou a esconder balanço de ninguém. |
| 2026-09-01 | **DT-01 fechado por completo:** `service_role` legada desativada no painel do Supabase (aba "Legacy anon, service_role API keys"), confirmado o bot saudável logo depois (`docker logs`, sem erro) — esperado, já que o código só usava a chave nova desde a rotação anterior no mesmo dia. Move para 🟢 Concluído. |
| 2026-09-01 | **DT-45 e DT-01 rotacionados (Evolution API key/GLOBAL_API_KEY e Supabase service_role/secret key de produção), e DT-57 corrigido no mesmo fluxo.** Rotação feita com o valor novo gerado sem eu (Claude) precisar digitá-lo — usuário editou `.env.prod` diretamente após eu levantar todos os arquivos/variáveis envolvidos sem ler os valores. Primeira tentativa da chave da Evolution usava um valor com `$`, que o Docker Compose interpretou como referência de variável dentro do `env_file` e cortou um pedaço ao carregar — corrigido gerando um valor hexadecimal puro (sem símbolos) via `openssl rand -hex 32`, escrito no arquivo por indireção de variável de shell para nunca aparecer em texto nas minhas próprias mensagens. Ao verificar a saúde pós-rotação, descoberto que o `evolution-go` vazava o token novo em texto puro no `docker logs` por DOIS caminhos — não só a linha já documentada no DT-57, mas também o logger padrão do GORM, que imprime a query SQL completa (com o token interpolado) toda vez que o fallback de auto-cura de token (`GetInstanceByToken`) dispara um "record not found", o que acontece a cada rotação por design. Corrigidos os dois no mesmo commit: `IgnoreRecordNotFoundError: true` no logger do GORM (`config.go`) e `maskToken()` aplicado nas 7 ocorrências (uma a mais do que o DT-57 original documentava) que logavam o token cru em `whatsmeow.go`. Teste unitário novo, build/vet limpos, imagem reconstruída e `evolution-go` recriado em produção — reconectou sozinho, sem QR, confirmado saudável. Chave da Supabase confirmada funcionando com dado real (`Blacklist carregada: 3 itens`). Restam: desativar a `service_role` legada no painel do Supabase, decidir sobre reescrita do histórico do git (chave antiga da Evolution ainda legível no commit `163ce12`, mas já inválida), e atualizar `pmo-bot-go/.env` (dev local) se quiser consistência — nenhum dos três é urgência de segurança agora. |
| 2026-09-01 | **DT-68 concluído e movido para 🟢.** Valores reais (`MESSAGE_BUFFER_WINDOW=4s`/`MESSAGE_BUFFER_MAX=12s`) ativados em produção com confirmação explícita ("faça"), depois do deploy anterior com kill-switch confirmar o binário saudável. `docker compose up -d pmo-bot-go` bloqueado pelo classificador de auto mode por ser repetição de um comando de produção já executado na sessão — passou após reconfirmação. `evolution-go` recriado de novo junto (mesmo `env_file`), reconectou sozinho sem QR. Env vars confirmadas via `docker exec printenv`, stack inteiro saudável via `docker ps`. Único item pendente, não bloqueante: observar as métricas de coalescência por um dia de uso real antes de dar o rollout por plenamente maduro. |
| 2026-09-01 | **DT-68 — binário novo publicado em PRODUÇÃO** com `MESSAGE_BUFFER_WINDOW=0` (kill-switch), confirmação explícita do responsável antes do rebuild/recreate do container ao vivo. Descoberto e resolvido na hora: `evolution-go` também lê `.env.prod`, então editá-lo recriou os dois serviços, não só o pretendido — verificado (`docker inspect` + logs) que a sessão do WhatsApp, persistida no volume `evolution_data`, reconectou sozinha sem pedir QR novo. Stack confirmado saudável (`docker ps`, `printenv` nas env vars). Fica pendente, por decisão separada: trocar para os valores reais do buffer (4s/12s) e observar as métricas de coalescência em uso real. |
| 2026-09-01 | **DT-68 — schema aplicado em PRODUÇÃO** (`hejewayflbuemnffrhae`), com confirmação explícita do responsável depois do classificador de auto mode bloquear a primeira tentativa via MCP do Supabase (comportamento correto: mutação em banco de produção real). Verificado pós-apply (colunas, função com `pg_try_advisory_xact_lock`, `cleanup_message_queue`) e confirmado o bot ao vivo saudável via `docker logs` (heartbeat normal, WhatsApp CONNECTED) — mudança inerte, o binário em produção ainda não sabe usar as colunas/lógica novas. Fica pendente, condicionado a nova decisão explícita: o rollout do binário em si (Fases 6.5-6.6 do PLAN), que aí sim muda comportamento observável do bot para produtores reais. |
| 2026-09-01 | **DT-68 aplicado e validado em staging** (`srboqpxrzejtxjfgodnc`), via MCP do Supabase, com autorização explícita do usuário antes de aplicar qualquer coisa no ambiente compartilhado. 5/5 execuções estáveis da bateria de testes contra staging, depois de corrigir uma colisão de `msg_id` fixo com o índice único que uma tarefa paralela aplicou lá, e de descobrir (e não ser afetado por) um `supabase db reset` de outra sessão que zerou o schema DT-68 do Postgres local compartilhado — lição registrada no PLAN sobre múltiplas sessões/worktrees disputando o mesmo container Docker local. Confirmado `get_advisors` sem regressão de segurança nova. Falta produção e o rollout do binário. |
| 2026-09-01 | **DT-68 validado ponta a ponta contra Postgres real, com um bug de concorrência real pego e corrigido.** Rodado `npx supabase db reset` local para destravar os grants que bloqueavam os testes (ver entrada anterior); com o banco funcional, o teste de concorrência da Fase 5 (5 workers × 5 fragmentos) expôs que a primeira versão de `claim_next_message_job` escolhia o "pai" por `SKIP LOCKED` sem noção de telefone — dois workers concorrentes podiam vencer sobre linhas diferentes da mesma rajada, fatiando um turno em 2-3 "pais". Corrigido com `pg_try_advisory_xact_lock` por telefone, serializando o dreno inteiro antes de tocar qualquer linha. Também corrigido um falso positivo intermitente no próprio teste (órfãs de subteste vizinho sendo reivindicadas por workers sobrando) via limpeza e filtro por telefone — higiene de teste, sem relação com o comportamento de produção. Resultado: 6/6 execuções completas estáveis (`-count=1`) contra o Postgres local. Nada aplicado a staging/produção. Achados à parte sinalizados como sugestões separadas: `seed.sql` referencia `profiles.user_id`, coluna que não existe no schema atual (`db reset` aplica todas as migrations com sucesso mas falha ao semear); e dois testes real-Postgres pré-existentes (`mutation_drafts`, idempotência de cotas) falham por razões próprias assim que finalmente conseguem rodar sua lógica de verdade neste ambiente — não investigado, fora do escopo do DT-68. |
| 2026-09-01 | **DT-68 avançado — Fases 1-4 do plano de coalescência de mensagens escritas.** Duas migrations novas (colunas `merged_into_job_id`/`parts_count` + reescrita de `claim_next_message_job` com dreno transacional dos irmãos `ai_pending` do mesmo telefone, e `cleanup_message_queue` estendido para o status `merged`); `Manager.MarkAIPending` passou a calcular `next_retry_at` a partir de `MESSAGE_BUFFER_WINDOW`/`MESSAGE_BUFFER_MAX` (`parseEnvDuration` em `main.go`, mesmo padrão de `AUDIT_GC_INTERVAL`); `Job` ganhou `CreatedAt`/`PartsCount`; `ai_worker.go` loga `parts_count` e alimenta 3 métricas Prometheus novas; `media_worker.go` dispara `composing` já na entrada em `ai_pending`. `go build`/`go vet`/`go test` verdes em tudo que não depende de Postgres real. **Nada aplicado a nenhum ambiente.** Escrito também um teste de integração real (`message_buffer_real_postgres_test.go`) que não pôde ser validado: o Postgres local desta máquina subiu durante a sessão (stack `supabase_kong_manejo-org-app-clean`, sem relação com produção — confirmado via `docker inspect` que o bot de produção real aponta para o projeto hospedado `hejewayflbuemnffrhae.supabase.co`) mas `service_role` não tem GRANT de INSERT em `message_queue` nele (`has_table_privilege` retornou `f`); confirmado que é um gap pré-existente do ambiente local e não deste trabalho, reproduzindo a mesma falha em `TestMutationDrafts_RealPostgreSQL_Integration`, teste já existente e sem relação com o DT-68. Achado à parte, fora do escopo deste item, sinalizado como sugestão separada (não corrigido): `message_queue.msg_id` não tem UNIQUE constraint em nenhuma migration, então o `Prefer: resolution=ignore-duplicates` do `Manager.Enqueue` não tem alvo de conflito e a deduplicação por `msg_id` pode não estar realmente ativa no banco. |
| 2026-09-01 | **DT-67 concluído por completo (itens 2-4) e movido para 🟢.** `ToolHandler` trocou `profile *supabase.Profile` por `tenant TenantCtx` na assinatura de todos os handlers de tools MCP, com `buildTenantCtx` como único ponto de validação/construção antes de qualquer handler rodar. Novo `internal/mcp/tenant_guard_test.go` varre via `go/ast` todo `tools_*.go` proibindo leitura de identificadores de tenant de `args`, mais um teste de sanidade sobre `GetAllMCPTools()`. `RegisterTool` passou a aplicar o middleware incondicionalmente. `go build`/`go vet`/`go test` verdes em `internal/mcp`. Ver linha do DT-67 em Concluído para o detalhamento completo. |
| 2026-09-01 | **Item (1) do DT-67 corrigido:** `handleConsultarBalancoFinanceiro` (`internal/mcp/tools_financeiro.go`) parava de derivar `propriedade_id` de `args` do LLM e passou a usar `profile.PropriedadeAtivaID` direto, com erro amigável quando a propriedade ativa é zero. Varredura confirmou ser o único handler com esse padrão. `go build ./internal/... ./cmd/server/...`, `go vet ./internal/mcp/...` e `go test ./internal/mcp/...` verdes. Itens (2)-(4) do DT-67 (contexto de tenant tipado, teste de guarda genérico, middleware incondicional) seguem em aberto. |
| 2026-09-01 | Levantamento da arquitetura do agente (modelos/tools, WhatsApp+painel, multimodal, base de conhecimento) contra o código. Abertos **DT-67** e **DT-68**. O DT-67 corrige de passagem uma leitura errada que circulava: o dispatcher **já** valida a sessão em `validateProfile`, então os `if profile == nil` espalhados pelos handlers são redundância, não a proteção — o furo real é `handleConsultarBalancoFinanceiro` derivar a propriedade dos args do LLM e consultar com a service_role key, que ignora RLS. Instância concreta do que o **DT-66** aponta que a suíte de multitenancy não testa. O DT-68 ganhou plano próprio em `PLAN-message-buffer-coalescing.md`, encurtado por dois achados no código: o portão `next_retry_at` já existe e já é aplicado pela RPC de claim, e a promoção para `ai_pending` tem caminho único. Levantamento também confirmou como **não implementados** o fallback humano no Monitor ao Vivo (a tela é somente-leitura) e a janela de 24h/templates (bloqueada por decisão de gateway: a stack roda Evolution, não a Cloud API oficial). |
| 2026-08-24 | DT-65 corrigido e movido para Concluído: `REVOKE`/`GRANT` aplicados em staging e produção, corpo de `rpc_registrar_transacao_com_rateio` reescrito para validar posse via `auth.uid()`, testado ao vivo no bot em produção via WhatsApp |
| 2026-08-24 | Adicionados DT-65 (RPCs `*_arg` sem checagem de posse, `EXECUTE` público) e DT-66 (suíte de isolamento não testa isolamento) ao levantar o baseline dos [ADR-010](../../docs/architecture/adr/010-multitenancy-por-organizacao.md) e [ADR-011](../../docs/architecture/adr/011-abstracao-de-canal-de-chat.md) |
| 2026-08-16 | Documento criado a partir da consolidação de `state.md` + `estado_tarefa.md` |
| 2026-08-16 | Adicionadas decisões arquiteturais da Fase 2.2 (mutation_drafts/HITL) — DT-09 a DT-12 |
| 2026-08-17 | Concluída Fase 2.2 (DT-09 a DT-12): Migração SQL, RPCs atômicas, tool `propose_batch_mutations`, interceptor webhook HITL e 100% dos testes de concorrência e integração |
| 2026-08-22 | Diagnosticado e corrigido (código) DT-23: sidecar `clockwork` (NTP) inoperante por remoção do pacote `ntpdate` no Alpine — causa raiz de drift de relógio e falhas 403 no download de áudio do WhatsApp. Container recriado; downloads de áudio voltaram a funcionar (403 → 200). |
| 2026-08-22 | Investigação de follow-up sobre os logs pós-fix revelou 3 problemas adicionais: **DT-25** (container de produção defasado em 5 dias, quebrando `consultar_previsao_tempo`), **DT-26** (drift de relógio persiste apesar do DT-23 — correção não fixa no WSL2) e **DT-27** (`SendVoice` incompatível com o contrato do `evolution-go`, bot nunca respondia em áudio). |
| 2026-08-24 | **DT-63** concluído: configurado canal de alerta Telegram do self-heal (DT-53 Estágio 0), mudo desde a implementação. Motivado por duas quedas reais do WhatsApp no dia (DNS transitório do Docker, ~19min), detectadas pelo self-heal mas nunca notificadas. `pmo-bot-go` recriado com `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`, alerta de teste confirmado ponta a ponta. Na mesma sessão: `SELF_HEAL_ENABLED=true` ligado (segue em dry-run, sem reconexão automática de verdade ainda) e criado ping periódico de uptime (1h/3h/6h/12h/24h, depois a cada 24h) para confirmar "estou vivo" mesmo sem incidente. |
| 2026-08-22 | **DT-28** concluído: criada a fronteira `ports.TTSProvider` (mesma estratégia do `LLMProvider`), `internal/tts` dividido por fornecedor, adotado **Piper auto-hospedado** (voz natural, gratuito, sem cota) e entrega passou a ser **áudio + texto**. 13 testes novos. Aberto **DT-29** para a preferência de formato por produtor. |
| 2026-08-22 | **DT-25** concluído: rebuild da imagem de produção, levando junto DT-27 e DT-28. Piper validado em produção. Correção de registro: o `deploy_production.yml` **rodou** (e falhou por secret ausente), ao contrário do que se anotou antes — mas ele só faz `db push` e nunca deployou o container. Causa raiz aberta como **DT-30**. |
| 2026-08-22 | **DT-26** concluído: causa raiz do clock drift era o serviço `W32Time` do Windows parado (trigger-start por ingresso em domínio, máquina fora de domínio). Corrigido no host; drift caiu para ~0,4s estável. O sidecar `clockwork` virou sentinela — mede e alerta em vez de fingir que corrige. |
| 2026-08-22 | **DT-31** concluído: teste real no WhatsApp expôs que o TTS lia a formatação em voz alta, que a resposta longa perdia o áudio por timeout (WAV grande + teto de 30s) e que o áudio ia antes do texto. Criado `utils.SanitizeForSpeech`, Piper migrado para MP3 (~6x menor), timeouts ampliados e ordem invertida para texto→áudio. Lição registrada: os três só apareceram no uso real — o DT-28 tinha build, vet e 13 testes verdes. |
| 2026-08-23 | Aberto **DT-53** (self-healing do heartbeat), planejado após o incidente do DT-52. Reestruturada a "Próxima Frente Ativa": distinguida a cadeia real de bloqueio do DT-38 (só o DT-45) dos demais 🔥 Alta que são prioritários mas independentes da VPS — corrige leitura anterior que tratava DT-30 como pré-requisito quando na verdade é consequência da migração. |
| 2026-08-23 | Teste de capacidade do DT-53 executado localmente (`docker-compose.captest.yml`, Piper isolado a `cpus: 1.0`/`1G`): 1/2/5 workers medidos com `cmd/loadtest_piper`, resultado anexado como adendo ao **DT-38**. Descoberto e absorvido o trabalho de TTS feito por fora da sessão (agente Antigravity, commit `c7aca18`) — registrado como **DT-54** (concluído), com nota de proveniência sobre os dois documentos que caíam silenciosamente em regras do `.gitignore` e nunca seriam versionados. Aberto **DT-55**: o container de produção do Piper ainda expõe a porta 5000 publicamente porque a edição do compose (parte do DT-54) não foi aplicada com um recreate. |
| 2026-08-23 | **Incidente real durante a própria sessão**: WhatsApp fora do ar 36min35s, diagnosticado ao vivo e recuperado com `POST /instance/reconnect` (autorização explícita, já que o classificador de auto mode bloqueia por padrão chamadas contra produção) — registrado como **DT-56**. Achado colateral de segurança (chave de API em texto puro nos logs do `evolution-go`) registrado como **DT-57**. Resolvido o DT-55 (`docker compose up -d piper` recriou o container aplicando a config sem a porta publicada; resto da stack confirmado saudável nos logs). Implementados e testados os **Estágios 0, 1 e 2 do DT-53** (alerta Telegram+e-mail, healer com graça/backoff/teto/cooldown em dry-run, detector por webhook de CONNECTION com disjuntor de QR) — o Estágio 3 (correções no `evolution-go-source`) fica deliberadamente para a janela do DT-38, junto do DT-57. Dois bugs reais achados pelos próprios testes automatizados e corrigidos antes de reportar sucesso: corrida assíncrona no teste do notifier, e um bug de lógica onde o cooldown pós-esgotamento resetava o estado mas nunca chegava a tentar de novo. DT-53 reescrito e movido de 🔴 A Fazer para 🟡 Em Andamento, corrigindo a menção a `POST /instance/reconnect` (mostrou-se inútil pro caso alvo) por `POST /instance/forcereconnect/:id`. |
| 2026-08-24 | **Fatia 3 do DT-59 (gateway REST):** implementado `internal/gateway/rpc_proxy.go` — proxy autenticado com allowlist de 10 RPCs de escrita do produtor. Achado no caminho que confirma a decisão do ADR-009: as RPCs alvo são `SECURITY DEFINER` e derivam o dono do registro de `auth.uid()`; chamar com a chave de serviço as quebraria (`auth.uid()` viria NULL, a própria função levantaria "Não autorizado"). O gateway resolve isso encaminhando o JWT do produtor, não a chave de serviço — zero mudança de comportamento nas RPCs, só um ponto central de log. `goApiRpc()` no frontend, `talhaoService.ts` migrado como referência (`create`/`delete`); as outras 8 chamadas continuam diretas ao PostgREST, mesmo padrão. **ADR-009** escrito. 4 testes novos no gateway. **Ainda não deployado** — ao contrário da Fatia 1 (DT-61) e da Fatia 2 (DT-58), que já foram para produção nesta mesma sessão. |
| 2026-08-24 | **Aberto e parcialmente resolvido DT-64**: onboarding via WhatsApp (DT-58) travava em produção por quatro causas empilhadas — extração de cadastro hallucinando nome a partir de perguntas de domínio, interceptor de HITL sequestrando SIM/NÃO de números sem perfil, greeting guard travando o onboarding em saudações puras, e o guardrail de PII apagando o e-mail digitado no estado `aguardando_email`. Todas as quatro corrigidas e deployadas em produção no mesmo dia, validadas ponta a ponta com webhooks sintéticos contra um número de teste. Achado colateral: template "Magic Link or OTP" do Supabase Auth só mandava link, não o código de 6 dígitos — corrigido no template. **Fica em aberto:** o e-mail de produção depende do serviço padrão/embutido do Supabase (rate limit baixo, dev-only); resolver exige SMTP customizado com domínio próprio, decisão do responsável de deixar para quando a VPS (DT-38/DT-45) for provisionada. |
| 2026-08-24 | **Troca de frente ativa, a pedido do responsável:** DT-58 e DT-59 assumem a prioridade e a cadeia DT-45 → DT-38 (VPS, prazo ~2026-09-05) é conscientemente despriorizada — registrado na "Próxima Frente Ativa" para não parecer esquecimento depois. Ambos saíram de ⚪ Decisão Pendente com as opções escolhidas: **DT-58 = (b)** extração one-shot (a FSM é in-memory e um slot-filling longo perderia o cadastro em deploy); **DT-59 = (a) como gateway**, não remoção do PostgREST, porque a leitura literal contradiz o **ADR-002 "Fat Database" (Aceito)** — que já avaliou e aceitou o lock-in do Postgres com mitigação documentada. Três achados durante o dimensionamento: (1) **as rotas administrativas do Go não tinham autenticação nenhuma** — aberto e já resolvido como **DT-61**; (2) `profiles.id` tem FK para `auth.users(id)`, então cadastro só-WhatsApp exige o Go criar o usuário via Admin API, o que **acopla DT-58 a DT-59** (são a mesma frente, não duas); (3) o frontend mandava `Bearer ${localStorage.getItem('token')}` em duas chamadas — chave que o Supabase nunca escreve, header ia com `null` e nunca funcionou, só não importava porque o servidor não verificava. Registrado também o **DT-60** (ordem do prompt sabotando o cache implícito do Gemini), corrigindo de passagem o rótulo errado do commit `f460c77`, que saiu como `DT-59` quando esse número já pertencia à centralização do backend. |
| 2026-08-23 | Implementado e testado o **Estágio 3 do DT-53** (F1, F2, F3 em `evolution-go-source`) — o retry limitado em `StartClient` (F2) teria evitado o próprio DT-56, e tornar `StreamReplaced` observável (F3) teria evitado o DT-52. Ao commitar o trabalho, descoberto que `evolution-go-source` é um gitlink quebrado sem `.gitmodules` (`git submodule status` falha, diretório sem `.git` próprio) — **nenhuma edição feita ali fica sob controle de versão**, achado que agrava e é acrescentado ao **DT-30**, que já documentava o sintoma no CI mas não a consequência prática de perda de trabalho local. Rebuild/deploy do fork continua adiado pra janela do DT-38, e agora depende também de resolver o DT-30 antes de sequer poder commitar. |

---

## Notas de Manutenção

- Ao resolver um item, mova-o para **🟢 Concluído**, registre a data e apague a linha correspondente das colunas anteriores.
- Itens novos entram sempre em **🔴 A Fazer** com um novo `DT-ID` sequencial.
- Revisar este documento a cada marco de fase para evitar que volte a virar rastreio informal espalhado por múltiplos arquivos.
