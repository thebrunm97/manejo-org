# 🗂️ Débitos Técnicos & Pendências — pmo-bot-go

> Documento formal de acompanhamento, consolidado a partir de `state.md` e `estado_tarefa.md`.
> Última atualização: **2026-09-09**

---

## Como usar este documento

- Cada item tem um **ID** único (`DT-XX`) para referência em commits, PRs e mensagens (`git commit -m "DT-01: rotaciona service_role key"`).
- Status possível: `🔴 A Fazer` · `🟡 Em Andamento` · `🟢 Concluído` · `⚪ Bloqueado/Decisão Pendente`
- Ao mover um item de coluna, atualize também a seção **Histórico** no final do arquivo.
- Este arquivo substitui o rastreio informal que vinha sendo feito via `state.md` / `estado_tarefa.md` — mantenha-o como fonte única de verdade para débitos.

---

## 🔴 A Fazer

| ID | Item | Categoria | Prioridade | Arquivo(s) |
|----|------|-----------|------------|------------|
| DT-128 | **Build/publish da imagem do `pmo-bot-go` continua manual.** Fechado o DT-30 (pipeline de *migrations* rodando ponta a ponta em CI), mas o `deploy_production.yml` só aplica migrations — build da imagem e deploy do container em si continuam via `deploy/scripts/02-deploy-fase4.sh` rodado à mão na VPS (`git pull` + `docker compose build/up`). Agora que a VPS tem IP público e SSH configurado (DT-38 já resolvido), dá pra automatizar via Action com SSH direto, ou registrar a VPS como runner self-hosted. | DevOps | Média | `.github/workflows/deploy_production.yml`, `deploy/scripts/02-deploy-fase4.sh` |
| DT-129 | **Workflows `Backend E2E` e `E2E` sem os secrets necessários pra rodar.** Faltam `SUPABASE_URL`/`SUPABASE_KEY`/`GEMINI_API_KEY` (backend) e `VITE_SUPABASE_ANON_KEY`/`E2E_TEST_USER_EMAIL`/`E2E_TEST_USER_PASSWORD` (frontend) no repositório. Falta decidir contra qual ambiente rodar (staging existente vs. projeto Supabase dedicado a E2E) e provisionar um usuário de teste antes de configurar. | CI/CD | Baixa | `.github/workflows/backend-e2e.yml`, `.github/workflows/e2e-tests.yml` |
| DT-131 | **Colunas órfãs de `caderno_campo_canteiros` nunca foram removidas após a correção do schema drift.** O DDL original (`supabase/migrations/20260402000000_create_core_app_tables.sql:215-226`) criou essa tabela de junção com as colunas de [[lote-de-rastreabilidade]] (`codigo_lote`, `cultura`, `quantidade`, `data_colheita`, `qr_code_url`) em vez das colunas de junção esperadas. A migration corretiva `20260817214500_fix_caderno_canteiros_fk.sql` (cujo próprio comentário declara "Fix Schema Drift", ver DT-22) adicionou `canteiro_id` + FK, e o frontend (`pmo-frontend/src/services/cadernoService.ts:14`) já usa só essa FK corrigida — mas nenhuma migration removeu as colunas antigas, que continuam na tabela em produção sem uso, gerando confusão sobre qual é o modelo de dados real. Sugestão: migration `DROP COLUMN IF EXISTS` para as 5 colunas órfãs, confirmando antes que nenhuma linha em produção depende delas. | Dados / Limpeza | Baixa | `supabase/migrations/20260402000000_create_core_app_tables.sql:215-226`, `supabase/migrations/20260817214500_fix_caderno_canteiros_fk.sql` |
| DT-130 | **Propriedades sem `cidade`/`uf` cadastradas fazem o bot pedir localização toda conversa.** Observado em produção ao validar a ferramenta `consultar_janela_plantio` (ZARC) com uma propriedade real: `GetPropriedadeLocation` retornou "localização não cadastrada", e tanto o ZARC quanto `consultar_previsao_tempo` (que usa a mesma função) caem no fluxo de pedir cidade/estado ao produtor a cada vez que a conversa não traz isso explicitamente — não há de onde puxar por padrão. Não é bug das duas ferramentas: é lacuna de cadastro. Falta decidir se o onboarding passa a exigir cidade/UF, se cabe um lembrete periódico ao produtor sem essa informação, ou se o fluxo de pedir a cada vez é aceitável. Vale medir primeiro quantas propriedades em produção estão nessa situação. | Produto / Dados | Média | `internal/supabase/location.go`, `internal/mcp/tools_weather.go`, `internal/mcp/tools_zarc.go` |
| DT-76 | **Nome da propriedade hardcoded `'Sítio / Fazenda'` em `complete_onboarding`.** `sql:45` — todas as propriedades criadas via bot recebem o literal `E'S\u00edtio / Fazenda'`; o usuário nunca informa um nome. Impossível distinguir fazendas por nome, relatórios ficam com nome genérico e a UI do hub mostra múltiplas entradas idênticas. Sugestão: adicionar parâmetro `p_nome_propriedade TEXT` à RPC e ao fluxo do bot. | Dados / Produto | Baixa | `supabase/migrations/20260830_create_complete_onboarding_rpc.sql:45` |
| DT-77 | **Dono pode falsificar dados de treinamento em `update_log_treinamento`.** A RPC (`supabase/migrations/20260903110000_create_dt18_remaining_mutation_rpcs.sql:~149-238`) aceita `modelo_ia`, `validado`, `status_validacao` e `json_corrigido` como parâmetros livres do chamador autenticado. Qualquer produtor com sessão válida pode marcar logs como validados, escolher o modelo da IA e injetar ground-truth arbitrário via chamada direta ao Supabase — sem passar pela UI. Impacto direto no pipeline de fine-tuning/avaliação. Sugestão: derivar `modelo_ia` do servidor; rejeitar ou ignorar `validado`/`status_validacao` vindo do caller; exigir que `json_corrigido` nasça de um diff calculado server-side. | Segurança / Dados de IA | 🔥 Alta | `supabase/migrations/20260903110000_create_dt18_remaining_mutation_rpcs.sql` |
| DT-78 | **RPCs de mutação do DT-18 sem `idempotency_key` — risco de duplicação em retry offline.** `create_propagacao_item`, `update_propagacao_item`, `delete_propagacao_item` e `update_log_treinamento` não seguem o contrato de idempotência definido em `20260816000000_add_idempotency_to_mutations.sql`. O engine offline do frontend (`useSyncEngine.ts`) pode acionar as RPCs múltiplas vezes em caso de falha de rede, criando registros duplicados de propagação e limpeza. Sugestão: convergir para o mesmo padrão (`idempotency_key` UUID + unique index por `(user_id, idempotency_key)`) já usado nas demais mutations. | Dados / Idempotência | Média | `supabase/migrations/20260903110000_create_dt18_remaining_mutation_rpcs.sql`, `pmo-frontend/src/hooks/offline/useSyncEngine.ts` |
| DT-79 | **Rate limiting completamente desligado quando `REDIS_URL` não está definida — fail-open em produção.** `cmd/server/main.go:565-569` — quando a env `REDIS_URL` está ausente ou o Redis está fora do ar, `inboundLimiter` e `warningLimiter` viram `NoopRateLimiter{}` e o servidor sobe normalmente com apenas um `log.Println` de aviso. Sem cota de entrada, qualquer remetente pode disparar LLM, TTS e transcrição Groq sem limitação, causando DoS e custo fora de controle. Sugestão: tornar `REDIS_URL` obrigatório no `docker-compose.prod.yml` com `required: true`; adicionar health check que bloqueia o container se o rate limiter for no-op em produção. | Segurança / Operação | 🔥 Alta | `pmo-bot-go/cmd/server/main.go:565-569` |
| DT-80 | **`http.Server` sem nenhum timeout — vetor de slowloris e exaustão de goroutines.** `cmd/server/main.go:759-762` — `&http.Server{Addr: ..., Handler: r}` sem `ReadHeaderTimeout`, `ReadTimeout`, `WriteTimeout` nem `IdleTimeout`. Conexões lentas ou maliciosas seguram goroutines indefinidamente. Crítico após migração para VPS (DT-38): em localhost o firewall do SO limita exposição, mas em VPS pública o servidor fica nu. Sugestão: `ReadHeaderTimeout: 5s`, `ReadTimeout: 15s`, `WriteTimeout: 30s`, `IdleTimeout: 120s`. | Segurança / Rede | 🔥 Alta | `pmo-bot-go/cmd/server/main.go:759-762` |
| DT-81 | **Endpoint `/metrics` público quando `PROMETHEUS_AUTH_TOKEN` não está definida — métricas de operação expostas.** `cmd/server/main.go:324-329` — sem a env, a rota é registrada sem auth e expõe latências de LLM, volume de mensagens e dados operacionais para qualquer um que alcançar a porta. Com a env, a proteção é `gin.BasicAuth` sobre HTTP puro (sem TLS), o que envia a senha em claro. Sugestão: exigir o token em produção (abortar boot se ausente) e servir a rota atrás de TLS ou apenas via rede interna Docker. | Segurança / Observabilidade | Média | `pmo-bot-go/cmd/server/main.go:324-329` |
| DT-83 | **Token de webhook aceito na query string — vazamento via logs de proxy e referrer.** `internal/webhook/handler.go:601-607` — `handleKnowledgeUpload` aceita tanto `?token=` quanto `Authorization: Bearer`. O token na URL vaza em logs de reverse proxy (nginx, Cloudflare), referrer HTTP e histórico do browser. Não há escopo por tenant nem mecanismo de rotação por cliente. Sugestão: aceitar apenas `Authorization: Bearer`; rotação programática e/ou chaves com escopo por evento/tenant. | Segurança / Auth | Média | `pmo-bot-go/internal/webhook/handler.go:601-607` |
| DT-84 | **`file.Filename` cru no path temporário de upload — path traversal.** `internal/webhook/handler.go:652` — `filepath.Join(tempDir, fmt.Sprintf("upload-%d-%s", time.Now().Unix(), file.Filename))` sem `filepath.Base` nem sanitização de charset. Um filename como `../../etc/cron.d/evil` pode escapar do diretório temporário. Sugestão: aplicar `filepath.Base(file.Filename)` + whitelist de charset (alfanumérico, `-`, `_`, `.`) e prefixar com UUID para evitar colisão. | Segurança / Filesystem | Média | `pmo-bot-go/internal/webhook/handler.go:652` |
| DT-85 | **Upload de PDF sem limite de tamanho — DoS de disco e custo de embeddings.** `internal/webhook/handler.go:653` — `c.SaveUploadedFile(file, tempPath)` sem `http.MaxBytesReader` ou verificação de `Content-Length`. Arquivos arbitrários são gravados em disco e depois processados (PDF → texto → chunks → embeddings pagos via OpenRouter). Sugestão: `MaxBytesReader` de 20 MB no middleware Gin ou antes do `FormFile`; validação de MIME via magic bytes; limite de concorrência de ingests simultâneos. | Segurança / Resiliência | Média | `pmo-bot-go/internal/webhook/handler.go:653` |
| DT-86 | **Embeddings de ingestão limitados a 15 RPM por limiter compartilhado entre N workers — fila longa.** `internal/webhook/handler.go:115` — `rate.NewLimiter(rate.Every(4*time.Second), 1)` = 15 RPM total, dividido por todos os workers de conhecimento. Um documento de ~500 chunks leva ~33 min para vetorizar; sob concorrência piora. Sugestão: limiter dedicado para ingestão com burst maior; processar chunks em lote (batch embeddings da API do OpenRouter). | Performance / Custo | Média | `pmo-bot-go/internal/webhook/handler.go:115`, `pmo-bot-go/cmd/server/main.go:763` |
| DT-87 | **`OPENROUTER_API_KEY` lida com `os.Getenv` dentro do método `GetEmbedding` — config espalhada e não injetável.** `internal/supabase/client.go:247` — a chave é lida no corpo da função em vez do `config.Config` injetado no construtor. Mesmo padrão em `internal/llm/reranker.go:14`. Impede testes unitários sem env e torna silenciosa a ausência da variável em runtime (falha só na primeira chamada). Sugestão: mover para `Config` e injetar via struct; guardar `http.Client` com timeout no construtor também. | Config / Testabilidade | Baixa | `pmo-bot-go/internal/supabase/client.go:247`, `pmo-bot-go/internal/llm/reranker.go:14` |
| DT-88 | **Roteamento por telefone ambíguo — terceiro fallback usa `ilike.*{últimos 8 dígitos}*` e retorna o primeiro resultado.** `internal/supabase/client.go:613-620` — quando as duas primeiras tentativas de lookup exato falham, a função faz `telefone=ilike.*{last8}*` e devolve `results[0]` sem desambiguação. Dois telefones que compartilham os últimos 8 dígitos (e.g., DDDs diferentes) podem mapear para o mesmo registro. Mensagens e dados seriam entregues ao produtor errado. Sugestão: normalizar para E.164 no cadastro e buscar por igualdade exata; falha no fallback deve gerar erro explícito, não primeiro resultado arbitrário. | Dados / Segurança | 🔥 Alta | `pmo-bot-go/internal/supabase/client.go:613-620` |
| DT-89 | **Parâmetros de queries PostgREST interpolados sem `url.QueryEscape` — filtros corrompidos por caracteres especiais.** Múltiplos pontos em `internal/supabase/client.go` (ex: linha ~573, 491, 599, 616) usam `fmt.Sprintf("...telefone=eq.%s", phone)` sem encoding de URL. Caracteres `%`, `+`, `&`, espaço e `+55` corrompem o filtro silenciosamente, produzindo queries erradas ou vazando "regex" acidental. Sugestão: `url.QueryEscape` nos valores interpolados ou construir via `net/url.Values`. | Robustez | Baixa | `pmo-bot-go/internal/supabase/client.go` (múltiplas linhas) |
| DT-90 | **`VersionNumber` de documento de conhecimento sempre `1` — versionamento não funciona.** `internal/knowledge/worker.go:112` — `VersionNumber: 1, // TODO: increment from previous versions`. Reindexar o mesmo documento sobrescreve com versão 1, sem histórico. Impede rastreio de regressão de conteúdo e auditoria de qual versão do documento influenciou uma resposta do bot. Sugestão: consultar `MAX(version_number)` do documento antes de inserir e incrementar. | Dados / RAG | Média | `pmo-bot-go/internal/knowledge/worker.go:112` |
| DT-91 | **Testes de RLS/integração SQL nunca rodam em CI.** `supabase/tests/integration/*.sql` (incluindo `dt18_remaining_mutation_rpcs_test.sql` e outros) não são executados por nenhum workflow: `test.yml` roda `go test ./internal/...`; `deploy_production.yml` faz `db diff` + `db push`. Regressões de RLS (como DT-70) passam despercebidas e são detectadas apenas em auditoria manual. Sugestão: job com `supabase start` + `supabase db reset` + `supabase test db` em CI, ativado em PRs que tocam `supabase/`. | Processo / Segurança | Média | `.github/workflows/test.yml`, `.github/workflows/deploy_production.yml` |
| DT-92 | **Deploy de migrations usa `version: latest` da CLI Supabase e vai direto em produção sem staging.** `deploy_production.yml:17-18,39-40` — duas ocorrências de `version: latest`; o job `deploy` depende apenas de `verify_schema_parity` (que só roda `db diff`) e faz `db push --db-url $PROD_DB_URL` a cada push em `main`. Versão `latest` muda comportamento autonomamente; push direto em produção sem canário deixa alterações destrutivas sem revisão. Sugestão: fixar a versão da CLI (padrão já documentado no `secret-scan.yml`); pipeline com step de staging + `supabase test db` antes de promover para prod. | CI/CD / Operação | Média | `.github/workflows/deploy_production.yml` |
| DT-71 | **Rerank existe mas não está no caminho de produção.** `internal/llm/reranker.go` implementa `RerankDocuments` (OpenRouter → `cohere/rerank-v3.5`), porém o **único** chamador é `cmd/tester/arena/main.go` — o binário de arena/avaliação. O fluxo real do bot vai do retrieval vetorial (`match_documents_with_context_1024`, `internal/supabase/client.go:1052`) direto para a expansão de contexto, **sem rerank**. A wiki (`wiki/concepts/rag-e-base-de-conhecimento.md:20`) lista Rerank como fase 5 do pipeline, descrevendo a intenção e não o que roda — ou seja, a documentação está otimista em relação ao código, o que é pior do que não documentar. Consequência prática: a qualidade de RAG medida na arena **não é a qualidade que o produtor recebe**, e qualquer conclusão tirada dos experimentos `rag_*` sobre ordenação de chunks não se transfere para produção. Decidir explicitamente: promover o rerank ao caminho real (e aí o DT-72 vira pré-requisito) ou remover a fase 5 da wiki e assumir que rerank é ferramenta de laboratório. | RAG / Qualidade | Alta | `internal/llm/reranker.go`, `cmd/tester/arena/main.go`, `internal/supabase/client.go`, `wiki/concepts/rag-e-base-de-conhecimento.md` |
| DT-72 | **`RerankDocuments` não tem timeout, retry nem fallback interno — pré-requisito para o DT-71.** A função usa `&http.Client{}` puro (`internal/llm/reranker.go:38`), sem `Timeout`: uma chamada pendurada ao OpenRouter segura a goroutine indefinidamente. Hoje isso é tolerável porque só o binário de arena chama, mas no AI Worker significaria um worker travado por resposta — exatamente a classe de falha do DT-41. O fallback que existe (cair para os "top 3 crus" quando o rerank falha) mora em `cmd/tester/arena/main.go:93`, no chamador, não na função — então promover a função a produção **não** leva o fallback junto. Correção: mover timeout, retry limitado e degradação graciosa para dentro de `RerankDocuments`, para que qualquer chamador herde o comportamento seguro. | Resiliência / LLM | Alta | `internal/llm/reranker.go` |
| DT-73 | **Telemetria de LLM só existe em log — não é consultável, e não há `trace_id` correlacionando as camadas.** Duas lacunas irmãs, ambas herdadas do DT-33. (a) `LLMCallTelemetry` (modelo, tokens, latência, TTFT, retry, custo estimado) é emitida via `slog` em `internal/telemetry/tracker.go:88` e morre no stdout: perguntas operacionais banais — "quanto gastamos por org no mês passado", "qual prompt_version regrediu" — exigiriam raspar log, enquanto as tabelas `rag_*` e o painel Knowledge Ops já existem e ficariam de fora. (b) Não existe identificador único atravessando webhook → media worker → AI worker → RAG → LLM, então as fases medidas em `RAGTelemetry` não são reconstituíveis como uma única requisição. O `docs/PLAN-latency-tracing.md` levantou exatamente essa pergunta ("devemos adotar OpenTelemetry?") e ela nunca foi respondida. **Decisão registrada em 2026-09-04:** não adotar Langfuse — o projeto já tem os três trilhos (slog estruturado, Prometheus com `RagLatencyMS` por fase, tabelas `rag_*` de avaliação) e o que falta é persistência e correlação, não uma ferramenta nova. Caminho recomendado: tabela `llm_call_logs` no Supabase + propagação de `trace_id`. O `go.opentelemetry.io/otel` no `go.mod` é dependência **indireta**, não instrumentação própria. | Observabilidade | Média | `internal/telemetry/tracker.go`, `internal/gemini/client.go`, `internal/queue/ai_worker.go`, `docs/PLAN-latency-tracing.md` |
| DT-51 | **Evolution devolve 500 ao baixar mídia — 403, causa externa (mídia expirada).** 6 jobs de áudio morreram assim, todos com `status code 403`, mais recente 2026-08-22, todos após esgotar as 3 retentativas — ou seja, **não é falha transitória** e mais retentativa não resolve. Hipótese principal: a mídia expira nos servidores do WhatsApp e o `rawPayload` armazenado deixa de ser suficiente para baixá-la. **Correção da nota anterior:** o log do `evolution-go` **é acessível** via `docker logs pmo-prod-stack-evolution-go-1` nesta mesma máquina (confirmado ao investigar o DT-52) — não está fora de alcance, só não foi lido antes. **Distinto do DT-52:** aquele foi `client disconnected` (sessão morta pelo loadtest), não 403 (mídia expirada) — duas causas diferentes atrás do mesmo sintoma de superfície. **Mitigação já aplicada (DT-50):** o produtor passa a ser avisado em vez de receber silêncio. **Possível correção de fundo:** baixar a mídia no momento do webhook, e não quando o worker pega o job — hoje pode haver minutos de fila entre os dois, e é nessa janela que a mídia expira. Note que isso interage com o DT-42: baixar antes significa reter o áudio antes de saber se ele será processado. | Confiabilidade | Média | `internal/adapter/evolution/adapter.go`, `internal/queue/media_worker.go` |
| DT-47 | **Remover código morto do banco de produção.** Identificado ao versionar as órfãs do DT-21 e deliberadamente **não** transportado para o staging. (a) `event_trigger_fn()` — corpo é literalmente `BEGIN -- Add logic here END;`, stub de tutorial, e alimenta um event trigger de nome genérico `event_trigger_name` em `ddl_command_end`; (b) `custom_access_role_protection()` — nenhum trigger ligado, e é duplicata **mais fraca** de `trg_prevent_self_promotion` (não trata `service_role`), o que a torna uma armadilha: quem a encontrar pode pensá-la ativa. Depois de removidas, os dois bancos ficam idênticos também em contagem. **Reconfirmado ao vivo em 2026-09-02:** `event_trigger_fn()` segue com o corpo stub e o event trigger `event_trigger_name` (em `ddl_command_end`) ainda ligado a ela; `custom_access_role_protection()` segue sem nenhum trigger apontando pra ela (`pg_trigger` vazio); staging confirmado sem nenhuma das duas — a remoção segue segura e disponível, só não foi executada por decisão do responsável (pediu para não mexer agora). | Limpeza | Baixa | Supabase Dashboard (produção) |
| DT-13 | Suporte opcional a Botões Interativos da Evolution API para confirmação de rascunhos HITL (fallback automático para texto livre) | UX / Webhook | Baixa | `internal/adapter/evolution/`, `internal/webhook/` |
| DT-44 | **Falso positivo do filtro de PII em sequências numéricas.** Observado ao validar o DT-39: a string `DT391787418354` foi persistida como `DT[CPF ocultado]4` — o filtro tratou a sequência de dígitos como CPF. Achado com dois lados: confirma que `filter_pii.go` **está ativo sobre o conteúdo persistido** (informação relevante e positiva para o DT-42), mas também mostra que ele redige por padrão numérico sem validar o dígito verificador do CPF. Em produção isso corromperia dados legítimos do produtor — códigos de lote de rastreabilidade, notas fiscais, identificadores de talhão e números de registro têm exatamente esse formato. Corrigir validando o DV antes de redigir, e cobrir com testes de lote/NF que devem passar intactos. | Dados / Segurança | Média | `internal/guardrails/filter_pii.go` | **Resolvido em 2026-09-06** — `isValidCPF`/`isValidCNPJ` (DV + rejeição de sequências repetidas) adicionados como `validator` das regras; regex de telefone ajustada para casar DDD compacto sem reintroduzir falso positivo; cobertura em `filter_pii_test.go` (lote/CNPJ inválidos intactos) e `sensitivity_test.go`. Cherry-pick `7029433` → `3b0f5cc` + correções no `e62dd9c`. Ver entrada no Histórico. |
| DT-40 | **Markdown do WhatsApp renderizado cru no painel web.** O bot formata para o padrão do WhatsApp (`*negrito*`, `**texto**`), e o Monitor ao Vivo exibe os asteriscos literalmente (`**Dicas de ouro**`, `*Consulta Técnica:*`). O texto é o mesmo dos dois lados, mas a convenção de formatação não é. Converter na exibição do painel (WhatsApp-markdown → HTML), sem alterar o que é enviado ao produtor. | Frontend | Baixa | `pmo-frontend/src/pages/` (Monitor ao Vivo) |
| DT-42 | **LGPD — anonimização e minimização antes de enviar dados a terceiros.** Hoje o pipeline envia a fornecedores externos o conteúdo bruto das mensagens do produtor (telefone, localização, biometria vocal). **Status de 2026-08-24:** As diretrizes foram consolidadas no documento `docs/LGPD_GUIDELINES.md`, que separa os dados em duas camadas: **Camada Operacional** (onde descartaremos áudios brutos e payloads em 7 dias, e mensagens sem vínculo probatório em 90 dias) e **Camada de Conformidade** (para evidências que demandam retenção de 3 anos via Portaria MAPA 52/2021). **Próximos Passos (Código):** 1. Criar `certification_records` e `plot_geometry_versions` no banco. 2. Implementar policies/cron jobs no Supabase para enforcing dos TTLs. 3. Finalizar lógica em `internal/guardrails/filter_pii.go` para pseudonimizar os dados antes da injeção no LLM e ocultação completa de biometria vocal (o LLM só deve receber texto). | Jurídico / Segurança | 🔥 Alta | `internal/guardrails/filter_pii.go`, `internal/gemini/`, `supabase/migrations/` |
| DT-34 | **Roteador de modelos por complexidade** (desenho definido em 2026-08-22). Hoje todas as tarefas usam o mesmo modelo, o que é caro para triagem e fraco para casos complexos. Desenho alvo: `gemini-3.1-flash-lite` para classificação de intenção, extração de entidades, triagem e resumo; `gemini-3.7-flash` para interpretar múltiplos dispositivos normativos, analisar exceções, confrontar documentos e produzir relatório preliminar; **motor de regras determinístico + auditor humano** para decisão final de conformidade, vigência de norma, substância autorizada e cálculo de prazo — nunca delegados ao LLM. O roteamento deve usar critérios objetivos (mais de uma norma aplicável, presença de exceções, conflito entre documentos, ausência de evidência, baixa confiança na recuperação), não a autoavaliação do modelo. Distinguir fallback de **infraestrutura** (mesmo modelo, outro provedor) de fallback de **capacidade** (modelo mais forte). | Arquitetura / Custo | Média | `internal/state/`, `internal/llm/`, `internal/gemini/` |
| DT-29 | **Preferência de formato de resposta por produtor**: hoje o formato é decidido **por mensagem** (espelha a entrada: mandou áudio → responde áudio), não por pessoa. Não existe nenhuma coluna de preferência em `profiles` (verificado no banco). Decisão de produto tomada em 2026-08-22: o produtor deve poder escolher entre `audio`, `texto` e `ambos` por **três** caminhos — (1) comando no WhatsApp ("modo texto"), (2) seletor no painel do `pmo-frontend`, (3) botões interativos no primeiro contato (casa com o DT-13). Escopo: migration adicionando `preferencia_resposta` em `profiles`; leitura em `queue/response_mode.go` (`ShouldRespondWithAudio`), que já é o ponto central de decisão; handler do comando no webhook; UI no frontend. **Pré-requisito já concluído**: a entrega áudio+texto do DT-28. **⚡ BACKEND CONCLUÍDO EM 2026-08-23.** Migration `20260823100000` adiciona `profiles.preferencia_resposta` (`texto` | `audio` | `automatico`, NULL = nunca escolheu). `ports.ResolveResponseModeFor` e `Job.ShouldRespondWithAudioFor` passam a considerar a preferência, com precedencia: modo já resolvido a montante > preferência > campos legados > espelhamento. A preferência vence os campos legados de propósito — senão um job enfileirado antes do deploy faria quem acabou de pedir "modo texto" receber áudio mesmo assim. Comando no WhatsApp implementado em `state/response_preference_command.go`, interceptado antes de qualquer chamada de LLM (interpretar "modo texto" com um modelo gastaria cota e segundos para reconhecer duas palavras fixas), e a confirmação sai NO formato escolhido. O comando só dispara com a mensagem inteira, nunca por substring: aceitar trecho faria "não quero áudio de outra pessoa no meu talhão" reconfigurar o produtor em silêncio (coberto por teste). **DECISÃO DE PRODUTO REVISADA:** o default é `automatico` (espelhamento), não `texto`. Texto como padrão economizaria mais CPU, mas cobra na acessibilidade e cobra de quem menos pode pagar — quem manda áudio frequentemente o faz porque ler e escrever é custoso, e com áudio em opt-in essa pessoa precisaria LER uma instrução para descobrir o recurso. **FALTA:** (a) seletor no `pmo-frontend`; (b) botões interativos no primeiro contato (casa com DT-13); (c) aplicar a migration no Supabase — o código é seguro sem ela (a leitura falha, loga e cai no espelhamento), então dá para deployar em qualquer ordem. **Achado colateral corrigido:** `LogResponseModeDecision` computava a origem da decisão DEPOIS de o pipeline marcar o modo como explícito, então toda decisão era registrada como `explicit` e a métrica `ResponseModeResolutionTotal` ficava cega. Sem isso não haveria como medir a adoção da preferência, que é justamente o número que falta para dimensionar a VPS (DT-38). | UX / Produto | Média | `supabase/migrations/`, `internal/queue/response_mode.go`, `internal/webhook/`, `pmo-frontend/src/` |


| DT-93 | **`save_pmo_memory_cache` / `match_pmo_memory_cache` com `EXECUTE` aberto a `PUBLIC` (anon) — leitura/escrita de memória cross-tenant.** `supabase/migrations/20260905153000_b_create_pmo_memory_cache_rpc.sql:2,43` — `SECURITY DEFINER SET search_path = public` com `p_user_id`/`p_pmo_id` fornecidos pelo chamador e **sem** `REVOKE`/`GRANT` em nenhuma migration (grep confirmado). Default do Postgres é `EXECUTE TO PUBLIC` (inclui anon). Sem `auth.uid()` check, qualquer anônimo grava/sobrescreve o cache de memória de qualquer PMO e lê fragmentos cross-tenant. Sugestão: `REVOKE ALL` + `GRANT EXECUTE TO service_role` (o bot autentica com service role), ou `auth.uid() = p_user_id` + posse do `pmo_id` no corpo (padrão DT-18). | Segurança / RLS | 🔥 Alta | `supabase/migrations/20260905153000_b_create_pmo_memory_cache_rpc.sql:2,43` |
| DT-94 | **`create_or_supersede_mutation_draft` / `commit_mutation_draft` com `EXECUTE` aberto a `PUBLIC` (anon) — commit de mutações como qualquer owner.** `supabase/migrations/20260816010000_create_mutation_drafts.sql:44,121` — `SECURITY DEFINER SET search_path = public`, `p_user_id`/`p_pmo_id`/`p_draft_id` do chamador, sem REVOKE. A RLS da tabela protege SELECT por dono, mas as RPCs SD ignoram RLS e o `EXECUTE` está aberto: anon pode criar e commitar drafts que gravam em `caderno_campo`, `transacoes_financeiras`, `cotas_produtores` sem checagem. Sugestão: `REVOKE ALL` + `GRANT EXECUTE TO service_role` OU validar `auth.uid() = p_user_id` + posse do `pmo_id`. | Segurança / RLS | 🔥 Alta | `supabase/migrations/20260816010000_create_mutation_drafts.sql:44,121` |
| DT-95 | **RPCs de Knowledge Ops com `EXECUTE` aberto a `PUBLIC` — hijack da fila de ingestão, publicação arbitrária e corrupção do catálogo de modelos.** `20260721180100_knowledge_ops_rpcs.sql:10,49` (`claim_next_ingestion_job`, `publish_knowledge_version` — `SECURITY DEFINER` sem `SET search_path`, sem auth check, sem REVOKE) e `20260723120000_update_rag_arena_models_sync.sql:14` (`upsert_arena_models`). `claim_next_ingestion_job` entrega linha da fila a qualquer chamador que invente um `worker_id` (jobs presos em `extracting`); `publish_knowledge_version` move qualquer versão para `live`; `upsert_arena_models` reescreve o catálogo de modelos. Sugestão: `REVOKE ALL` + `GRANT EXECUTE TO service_role` + `SET search_path` explícito. | Segurança / RLS | 🔥 Alta | `supabase/migrations/20260721180100_knowledge_ops_rpcs.sql:10,49`, `supabase/migrations/20260723120000_update_rag_arena_models_sync.sql:14` |
| DT-96 | **`get_knowledge_role()` faz fallback para `user_metadata` (claim editável pelo usuário) — privilege escalation para `knowledge_publisher/editor`.** `supabase/migrations/20260721180000_knowledge_ops_panel.sql:14-21` — `COALESCE(app_metadata->>'knowledge_role', user_metadata->>'knowledge_role', 'none')`. `user_metadata` é atualizável pelo próprio usuário via client; autodeclarar o claim vira `knowledge_publisher` e todas as policies `kd_*`/`kv_*`/`ij_*`/`rql_*`/`rf_*`/storage herdam. Sugestão: ler **apenas** `app_metadata` ou derivar de tabela (`profiles`), nunca de `user_metadata`. | Segurança / RBAC | 🔥 Alta | `supabase/migrations/20260721180000_knowledge_ops_panel.sql:14-21` |
| DT-97 | **Policies `profiles` UPDATE e massa de `FOR ALL` do core sem `WITH CHECK` — reatribuição de ownership / tenant-shift por `pmo_ativo_id`.** `20260401_create_profiles.sql:61-64` — `FOR UPDATE USING (id = auth.uid())` sem `WITH CHECK`; `pmo_ativo_id` (perfil) é reatribuível pelo próprio usuário e autoriza o ledger financeiro (`20260525_create_financial_ledger.sql`, policies confiam no campo; re-exposto por `update_profile` em `20260818140000_create_domain_mutation_rpcs.sql:8-37`). `20260402000000_create_core_app_tables.sql` tem ~18 tabelas (`propriedades:26`, `pmos:46`, `talhoes:83`, `canteiros:108`, `ciclos_cultivo:130`, `analises_solo:163`, `caderno_campo:211`, `caderno_campo_canteiros:231`, `culturas_anuais:247`, `lotes_rastreabilidade:294`, `pmo_culturas:316`, `pmo_manejo:337`, `pmo_pragas:352`, `pmo_equipamentos:372`, `pmo_insumos:392`, `pmo_propagacao:412`, `pmo_limpeza:433`, `pmo_clima:453`) + `categorias_financeiras:142-149` + `conversations` admin ALL (`20260825020000:37-39`) com `FOR ALL` usando só `USING`. UPDATE de `user_id`/`pmo_id` pode migrar linha para outro dono (falta `WITH CHECK`); DELETE em massa permitido. Sugestão: policies separadas por operação com `WITH CHECK` replicando o `USING`; estender `trg_prevent_self_promotion` para proteger `pmo_ativo_id`. | Segurança / RLS | Alta | `supabase/migrations/20260401_create_profiles.sql:61-64`, `supabase/migrations/20260402000000_create_core_app_tables.sql` |
| DT-98 | **`knowledge_chunks` com policy `USING (true)` para authenticated — leitura cross-tenant total do conhecimento.** `supabase/migrations/20260823110000_sync_prod_orphan_functions.sql:78-84` — `FOR SELECT TO authenticated USING (true)` + `GRANT SELECT ... TO anon` (linha 84). Qualquer autenticado lê todo o conteúdo ingestado de todos os PMOs. Sugestão: escopar por `pmo_id` (padrão das demais tabelas) ou `get_knowledge_role()`; revogar grant de anon. | Segurança / RLS | Alta | `supabase/migrations/20260823110000_sync_prod_orphan_functions.sql:78-84` |
| DT-99 | **Policy de admin usa `auth.jwt() ->> 'role' = 'admin'` — sempre falsa para admins reais (anti-pattern de claim).** `supabase/migrations/20260721230000_rag_judge_runs.sql:36-41` — o claim `role` do JWT é o papel do banco (`authenticated`), não o papel da app (`profiles.role`). Admins ficam sem gerenciar `rag_run_judgments` (sub-privilegiado) e o check confunde quem lê. Sugestão: `EXISTS (SELECT 1 FROM profiles WHERE id = auth.uid() AND role = 'admin')`, padrão usado em ~40 policies do repo. Agravado pela fragilidade de `profiles.role` sem `WITH CHECK` (DT-97). | Segurança / RLS | Média | `supabase/migrations/20260721230000_rag_judge_runs.sql:36-41` |
| DT-100 | **`messages` — `GRANT SELECT, INSERT, UPDATE, DELETE` a `authenticated, anon, service_role` nunca revogado.** `supabase/migrations/20260610120000_evolve_messages_table.sql:116`. RLS hoje segura o acesso, mas o grant amplo é uma camada latente abaixo da RLS: qualquer erro futuro de policy em `messages` expõe todas as mensagens. Sugestão: `REVOKE ALL ON public.messages FROM anon` (e autenticados se a UI passa por RPCs). | Segurança / RLS | Média | `supabase/migrations/20260610120000_evolve_messages_table.sql:116` |
| DT-101 | **Grants a `anon` latentes em `knowledge_chunks` e `invalid_auth_token`.** `20260823110000_sync_prod_orphan_functions.sql:84-86` (`knowledge_chunks` → `GRANT SELECT` anon) e `20260823110100_add_n8n_invalid_auth_token.sql:46-47`. Bloqueados hoje pelas policies (target authenticated), mas qualquer policy futura mirando anon expõe tudo. Sugestão: `REVOKE ALL ... FROM anon`. | Segurança / RLS | Média | `supabase/migrations/20260823110000_sync_prod_orphan_functions.sql:84-86`, `supabase/migrations/20260823110100_add_n8n_invalid_auth_token.sql:46-47` |
| DT-102 | **`rpc_registrar_compra_insumo` — corpo continua IDOR, protegido só por grants (residual).** `20260607_fase2_ledger_rateio.sql:5,24` + `20260609002200_add_raw_payload_to_rpcs.sql:241` — `SECURITY DEFINER` sem `SET search_path`, aceita `user_id`/`propriedade_id` do chamador sem validar posse. Acesso hoje restrito a `service_role` via `20260824140000_revoke_dt65_gen1_rpc_grants.sql:22-25`, mas qualquer re-grant reabre o IDOR em silêncio. Sugestão: reescrever o corpo para derivar `user_id` de `auth.uid()` + validar posse (padrão do DT-65 para `rpc_registrar_transacao_com_rateio`). | Segurança / RPC | Média | `supabase/migrations/20260607_fase2_ledger_rateio.sql:5,24` |
| DT-103 | **`seed.sql` quebrado — `INSERT INTO profiles (id, user_id, full_name, role)` referencia colunas inexistentes.** `supabase/seed.sql:4`; o schema (`20260401_create_profiles.sql:6-16`) tem `id, nome, telefone, role, ...` — `user_id`/`full_name` não existem. `supabase db reset` aplica as migrations mas falha ao semear; quebra on-boarding local e é pré-requisito não resolvido para o DT-91 (`supabase test db` em CI). Sugestão: alinhar `seed.sql` ao schema. | Reproducibilidade | Baixa | `supabase/seed.sql:4` |
| DT-104 | **`rag_experiments` / `rag_experiment_runs` — observers leem snapshots de qualquer tenant (sem escopo por pmo).** `20260721221610_playground_benchmarks.sql:48-96` — policies gateiam por `get_knowledge_role()` (herdam o DT-96) e não escopam por `pmo_id`; `retrieved_chunks_snapshot` legível por qualquer observer. Sugestão: escopar por `pmo_id` nas duas tabelas. | Segurança / RLS | Baixa | `supabase/migrations/20260721221610_playground_benchmarks.sql:48-96` |
| DT-105 | **Bucket de storage `audios_audit` continua público (fora de SQL).** `20260822160000_create_audit_vault.sql` (comentado nas linhas ~18-21, 62 como P0-1). A tabela `audios_audit` é exemplar (FORCE RLS, titular-only, via `20260822170000_audios_audit_signed_url_policy.sql`), mas o bucket correspondente permanece público — áudios de auditoria acessíveis sem assinatura se a URL for conhecida. Sugestão: bucket privado + signed URLs on-demand (padrão de `audioSigningService.ts`). | Segurança / Storage | Baixa | `supabase/migrations/20260822160000_create_audit_vault.sql` |
| DT-106 | **Subsistema de memória do usuário sem governança nenhuma (`user_memory_profiles` sem RLS + `match_user_memory` com `EXECUTE` público). ⚡ Condicional.** `pmo-bot-go/migrations/008_create_user_memory.sql:5-14` cria a tabela sem `ENABLE ROW LEVEL SECURITY` (grep no repo confirmou ausência em qualquer migration) e `009_match_user_memory_rpc.sql:1-28` a expõe via função sem REVOKE (default `EXECUTE TO PUBLIC`), sem `auth.uid()` e sem check de posse — qualquer anônimo lê `fact`/`category` de memória de qualquer PMO se a tabela existir ao vivo. ⚡ A função referencia `pmo(id)` (tabela inexistente no repo) — só é explorável se as migrations do harness foram aplicadas em produção; confirmar via `psql` antes de remediar. Sugestão: bootstrap canônico (ENABLE RLS + policy owner-only + REVOKE/GRANT service_role, padrão DT-93) ou remoção das migrations órfãs. | Segurança / RLS | 🔥 Alta | `pmo-bot-go/migrations/008_create_user_memory.sql:5-14`, `pmo-bot-go/migrations/009_match_user_memory_rpc.sql:1-28` |
| DT-107 | **Policies `USING (true)` do harness leem tabelas de auditoria cross-tenant se aplicadas ao vivo. ⚡ Condicional.** `pmo-bot-go/migrations/005_guardrail_events.sql:163-167` (`admin_read_guardrail_events` — `FOR SELECT USING (true)` sem check de admin) e `006_hitl_pending.sql:145-149` (`admin_read_hitl` idem). Por padrão toda tabela criada sem RLS fica exposta ao grant default de anon (o hotfix `20260819000000_hotfix_rollback_grants.sql:8-12` só revogou TRUNCATE/I/U/D, não SELECT). ⚡ Só explorável se as migrations do harness foram aplicadas em produção — precedente de snippet permissivo offline aplicado em prod é o DT-70. Sugestão: reescrever as policies escopadas a admin (`SELECT ... FROM profiles WHERE role='admin'`, padrão canônico) e confirmar ao vivo se existem. | Segurança / RLS | 🔥 Alta | `pmo-bot-go/migrations/005_guardrail_events.sql:163-167`, `pmo-bot-go/migrations/006_hitl_pending.sql:145-149` |
| DT-108 | **Bucket `avatars` sem política de dono — uploads são públicos e colocáveis como "avatares" de qualquer PMO.** `pmo-frontend/supabase/migrations/20260328_add_avatar_to_profiles.sql:5-7,20-41` — criado público com `file_size_limit` e `allowed_mime_types`, sem `public/avatars/<id...>`; `profileService.ts:129` ainda usa `getPublicUrl` (URL previsível). Qualquer anônimo sobe/lê/subscreve `*` (segue vigente: hotfix só revogou I/U/D/TRUNCATE, não SELECT/INSERT). Sugestão: bucket privado + `Storage: { path: avatars/... }` siguiendo o padrão de `audios_audit` (DT-105) com signed URL; validar posse. | Segurança / Storage | 🔥 Alta | `pmo-frontend/supabase/migrations/20260328_add_avatar_to_profiles.sql:5-7,20-41` |
| DT-109 | **`CONECTAR` sem brute-force/dedupe/expiração — vincula dispositivo com qualquer código.** `internal/supabase/quota.go:172-218` — `LinkDeviceToWeb(phone, codigo)` seleciona por `codigo_vinculo`, sem throttle, janela de validade, anti-brute-force nem atomicidade (`..codigo_vinculo=eq.%s` em :174 e `..id=eq.%s` em :192 sem escape); FSM inicia em `fsm.go:235`. Adicionar-se-ia à superfície de exposição o PHONE: quem tiver o número tenta vincular-se. Sugestão: hash + expiração (5-10min) + tentativas/cool-down + transação única + `LIMIT 1` ordenado (seed/random), padrão OTP. | Segurança / Auth | 🔥 Alta | `pmo-bot-go/internal/supabase/quota.go:172-218` |
| DT-110 | **6 views de auditoria/relatórios do harness sem `security_invoker`.** `pmo-bot-go/migrations/004_analises_solo.sql:62-79,88-98`, `005_guardrail_events.sql:85-101,110-124`, `006_hitl_pending.sql:72-85,94-104` — sem `security_invoker = true`, rodam com permissões do dono e ignoram a RLS das tabelas base (mesma classe do DT-62, que fixou só `view_conversas_recentes`). Sugestão: `security_invoker = true` nas vistas + `REVOKE ALL ... FROM anon` (padrão do DT-62) ao promover para o schema canônico. | Segurança / RLS | Média | `pmo-bot-go/migrations/004_analises_solo.sql:62-98`, `pmo-bot-go/migrations/005_guardrail_events.sql:85-124`, `pmo-bot-go/migrations/006_hitl_pending.sql:72-104` |
| DT-111 | **Bucket `comprovantes` sem governança de dono — URLs previsíveis via `getPublicUrl`.** `dashboardService.ts:224,231` — bucket público com `public/comprovantes/` "gratuito" (public path); `storageBucketService.ts:49` confirma `getPublicUrl` (`anexos-pmos` idem). Se a URL for conhecida, baixa qualquer comprovante sem sessão. Sugestão: bucket privado + signed URLs com expiração curta (padrão `audios_audit`); revisar `anexos-pmos` junto. | Segurança / Storage | Média | `pmo-frontend/src/services/dashboardService.ts:224,231`, `pmo-frontend/src/services/storageBucketService.ts:49` |
| DT-112 | **`sessionMu` (FSM) sem eviction — sessões mortas acumulam na memória.** `internal/state/fsm.go:50-56` — sem TTL/eviction/limite; sessões abandonadas acumulam até OOM ou vazamento de estado entre usuários. Sugestão: LRU/TTL + cap (padrão de `session.go` de `evolution-go`); garantir `map[sessionID]` com lock único. | Performance / Memória | Média | `pmo-bot-go/internal/state/fsm.go:50-56` |
| DT-113 | **Goroutines sem controle no download de mídia.** `internal/webhook/handler.go:670,736` — `go func() {}` sem WaitGroup/errgroup, sem limite de concorrência nem recovery; em pico (rajada de mídias) cria N goroutines simultâneas; panics derrubam o processo. Sugestão: `errgroup.WithContext` + semáforo + recovery de panic (padrão já usado em `internal/state/utils.go`). | Robustez / Concorrência | Baixa | `pmo-bot-go/internal/webhook/handler.go:670,736` |
| DT-114 | **`utils.go:90,148` — dois helpers de download de mídia duplicados e não-queridos.** `internal/state/utils.go:90` (`DownloadMedia`) e `:148` (`DownloadMediaFromURL`); apenas um é usado, mas ambos baixam mídia do WhatsApp com a evolution-go. Sem retry/backoff/teto de tamanho robustos. Sugestão: unificar num helper único de download (com timeout + teto de tamanho) reusado pelos dois sites. | Robustez / Perf | Baixa | `pmo-bot-go/internal/state/utils.go:90,148` |
| DT-115 | **`weather/client.go:255` usa `http://` (insecure) com `q=%s` e HTML/`api_key` de produção.** `internal/weather/client.go:255` — URL HTTP para o serviço de clima; `q=%s` formatado sem encoding; consultas de produção passam em claro. Sugestão: `https://`, `url.QueryEscape`, e validar que a chave não vaze nos headers do PostgREST; consertar se estiver citada em qualquer log. | Robustez / Config | Baixa | `pmo-bot-go/internal/weather/client.go:255` |
| DT-116 | **Prompt de avaliação lido de path relativo (CWD).** `internal/knowledge/evaluator.go:181` — `os.ReadFile("prompts/...")` resolve contra o CWD do processo; se o container rodar de outro diretório (ou em testes), quebra silenciosamente a avaliação Knowledge Ops. Sugestão: `fs.FS` embutido (`embed`) ou path resolvido via `runtime.Caller` (padrão já usado nas tools). | Robustez / Config | Baixa | `pmo-bot-go/internal/knowledge/evaluator.go:181` |
| DT-117 | **`plantio.go` janela de deduplicação apertada — reenvio em rajada vira plantio duplicado.** `internal/jobs/plantio.go:18,65-76` — janela de 60s baseada no trigger; mensagens do WhatsApp caem em rajada e o mesmo evento chega 2-3x seguidas; a janela não resgata o `msg_id` como chave (DT-68 tinha o mesmo problema). Sugestão: dedupe por `msg_id` (padrão manifestado no DT-68) no insert do job. | Dados / Integridade | Baixa | `pmo-bot-go/internal/jobs/plantio.go:18,65-76` |
| DT-118 | **Bucket de runtime `audit-vault` criado fora do SQL — público se não tinha sido definido privado.** `internal/adapter/auditvault/adapter.go:70-76,100` — criação de bucket em runtime via `createBucket`; `audios_audit` também nasce do mesmo path (`:100`). Os buckets de runtime podem nascer públicos (padrão do Supabase storage, se `public` não vem explícito) — o que derruba toda a effort de privacidade do DT-105. Sugestão: definir buckets no SQL (privados, padrão `audios_audit` canônico) e remover a criação em runtime. | Segurança / Storage | Baixa | `pmo-bot-go/internal/adapter/auditvault/adapter.go:70-76,100` |
| DT-119 | **`/metrics` (Prometheus) aberto sem autenticação quando `PROMETHEUS_AUTH_TOKEN` não está configurado.** `cmd/server/main.go:325-330` — `promToken := os.Getenv("PROMETHEUS_AUTH_TOKEN"); if promToken != "" { BasicAuth } else { r.GET("/metrics", gin.WrapH(promhttp.Handler())) }`. Sem o token, qualquer um que alcance a porta lê `LLMCallTelemetry` (latências por modelo), contadores de alerta/Redis/memória — info disclosure, agravado no corte para IP público (DT-38). ⚡ Condicional: confirmar se a env está setada no `.env.prod`. Sugestão: exigir o token (falha fechada, padrão do middleware de auth) ou restrigir por rede/HTTPS no proxy. | Segurança / Observabilidade | Média | `pmo-bot-go/cmd/server/main.go:325-330` |
| DT-120 | **Gateway `/api/v1/rpc/*` sem rate limit por usuário — abuso autenticado martela o PostgREST.** Rotas em `cmd/server/main.go:396-399` (só `RequireAuth`), handler em `internal/gateway/rpc_proxy.go` — sem throttle algum, ao contrário do webhook do WhatsApp (`webhook/handler.go:116`, 1 req/4s) e do mapa (limiter via Redis). Um usuário autenticado pode chamar `create_talhao`/`update_pmo`/etc. em loop, gerando carga direta e barata contra o Postgres e logs. Sugestão: reusar `ports.RateLimiter` (padrão do mapa) no grupo `/api/v1`, com janela por `sub` do JWT. | Segurança / Abuso | Média | `pmo-bot-go/cmd/server/main.go:396-399`, `pmo-bot-go/internal/gateway/rpc_proxy.go` |
| DT-121 | **Goroutines sem `recover` fora do webhook — estende o padrão do DT-113.** `internal/knowledge/handler.go:511,514` (`go asyncFetchTelemetry` + `go func(){...judge...}` por run no playground, sem recover), `internal/guardrails/logger_supabase.go:66` (persistência assíncrona, 5s ctx, sem recover), `internal/state/fsm.go:118` (gravação do greeting em goroutine, sem recover), `internal/adapter/rabbitmq/consumer.go:110` (loop de consumo sem recover; `:133` ainda usa `context.Background()` sem timeout no handler). Um panic em qualquer um derruba o processo inteiro do bot. Sugestão: wrapper `safeGo` com `recover` (padrão de `disparo.go:45-56`) + semáforo nos pontos de rajada (logger/playground), e contexto com timeout no consumer. | Robustez / Concorrência | Baixa | `pmo-bot-go/internal/knowledge/handler.go:511-552`, `pmo-bot-go/internal/guardrails/logger_supabase.go:66-77`, `pmo-bot-go/internal/state/fsm.go:118-131`, `pmo-bot-go/internal/adapter/rabbitmq/consumer.go:110-134` |
| DT-123 | **`cap_add: SYS_TIME` desnecessário no bot e no evolution-go.** `docker-compose.prod.yml:75-76,117-118` — desde o DT-26 (correção do drift no host/W32Time) o `clockwork` só MEDE (sentinela, `:21-33`), não ajusta relógio; a capacidade de setar tempo do kernel viola o princípio do menor privilégio — um container comprometido ganha a borda de `SYS_TIME`. Sugestão: remover `cap_add` dos dois serviços quando houver rebuild. | Segurança / Infra | Baixa | `docker-compose.prod.yml:75-76,117-118` |
## ⚪ Bloqueado / Decisão Pendente

| ID | Item | Decisão Necessária | Opções |
|----|------|---------------------|--------|
| DT-08 | Política de acesso ao painel Knowledge Ops | Reabrir para produtores ou manter restrito ao admin? | (a) Abrir para produtores / (b) Restrito a admin |

## 🟡 Em Andamento

| ID | Item | Estado |
|----|------|--------|
| DT-58 | **Cadastro 100% via WhatsApp (Onboarding)** — extrair os dados iniciais do produtor no chat, removendo o atrito de web/PWA. | **DECIDIDO em 2026-08-24: opção (b), Extração One-Shot Estruturada**, com confirmação por botões. Motivo da escolha: o estado da FSM vive em memória (`history.Manager`, TTL 45min, `map[phone]*Conversation`) e some em restart/deploy — um slot-filling longo (opção *a*) perderia o cadastro pela metade e exigiria persistir estado fora da memória, trabalho que a (b) não pede. WhatsApp Flows (opção *c*) exigiria tocar `evolution-go-source`, mesma mecânica de rebuild que o DT-53 já adiou. **O alvo já existe e é pequeno:** a RPC `setup_initial_profile(p_user_id, p_nome, p_propriedade_nome, p_area_ha, p_talhao_nome)` cria Profile+Propriedade+Talhão numa transação e já é usada pelo portal web (`pmo-frontend/src/services/onboardingService.ts`) — são **4 campos** do produtor. **O bloqueio real é outro:** `profiles_id_fkey` é `FOREIGN KEY (id) REFERENCES auth.users(id) ON DELETE CASCADE` (verificado no schema vivo), e um produtor só-WhatsApp não existe em `auth.users`. Por isso o DT-58 depende do DT-59: quem cria esse usuário é o Go, via Admin API. **Já entregue (2026-08-24):** `CreateAuthUserByPhone`, `DeleteAuthUser` e `SetupInitialProfile` em `internal/supabase/auth_admin.go`. **Falta:** o fluxo em si, em `internal/state/fsm.go:196` — hoje o número desconhecido recebe `"❌ WhatsApp não vinculado. Vincule via portal web."` e o fluxo morre. Preservar o comando `CONECTAR <código>` (`LinkDeviceToWeb`), que continua sendo o caminho de quem já tem conta. Reusar `SendButton` (`internal/ports/whatsapp.go:18`, já usado no HITL em `tool_pipeline.go:96`) para a confirmação. **Cuidado de idempotência:** se o usuário for criado no `auth` e a RPC seguinte falhar, sobra órfão e a próxima mensagem tentaria criar outro — daí o `DeleteAuthUser` como compensação. |
| DT-37 | **Filtro de ferramentas por intent não filtra nada** (economia de custo por token — não é correção de performance, ver DT-33 em Concluído: payload refutado como causa dos timeouts). `GetToolsForIntent` inclui `CategoryRAG + CategoryDBWrite + CategoryDBRead` tanto para `RAG` quanto para `DATABASE` — 29/29 ferramentas nos dois casos, ~7.400 tokens por turno só de definição. | **Revisado em 2026-09-02, sem novo corte possível com segurança.** A única amostra de telemetria disponível (`logs/.telemetria_acumulada.log`, 26 eventos `tool_invoked`) continua sendo a mesma amostra sintética de 22/08 já analisada — não houve acúmulo de tráfego real desde então para dar base a um corte por intent sem risco de quebrar em silêncio um registro legítimo sob o intent errado (o próprio risco que motivou medir antes: `registrar_colheita`/`registrar_limpeza` já apareceram sob RAG na amostra). Verificado de novo, sem achar mais nenhuma instância: os fragmentos de *system prompt* vazados em descrições de tool (o padrão corrigido em `calcular_recomendacao_adubacao`) não se repetem em nenhuma outra das 29 ferramentas — a limpeza de 2026-08-22 já era completa. Sem ação adicional segura até `event=tool_invoked` acumular uma amostra real maior. |
| DT-64 | **Onboarding via WhatsApp (DT-58) travava em produção para número desconhecido, por quatro causas empilhadas.** Achado ao investigar por que o bot não respondeu ao áudio de um produtor real (2026-08-24): (1) `HandleOnboarding` pulava a pergunta "você já tem cadastro?" sempre que a mensagem tinha ≥5 palavras (`pareceConterDados`), e o LLM de extração às vezes "alucinava" um nome a partir de perguntas de domínio, travando o fluxo numa confirmação de cadastro errada; (2) o interceptor global de HITL (`handleHITLResponse`, `internal/webhook/handler.go`) sequestrava qualquer "SIM"/"NÃO" de número **sem perfil**, respondendo sempre "não encontrei rascunho pendente" antes da mensagem chegar no onboarding — reproduzido ao vivo com uma segunda produtora real, que nunca conseguiu vincular a conta por e-mail por causa disso; (3) o "Ultra-Fast Greeting Guard" (`internal/state/fsm.go`) interceptava saudações puras ("oi", "boa tarde") **antes** de checar se o número tinha cadastro, também travando o onboarding na primeira mensagem; (4) o `PIIScrubber` (guardrail de entrada) trocava o e-mail digitado por `[Email ocultado]` justamente no estado `aguardando_email`, quebrando a checagem de e-mail válido — o próprio fluxo pedia o dado sensível e o guardrail o apagava antes de chegar na lógica. | **Os quatro corrigidos e deployados em produção em 2026-08-24:** (1) extração ganhou um campo `eh_cadastro` decidido pelo próprio LLM (`internal/state/onboarding.go`), só avança para confirmação quando a mensagem é de fato dado de cadastro; (2) `handleHITLResponse` agora retorna cedo (sem tratar) quando o telefone não tem perfil (`internal/webhook/handler.go`); (3) o greeting guard passou a checar profile antes de disparar (`internal/state/fsm.go`); (4) `ai_worker.go` pula o `PIIScrubber` quando o estado da FSM é `aguardando_email`/`aguardando_otp_email`, resolvendo o telefone com `ResolvePhone` (mesma chave usada por `SetFSMState`) para casar o estado certo. Validado ponta a ponta em produção via webhooks sintéticos (número de teste fictício, sem afetar produtores reais) — cadeia completa até `otp_enviado` funcionando. **Achado colateral no mesmo teste, ainda em aberto:** o template de e-mail "Magic Link or OTP" do Supabase Auth só tinha `{{ .ConfirmationURL }}` (manda link clicável), não `{{ .Token }}` (código de 6 dígitos) — o bot pede pro produtor digitar um código que nunca existia. **Corrigido no template** (adicionado `{{ .Token }}`, testado e confirmado recebendo código). **Bloqueador real que sobra:** o projeto usa o serviço de e-mail **padrão/embutido do Supabase** (`noreply@mail.app.supabase.io`), documentado pela própria Supabase como *dev-only*, "best-effort", com rate limit baixo por projeto (não por destinatário) — 4 envios de teste em ~15min já foram suficientes para a cota parar de entregar. Resolve configurando **SMTP customizado** (Project Settings → Auth → SMTP Settings) com um provedor como Resend/SES/Postmark, o que exige um domínio próprio verificado (registros SPF/DKIM). **Decisão do responsável (2026-08-24): domínio fica para quando a VPS (DT-38/DT-45) for provisionada** — registrado aqui para não represar o DT-58 esperando por isso; o onboarding funciona ponta a ponta hoje para quem NÃO precisa vincular por e-mail (cadastro novo direto), só o caminho "já tenho conta, vincular por e-mail" fica bloqueado até o SMTP próprio existir. |

## 🟢 Concluído

| ID | Item | Concluído em | Observações |
|----|------|---------------|-------------|
| DT-74 / DT-75 | **`complete_onboarding` sem idempotência (duplicava propriedade e talhão em retry) e modalidade desconhecida caindo silenciosamente em `CONVENCIONAL`.** A RPC (`supabase/migrations/20260830_create_complete_onboarding_rpc.sql`) fazia `INSERT INTO propriedades`/`INSERT INTO talhoes` sem nenhuma guarda de existência prévia — qualquer chamada dupla (reload, duplo clique, resubmissão após resposta perdida) criava linhas duplicadas, quebrando `allPropriedades.length` no frontend. Em paralelo, o `ELSE` do mapeamento de modalidade convertia qualquer valor desconhecido (ex.: `'nao_sei'`) para `CONVENCIONAL` sem erro, podendo cadastrar produtor orgânico como convencional. | 2026-09-09 | Migration `20260909030000_dt74_75_complete_onboarding_idempotencia_modalidade.sql`. **DT-74:** fast-path checando `profiles.propriedade_ativa_id` — se já setado, retorna os ids existentes (`already_onboarded: true`) em vez de inserir de novo; dois índices únicos parciais (`propriedades(user_id) WHERE nome = 'Sítio / Fazenda'`, `talhoes(propriedade_id) WHERE nome = 'Sede'`) como rede de segurança contra corrida de chamadas concorrentes, capturados via `EXCEPTION WHEN unique_violation` (refaz o lookup e devolve os ids do vencedor da corrida em vez de erro). **DT-75:** modalidade fora do enum conhecido agora retorna `success: false` com erro explícito, em vez de cair em `CONVENCIONAL`. Verificado ao vivo em produção antes de escrever a migration que não havia duplicatas existentes para nenhum dos dois índices, então não precisou de etapa de limpeza. Testado em staging (`pmo-staging`): duas chamadas seguidas retornam os mesmos ids na segunda com `already_onboarded: true`, sem duplicar linhas; `p_modalidade_producao = 'nao_sei'` retorna erro explícito para usuário ainda não cadastrado. Aplicado em staging e produção (`pmo-inteligente`), migration registrada em `supabase_migrations.schema_migrations` com a versão que corresponde ao arquivo do repositório. Contrato de retorno ganhou o campo `already_onboarded` (opcional, frontend não precisou de mudança). **Nota de processo:** este item foi trabalhado por duas sessões em paralelo no mesmo diretório de trabalho — a versão final mescla a checagem de idempotência de uma com os índices únicos/tratamento de corrida da outra. **Fora de escopo, registrado à parte:** `setup_initial_profile` (RPC equivalente do onboarding via WhatsApp, `pmo-bot-go/internal/supabase/auth_admin.go:229`) tem o mesmo problema de idempotência do DT-74 e não foi tocada aqui. |
| DT-122 | **RabbitMQ de produção: credenciais externalizadas e portas restritas a localhost.** Credenciais deixaram de ser hardcoded (`admin`/`admin_password`) e passaram a vir de `${RABBITMQ_USER}`/`${RABBITMQ_PASSWORD}` (`.env.prod`, fora do repo); `5672`/`15672` deixaram de publicar em `0.0.0.0`. | 2026-09-07 | Corrigido no commit `7273552` ("hardening da Fase 0/3"), na mesma migração pra VPS que fechou o DT-38/DT-45 — mas nunca movido pra esta seção, ficou marcado como aberto por dois dias. **Reconfirmado ao vivo em 2026-09-09** (via SSH, antes de qualquer ação): `docker-compose.prod.yml` na VPS já usa as variáveis de ambiente; `docker ps` mostra `pmo-prod-stack-rabbitmq-1` com portas `127.0.0.1:5672->5672/tcp` e `127.0.0.1:15672->15672/tcp` (sem bind em `0.0.0.0`); `.env.prod` tem `RABBITMQ_PASSWORD` definida e **não** é mais o default fraco `admin_password` (checado por comparação, sem imprimir o valor). Nada pendente de código ou infra — só a atualização deste registro. |
| DT-125 | **`evolution-go` deixou de publicar `8082` em `0.0.0.0`.** Mesma correção do DT-122, mesmo commit. | 2026-09-07 | Corrigido junto no `7273552`. **Reconfirmado ao vivo em 2026-09-09:** `docker ps` mostra `pmo-prod-stack-evolution-go-1` com porta `127.0.0.1:8082->8082/tcp`. A ressalva de força-bruta sem rate limit no middleware de auth (`auth_middleware.go`) deixa de ser explorável de fora — só um processo já dentro da VPS alcança a porta agora; não abrir novo item, risco residual aceitável dado o escopo do bind localhost. |
| DT-82 | **Cota de ingestão de documentos burlável omitindo `pmo_id` no upload.** `internal/webhook/handler.go:613-641` — a checagem de cota (3 docs para non-pro) só era executada `if pmoIDStr != ""`. Enviar o request sem `pmo_id` pula a cota inteiramente e cria `ingestion_jobs` com `pmo_id = 0`, que são processados e cobram embeddings da conta global. | 2026-09-09 | Correção: `pmo_id` agora é obrigatório — handler retorna 400 se ausente ou ≤ 0, eliminando o bypass. A rota não usa `RequireAuth` (usa token estático compartilhado), então não há JWT disponível; decidiu-se por tornar o campo obrigatório em vez de adicionar autenticação JWT. Teste novo: `TestKnowledgeUpload_RejectsMissingPmoID` + `TestKnowledgeUpload_RejectsZeroPmoID` em `internal/webhook/knowledge_upload_test.go`. `go build`, `go vet` e `go test ./internal/webhook/...` verdes. Branch: `fix/dt82-quota-bypass`. |
| DT-126 | **Bug de Looping no Vínculo de Conta via WhatsApp (OTP).** O fluxo de vinculação OTP concluía visualmente com sucesso, mas o estado não persistia de forma que o Roteador identificasse o usuário futuramente, reiniciando o fluxo. Achado ao investigar um caso real em produção: a produtora Valéria Batista Soares completou a vinculação por OTP às 15:22 de 6/set, e às 18:33 do mesmo dia (mensagem nova, fora da janela da sessão em memória) o bot não a reconheceu como vinculada e reiniciou o onboarding do zero. | 2026-09-08 | O commit `4dc01f2` ("feat(multicanal): liga ChannelSender/DeliveryManager e conserta onboarding", 7/set 03:57) já continha a correção — reescreveu `onboarding.go`/`client.go`/`fsm.go` e adicionou o teste de regressão `internal/state/onboarding_otp_integration_test.go` (simula OTP → `LinkPhoneToUser` → `UpdateProfilePhone` → nova mensagem → `GetProfileByPhone` deve achar o perfil), que passa. **O caso relatado é anterior a essa correção** (log de 6/set, commit de 7/set), então não prova regressão. Confirmado ao vivo em produção nesta sessão: repo na VPS (`~/manejo-org-app-clean`) em `386f679`, que tem `4dc01f2` como ancestral; imagem `pmo-prod-stack-pmo-bot-go:latest` buildada em 8/set 20:44 UTC e container rodando a partir dela — ou seja, o binário em produção já inclui o fix. Este item só estava com o status desatualizado no documento; nenhuma ação de código adicional foi necessária. | DT-30 | **Pipeline de deploy do container** — causa raiz do DT-25. O `deploy_production.yml` nunca conseguia sequer aplicar migrations em produção, por uma pilha de causas independentes. | 2026-09-08 | **Histórico:** gitlink quebrado do `evolution-go-source` (sem `.gitmodules`) resolvido em 2026-08-23. `Tests & Coverage` corrigido em 2026-09-02 (5 causas: testes reais de Postgres sem infra no CI, erro de compilação não pego por `go vet` escopado, gate de cobertura de 80% nunca real, `go.sum` incompleto, `covdata` ausente por `setup-go` genérico) — nesse mesmo dia foi achado e removido um **primeiro** gitlink órfão do `mcp/` (clone do SDK da Microsoft, sem relação com `internal/mcp` deste projeto, commit `f426490`). **Essa correção do `mcp/` vivia só na branch `feature-multicanal`, nunca chegou ao `main`** — por isso, de forma independente e sem saber da anterior, esta sessão (2026-09-08) achou e removeu o **mesmo** gitlink de novo (`git rm -r --cached`, `.gitignore`) ao investigar por que o `main` ainda quebrava no checkout. Retrabalho real por falta de merge entre branches, mesma lição já registrada no episódio de duplicata do DT-45. **A correção que faltava:** o job de migrations usava `--db-url ${{ secrets.PROD_DB_URL }}`, secret nunca configurado — trocado pelo padrão oficial da Supabase (`supabase link --project-ref` com `SUPABASE_ACCESS_TOKEN`, seguido de `db diff --linked`/`db push` com `SUPABASE_DB_PASSWORD`). Com os dois secrets configurados, o `Verify Schema Parity` conseguiu pela primeira vez conectar de verdade em produção — e revelou 3 bugs reais de migration escondidos atrás do secret ausente (nunca haviam sido testados contra o banco real): policy duplicada no fix do DT-70, `conversations.tenant_id` declarada `UUID` quando `organizacoes.id` é `BIGINT`, e `profiles.tenant_id`/`pmos.organizacao_id` referenciadas em RLS sem nunca terem sido criadas (ADR-010 incompleto). Corrigidos os três. Por fim, achado o mais sério: `20260825020000_create_conversations_channel_abstraction` (base do multicanal) estava marcada como aplicada no histórico de produção, mas nenhum efeito dela existia de fato — nem a tabela `conversations`, nem `messages.conversation_id`. Removida do histórico (`DELETE` em `supabase_migrations.schema_migrations`, equivalente a `migration repair --status reverted`) e reaplicada de verdade via `db push --include-all` (flag removida logo depois, num commit separado, para não mascarar futuros casos reais de migration fora de ordem). **Confirmado ao vivo:** `conversations` e `channel_links` agora existem em produção; dois runs consecutivos do `Deploy to Production` passaram 100% verde (`Verify Schema Parity` + `Deploy Migrations to Production`), o segundo já sem `--include-all`. **Falta, registrado como novos débitos:** build/publish automatizado da imagem do `pmo-bot-go` (DT-128) e secrets para os workflows de E2E (DT-129) — este item cobria só o pipeline de *migrations*, que agora funciona ponta a ponta. |
| DT-41-STREAM | **Detectar stall por time-to-first-token (streaming)** — refinamento do DT-41. A correção anterior eliminava os retries inúteis, mas ainda gastava o `attemptTimeout` inteiro (25s) antes de reconhecer um stall, porque a chamada não-streaming só sabe "terminou" ou "não terminou". Só o TTFT distingue "modelo pensando" de "conexão morta" *durante* a chamada; por latência total isso é impossível, pois há respostas legítimas em 17s, 19s, 22s e 23s (verificado na amostra do DT-41). | 2026-09-08 | `CallGoogle` (`internal/gemini/client.go`) migrado de `GenerateContent` para `GenerateContentStream`: uma goroutine consome o iterador do SDK e publica cada chunk num channel; `assembleFromStream` (função nova, extraída para ser testável sem um `*genai.Client` real) monta a resposta incrementalmente (texto concatenado, tool calls acumuladas, `ThoughtSignature`/`Usage`/`Model` do último chunk que os traz) e mede o time-to-first-token. Se nenhum chunk chega dentro de `ttftStallTimeout` (12s, valor provisório — ainda não há dado real de TTFT em produção, só de latência total; revisar quando `ttft_ms` acumular tráfego real, mesmo método do DT-33/DT-41), cancela a chamada e devolve um erro que embrulha `context.DeadlineExceeded` — o mesmo contrato que `isStallError`/`classifyFallbackReason` já reconheciam, então `withFallback` escala de imediato **sem nenhuma mudança lá**. `ttft_ms` passou a ser emitido em todo `event=llm_provider_call` (sucesso ou erro), inclusive quando o próprio stall acontece (`ttft_ms=stall`). 6 testes novos cobrindo a lógica extraída (`assembleFromStream`/`mergeStreamResponse`): montagem incremental de texto, acumulação de tool calls, stall sem nenhum chunk a tempo (com prova de que não fica preso no timeout inteiro), erro no meio do stream preservando o TTFT observado até ali, canal fechado sem erro terminando limpo, e merge que não sobrescreve `Model` com valor vazio de um chunk posterior. `go build ./...`, `go vet ./...` e suíte completa do pacote `internal/gemini` verdes; suíte do projeto inteiro rodada, única falha (`TestIdempotency_RealPostgreSQL_Integration`) confirmada pré-existente e não-relacionada (falha idêntica com as mudanças revertidas). **Limitação honesta, mesmo padrão já registrado no DT-53:** não há interface para mockar `c.Client.Models` do SDK, então o caminho real de rede (goroutine + `GenerateContentStream` de verdade) segue sem teste automatizado — só a lógica de montagem/stall, que é o que mudou de fato. `-race` não roda neste shell Windows (exige cgo, mesma limitação já documentada). **Falta:** validar `ttftStallTimeout` com telemetria real de produção antes de apertar (ou afrouxar) o valor. |
| DT-53 | **Self-healing do heartbeat.** O heartbeat de `sendHeartbeat` detectava `WhatsApp DISCONNECTED` a cada 60s mas nunca reagia — só logava e gravava em `bot_status`. Confirmado por DOIS incidentes reais no mesmo dia (2026-08-23): DT-52 (24min, `StreamReplaced` engolido em silêncio pelo fork) e um segundo incidente descoberto e diagnosticado ao vivo durante esta própria sessão (**36min35s**, 17:29–18:05, DNS transitório + auto-restart de tiro único do `evolution-go` + `connected=false` impedindo religar no boot — um QUARTO caminho de falha distinto do DT-52, mesmo sintoma). | 2026-09-08 | **Estágios 0, 1, 2 e 3 implementados e testados, deploy e dry-run concluídos nesta sessão** (a VPS do DT-38 já estava no ar). `SELF_HEAL_DRY_RUN=false` aplicado em `.env.prod`, `pmo-bot-go` reiniciado, log confirma `self-healing ATIVO`/`modo=ativo` e sondagem saudável (`connected=true logged_in=true`) — o self-heal agora reconecta de verdade, não só alerta. **Achado e resolvido durante o Estágio 3, ver DT-30:** o código do Estágio 3 vive em `evolution-go-source/`, que era um gitlink quebrado sem `.gitmodules` — as edições ficaram em disco, sem controle de versão algum, até o gitlink ser removido e o diretório passar a ser versionado como pasta normal deste repositório (mesmo commit que entrega este Estágio 3). **Desvio importante do plano original:** a investigação achou que `POST /instance/reconnect` (o que estava planejado) **recusa exatamente o caso que se quer curar** — `ensureClientConnected` devolve `client disconnected` quando o cliente existe no pool com o socket morto, e `ReconnectClient` nunca chega a ser chamado. A ação real usa `POST /instance/forcereconnect/:id` (rota admin, mas a chave do bot já alcança — confirmado ao vivo). **Estágio 0** (`internal/ports/notifier.go`, `internal/notify/`): Telegram (push) + e-mail (fallback) em cascata, com cooldown por chave que fura na recuperação, tudo atrás de `notify.Disparar` (goroutine própria, `context.WithoutCancel`, `recover`, nunca bloqueia nem derruba quem chamou — mesma lição do defeito (c) do DT-32). **Estágio 1** (`internal/selfheal/`): máquina de estados pura (`classificar`) distinguindo `caida` (recuperável) de `deslogada`/`nao_autorizada` (nunca age, só alerta) via o campo `Jid` de `/instance/info` — a trava central contra QR automático, testada isoladamente. `Healer.HealOnce` aplica graça (2 sondagens, dá margem ao auto-restart nativo do evolution-go), backoff (2/5/15/30min), teto de 4 tentativas por incidente, cooldown de 6h antes de reabrir um novo lote, e teto móvel de 10 tentativas/24h. Trata corretamente o falso negativo conhecido do `forcereconnect` (dorme 2s e devolve 500 mesmo quando a reconexão real vai funcionar em ~10s) — decide sempre por nova sondagem, nunca pelo status HTTP do POST. **Estágio 2** (`internal/webhook/handler.go`, `ExtractEventType`): o bot já recebia webhooks de `CONNECTION` e os descartava (`ParseWebhook` devolve `nil` pra tudo que não é mensagem) — agora um evento `Disconnected/ConnectFailure/LoggedOut/Connected` acorda a sondagem em segundos em vez de esperar até 60s, sem furar a graça. `QRCode` é tratado à parte como **disjuntor**: desliga o healer permanentemente na execução corrente e alerta uma única vez — terceira trava contra QR, complementar às duas primeiras (`Gateway` sem método `Connect`, e o check de `Jid` em `classificar`). **Dois bugs reais achados pelos próprios testes, corrigidos antes de reportar sucesso:** uma corrida nos testes (checagem rodando antes da goroutine assíncrona do alerta terminar, corrigida com sinal por canal) e um bug de lógica de verdade — depois do cooldown pós-esgotamento expirar, o código resetava o estado mas nunca chegava a tentar de novo, só devolvia; ficava preso até o próximo tick. **Flags:** `SELF_HEAL_ENABLED` (default `false`) e `SELF_HEAL_DRY_RUN` (default `true` quando ligado — percorre toda a máquina de estados e dispara todo alerta, mas nunca chama `forcereconnect`; é o modo de estreia contra a sessão real, risco zero). **Para ativar de verdade:** configurar `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` (ou SMTP) e `SELF_HEAL_ENABLED=true` no `.env.prod`, rebuild do `pmo-bot-go` (não do fork). Build limpo, `go vet` limpo, suíte nova (selfheal + notify + evolution) rodada 5× seguidas sem flakiness, suíte completa do projeto sem novas falhas. **Estágio 3** (`evolution-go-source/pkg/whatsmeow/service/whatsmeow.go`, `pkg/instance/service/instance_service.go`): **F1** — `Reconnect` deixou de checar `ensureClientConnected` antes de delegar a `ReconnectClient`, que já trata todos os estados sozinho (o gatekeeper era o motivo de `/instance/reconnect` não funcionar pro caso comum). **F2** — o `return` que matava a sessão em silêncio num soluço de rede (a causa exata do DT-56) virou retentativa limitada (5/10/20/40/80s) via `retryConnect`, e a desistência final persiste `disconnect_reason` e dispara webhook `ConnectFailure` via `reportarFalhaDeConexao` — sem isso o self-heal do bot só descobriria pelo poll de 60s. **F3** — `StreamReplaced` (a causa do DT-52) deixou de só logar e dar `return`: agora persiste estado e dispara webhook, nas três listas de categoria `CONNECTION` (`CallWebhook` e as dela duas filas globais AMQP/NATS). Deliberadamente **só reporta, nunca reconecta sozinho** — evita duas autoridades de reconexão brigando pela mesma sessão (ping-pong). **F4 não implementado** (opcional no plano original — `AllowQR` em `ClientData` fecharia de vez a terceira trava contra QR; fica pra depois, o disjuntor por webhook do Estágio 2 já cobre esse caso na prática). Testado com fakes de `WhatsmeowService`/`InstanceRepository`/`Producer` (5 testes nos dois pacotes) — `retryConnect` em si não é testável sem um `*whatsmeow.Client` real, registrado como limitação honesta. Build/vet nativo neste ambiente Windows não cobre o binário inteiro (um pacote não-relacionado, `sendMessage`, exige cgo que não está configurado aqui — o Dockerfile já usa `CGO_ENABLED=1`, isso é só limitação do shell local); os dois pacotes tocados compilam, passam vet e testam limpos isoladamente. Nada pendente. |
| DT-127 | **Webhook do evolution-go entrega duplicada por ciclo — segunda via chegava com `Authorization` vazio, mascarada por `handleWebhook` sempre retornar 200.** A causa raiz real, confirmada ao vivo em produção via SSH/`docker logs` na VPS Hostinger recém-provisionada: **não era race condition nem nada externo** — era sempre a MESMA entrega que falhava, sempre pelo mesmo motivo. O binário do `evolution-go` rodando em produção era uma versão anterior ao código já presente no repo: nunca setava o header `Authorization` em `sendWebhook` (`evolution-go-source/pkg/events/webhook/webhook_producer.go`), então a entrega **global** (`p.url`, da env `WEBHOOK_URL`, sem `?token=`) sempre saía sem autenticação nenhuma e caía em `token_invalid`; só a entrega **por-instância** (registrada via `ConfigureWebhooks` com `?token=` embutido na URL) passava, porque o `handleWebhook` deployado (também versão antiga) ainda lia `c.Query("token")` antes do header. O fix (mover a autenticação inteiramente para o header `Authorization: Bearer`, dos dois lados) já estava escrito no repo havia tempo, só nunca tinha sido commitado nem reployado. | 2026-09-08 | Investigação ao vivo: SSH na VPS (`82.25.79.6`), `docker logs` cruzados de `pmo-bot-go` e `evolution-go` no mesmo timestamp confirmaram o padrão determinístico (mesma URL falhando sempre); `git diff` no `webhook_producer.go` mostrou que o header `Authorization` nunca tinha sido commitado. **Corrigido de vez** um resquício inconsistente do próprio WIP: `pmo-bot-go/internal/config/config.go` tinha uma `WebhookURLComToken()` que continuava embutindo `?token=` na URL registrada mesmo depois do `handleWebhook` migrar para header-only — undermining exatamente o vazamento que o fix dizia estar fechando (token em texto plano na SQLite do evolution-go). Removida essa função e os dois call-sites em `pmo-bot-go/cmd/server/main.go` voltaram a usar `cfg.WebhookURL` puro — a autenticação agora é 100% via header dos dois lados. `go build ./...`, `go vet ./...` e os 3 testes de `webhook_auth_test.go` (header autentica, query não autentica mais, token errado é rejeitado) verdes antes do commit. **Deploy também concluído na mesma sessão:** commit cherry-picked para `main`, push, `git pull` na VPS, rebuild das imagens `evolution-go`/`pmo-bot-go` e restart dos containers — confirmado ao vivo nos logs pós-restart que a entrega global passou a autenticar (zero ocorrências de "Token inválido" desde então). |
| DT-38 | **Migrar a stack para VPS.** Produção rodava na máquina local do desenvolvedor (Docker Desktop/WSL2) — origem do clock drift (DT-26), bloqueava qualquer pipeline de deploy (DT-30) e caía toda vez que a máquina desligava. | 2026-09-07 (achado em 2026-09-08) | **Descoberto já concluído**, não executado nesta sessão: ao usar o conector MCP da Hostinger por outro motivo, encontramos uma VPS real (`srv1961891.hstgr.cloud`, Ubuntu 24.04, IP público `82.25.79.6`, criada em 2026-09-07) já rodando a stack completa (`pmo-prod-stack-*`: evolution-go, pmo-bot-go, caddy, frontend, rabbitmq, redis, clockwork, piper) — confirmado via SSH/`docker ps`. O board nunca tinha sido atualizado para refletir a migração. **⚠️ Ressalva importante, não confirmada:** este item estava explicitamente **bloqueado pelo DT-45** (rotacionar a API key da Evolution, exposta em texto puro num commit público, ANTES de provisionar a VPS) — e o **DT-45 continua aberto no board**, sem evidência de rotação. Se a chave não foi de fato rotacionada antes do cutover, a credencial pode estar exposta rodando em produção real neste exato momento — tratar o DT-45 como ainda mais urgente do que antes, não como resolvido por tabela. |
| DT-124 | **Higiene do repositório git: binários, sessão WhatsApp (LGPD) e segredos no histórico, .git com 365 MiB.** Achado na varredura de higiene: `pmo-bot-go/cmd/server/seu-bot` (42,5 MB) commitado; histórico com `tmp_test_bin` (44 MB), `tmp_bot` ×3 (~101 MB), `pmo_bot/wppconnect-profiles/` (~75 MB de cache de sessão WhatsApp com dados pessoais — cluster LGPD do DT-42), `temp_benchmark.exe`, `scratch/benchmark_massivo_test.exe`, `repomix-output.xml`, `.skills_temp/`, `test_scanned.pdf`; e segredos reais em commits: chave da Groq em `.env.prod`/`.env`, `sb_secret_*` + `gsk_*` (Supabase/Groq) em 3 scripts JS de check/cleanup e em `deploy-aci.yml:26,28`, e PAT do Azure DevOps em `.vscode/mcp.json:15`. 9 PDFs duplicados entre `docs/knowledge_base/` e `pmo-bot-go/docs/knowledge_base/` (~5,6 MB, menor). | 2026-09-06 | **Etapa 1** (commit 4ac4f07/reescrito, branch `fix/bigpickle-bugfix-loop`): `.gitignore` da raiz ganhou `pmo-bot-go/tmp_server`, `tmp_bot`, `cmd/server/seu-bot` (além de `tmp_test_bin*`, `bin/`); `git rm --cached pmo-bot-go/cmd/server/seu-bot`; push ok. **Etapa 2**: backup mirror em `C:\Users\T-GAMER\AppData\Local\Temp\opencode\manejo-org-backup-20260906.git`; WIP preservado em snapshot `manejo-org-wip-20260906`; 4 rodadas de `git filter-repo` removendo binários, `pmo_bot/`, `.env*`, os 3 scripts JS, `deploy-aci.yml` e `.vscode/mcp.json`; `git reflog expire` + `git gc --prune=now --aggressive`; force-push de **todas as branches e tags** (decisão explícita do responsável). 3 pushes foram bloqueados pelo **GitHub Push Protection (GH013)** com os segredos listados — removidos do histórico em vez de dar "unblock-secret" (que deixaria o segredo no repo). **Resultado final:** size-pack 365 → **163,72 MiB** (-55%); in-pack 13.828 → 7.692; **0 branches divergentes; 0 commits com segredo** (verificado com `git grep` por `sb_secret_`, `gsk_`, `AZURE_DEVOPS_PAT`, `mcp.json` → 0 hits); tags reescritas (forçadas: v0.1.10/v0.14.0/v0.5.0/v0.6.1/v0.6.2/v0.6.3/v1.0.0-python-mvp; novas: phase-4-start-, phase-4-start-20260730202246, pre-refactor-, v0.1.11). Segue vivo com histórico 100% clean: origem em `https://github.com/thebrunm97/manejo-org` (público). Todos os __HEAD__ do repo foram reescritos. |
| DT-70 | **IDOR de leitura/escrita ao vivo em produção: 4 tabelas com policy RLS permissiva sem checar dono.** Achado ao investigar o DT-18. `canteiros`, `analises_solo` e `culturas_anuais` tinham como ÚNICA policy `auth.role() = 'authenticated'` (sem checar dono nenhum) — qualquer usuário autenticado lia/escrevia dados de qualquer outro produtor via `supabase.from(...).select()`/`.update()` direto. `pmo_propagacao` tinha a policy correta (escopada por dono) **e** uma permissiva (`qual: true`) ao lado — Postgres combina policies PERMISSIVE com OR, então a permissiva anulava a correta mesmo assim. Nenhuma das 4 policies permissivas tinha migration correspondente no repositório — foram criadas fora de banda direto em produção (mesmo padrão de drift do DT-22), nunca chegando a existir no Postgres local. | 2026-09-03 | Migration `20260903100000_fix_dt70_permissive_rls_policies.sql`: remove as 4 policies permissivas; para `canteiros`/`analises_solo`/`culturas_anuais`, recria a policy correta (escopada via `talhao_id`→`talhoes.user_id` ou `pmo_id`→`pmos.user_id`, replicando exatamente o que já era o desenho original em `20260402000000_create_core_app_tables.sql` e já estava ativo no Postgres local); para `pmo_propagacao`, só remove a permissiva (a correta já existia). Validado antes de tocar produção: simulei o estado real de produção dentro de uma transação no Postgres local (recriando as 4 policies permissivas), apliquei a migration, confirmei o estado final correto, e dei `ROLLBACK` — local nunca foi alterado de verdade. Aplicado em produção via MCP do Supabase, confirmado ao vivo (`pg_policies`) que as 4 tabelas agora têm exatamente 1 policy cada, todas escopadas por dono. Reconciliado o drift de migration history esperado (`apply_migration` grava sob timestamp automático) via `supabase migration repair`. |
| DT-18 | **Caminhos de escrita direta quebrados silenciosamente no frontend (Secao9.tsx, pmoService.ts, PropertyProfilePage.tsx).** A migration com as 5 novas RPCs `SECURITY DEFINER` já existia no repositório (`20260903110000_create_dt18_remaining_mutation_rpcs.sql`), mas não tinha sido aplicada em produção, resultando em erro `function does not exist` que era engolido no frontend. | 2026-09-05 | Confirmado na base de código que o frontend **já utilizava** os nomes corretos para as RPCs. O trabalho resumiu-se a rodar `supabase db push --linked --include-all` em produção, o que aplicou a nova migration e fechou os últimos 3 caminhos de escrita direta, reestabelecendo a operação normal. |
| DT-07 | **Duas linhas de ingestão de RAG paralelas e desconectadas — nenhum dos 17 PDFs reais era pesquisável pelo bot.** A pergunta original ("aposentar `rag_ingest.py` em favor do `cmd/ingestor`, ou manter híbrido?") partia de premissa errada: os dois eram estágios do MESMO pipeline morto, não alternativas. O achado real: a extração boa (PyMuPDF, ADR-007) gravava em `knowledge_chunks` com embeddings Gemini (3072d), tabela que nenhuma tool do bot consulta; `consultar_base_conhecimento` sempre leu `farm_documents.embedding_1024` (OpenRouter bge-m3, 1024d), onde só havia dados de teste (`integration_test_doc.pdf`, `obs_test.pdf`). | 2026-09-02 | Plano completo em [`PLAN-rag-ingestion-unification.md`](./PLAN-rag-ingestion-unification.md). `scripts/rag_ingest.py` (raiz do repo) adaptado: mantém o extrator PyMuPDF (ADR-007, addendum registrado), troca Gemini por OpenRouter/bge-m3 (mesmo contrato de `internal/supabase/client.go:GetEmbedding`) e passa a gravar direto em `farm_documents` via upsert em `chunk_hash` (mesmo contrato de `UpsertFarmDocumentChunks`). Validado primeiro em Postgres local (2 PDFs pequenos, confirmado que `match_documents_with_context` recupera o conteúdo novo com acentuação intacta), depois rodado contra produção para os 17 PDFs reais de `docs/knowledge_base/`: 16 documentos/840 chunks na primeira passada; `L10831.pdf` (PDF puramente escaneado, sem camada de texto) ficou de fora porque o PyMuPDF não faz OCR. **Fechado de vez no mesmo dia:** adicionado fallback automático de OCR (`ocr_pdf_to_text`) — quando a extração normal volta vazia, roda `ocrmypdf`/Tesseract (idioma Português) via Docker (`jbarlow83/ocrmypdf`) antes de desistir, cobrindo esse e qualquer PDF escaneado futuro sem intervenção manual. Rodado contra `L10831.pdf`: OCR extraiu 7.353 caracteres, 7 chunks gravados. **Total final em produção: 17 de 17 documentos, 847 chunks**, retrieval real confirmado via `match_documents_with_context` para ambos os casos (extração direta e via OCR). Código morto removido: `pmo-bot-go/scripts/rag_ingest.py` (cópia antiga, ainda em Docling, nunca removida desde a ADR-007 em 07-24) e `pmo-bot-go/cmd/ingestor/` (ingestor Go para `knowledge_chunks`, zero chamadores). `go build ./...` limpo após a remoção. |
| DT-67 | **Isolamento multi-tenant nas tools MCP era convenção de quem escreve o handler, não propriedade do dispatcher — e havia um caso furado no financeiro.** Levantado em 2026-09-01 ao revisar a arquitetura do agente. O buraco real: nada no tipo nem no fluxo impedia um handler de derivar o tenant dos **argumentos do LLM** em vez do perfil da sessão — era o que `handleConsultarBalancoFinanceiro` fazia, lendo `args["propriedade_id"]` vindo do modelo em vez de `profile.PropriedadeAtivaID`, contra uma RPC autenticada com a service_role key (ignora RLS). | 2026-09-01 | **Item (1)** (já registrado antes desta entrada consolidar tudo): `handleConsultarBalancoFinanceiro` parou de ler `propriedade_id` de `args`. **Item (2):** `ToolHandler` (`internal/mcp/server.go`) trocou `profile *supabase.Profile` por `tenant TenantCtx` (`PmoID`, `UserID`, `PropriedadeID`, `Telefone`) — um handler que só enxerga `TenantCtx` não tem mais como ler um id de tenant de outro lugar que não seja a sessão validada. Nova função `buildTenantCtx(profile) (TenantCtx, error)` centraliza validação (profile nulo / `PmoAtivoID == 0`) e construção, chamada por `CallTool`/`CallToolWithGuard` antes de qualquer handler rodar; as assinaturas públicas desses dois métodos não mudaram. Os ~29 `if profile == nil` redundantes nos handlers (ruído identificado na análise original, não a vulnerabilidade em si) desapareceram como efeito colateral da troca de tipo. Achado e corrigido de passagem no mesmo padrão: `tools_memory.go` declarava `pmo_id`/`phone_number` no schema JSON da tool sem o handler jamais os usar. Caso especial preservado via `context.Value` estreito (`withTenantUpdater`/`tenantUpdaterFromContext`, `internal/mcp/server.go`): `handleCadastrarPropriedade` precisa mutar o profile compartilhado em memória para que chamadas seguintes na mesma resposta do LLM já vejam a nova propriedade ativa sem reconsultar o banco. **Item (3):** `internal/mcp/tenant_guard_test.go`, dois testes novos — `TestNoHandlerReadsTenantIDsFromArgs` varre via `go/ast` todo `tools_*.go` não-teste do pacote em busca de um índice literal `args["pmo_id"\|"pmo_ativo_id"\|"propriedade_id"\|"propriedade_ativa_id"\|"user_id"\|"telefone"]`, tornando a garantia verificável para handlers futuros, não só os corrigidos agora — validado injetando deliberadamente uma leitura proibida num handler e confirmando que o teste falha, depois revertendo; `TestAllRegisteredToolsAreWellFormed` itera `GetAllMCPTools()` garantindo nome, handler e unicidade de cada ferramenta registrada. **Item (4):** `RegisterTool` (`internal/mcp/server.go`) passou a aplicar `wrapWithMiddleware` incondicionalmente — antes só envolvia o handler quando `tool.Options != nil`, deixando 16 das 29 ferramentas sem suporte a `dry_run`. **Item (5) — dedup de `message_queue` por `msg_id` (levantado 2026-09-06, na onda de branch-hygiene):** `Manager.Enqueue` (`internal/queue/manager.go`) mandava `POST /rest/v1/message_queue` sem `on_conflict=msg_id` — o PostgREST usava a PK (`id`, UUID novo a cada chamada) como alvo do `ON CONFLICT` e a deduplicação prevista **nunca disparava**, mesmo com o índice único `idx_mq_msg_id` já versionado (`20260901115000_add_message_queue_msg_id_unique.sql`, DT-22). Corrigido adicionando `?on_conflict=msg_id` ao POST. **Verificação:** `go build ./internal/mcp/...`, `go vet ./internal/mcp/...` e `go test ./internal/mcp/...` (27 testes, só os de integração contra `DATABASE_URL` pulados) verdes. **Relação com o DT-66:** aquele item apontava que a suíte de multitenancy não testava isolamento de fato; vários testes ali precisaram ser reescritos nesta correção porque testavam uma checagem (`if profile == nil` no handler) que deixou de existir — a validação real hoje só vive em `buildTenantCtx`. |
| DT-62 | **View `view_conversas_recentes` fazia bypass de RLS — telefones e mensagens de todos os produtores expostos a `anon`.** Achado numa auditoria de segurança do schema Supabase (2026-08-24). A view rodava com as permissões do dono (`postgres`), que ignora RLS por padrão mesmo no Postgres 15+ a menos que marcada com `security_invoker = true`, e tinha `GRANT SELECT` para `anon` — qualquer chamador anônimo via PostgREST lia a lista completa de conversas, bypassando a policy "Admins select all messages" que protege a tabela `messages` de origem. | 2026-08-24 | **Correção de registro (2026-09-01):** esta entrada ficou marcada como aberta no board por mais de uma semana depois de já corrigida — a proteção real (`security_invoker = true`) foi aplicada em produção em **2026-08-24**, via `supabase/migrations/20260824120001_fix_view_conversas_recentes_security_invoker.sql`, mas o board nunca foi atualizado para refletir isso. Achado só ao investigar o DT-22 (schema drift): o arquivo e o registro de migration já batiam entre local e produção desde aquela data. Uma sessão posterior (2026-09-01, ver histórico) reabriu o item por engano lendo o board como fonte de verdade, e adicionou só um reforço de defesa em profundidade (`REVOKE ALL ... FROM anon`, já que `security_invoker` sozinho não removia o `GRANT` inofensivo mas desnecessário) — não fechava uma exposição ativa, que já não existia. Lição registrada no DT-22: o board pode ficar defasado da produção real; conferir o estado ao vivo (`has_function_privilege`, `pg_class.reloptions`, etc.) antes de tratar uma entrada 🔴 como corrigível do zero. |
| DT-69 | **Achado ao escrever o teste real do DT-66: `consultar_balanco_financeiro` retornava receitas/despesas/saldo sempre zerados em produção, silenciosamente, desde ~2026-08-16.** `rpc_registrar_transacao_com_rateio` passou a normalizar `tipo` com `LOWER(p_payload->>'tipo')` a partir de `20260816000000_add_idempotency_to_mutations.sql` (mantido em `20260824140000_revoke_dt65_gen1_rpc_grants.sql`), então toda despesa/receita gravada desde então ficou minúscula (`'despesa'`/`'receita'`). `rpc_get_balanco_ia` (`20260526060000_create_rpc_balanco_ia.sql`) nunca foi atualizada e seguia comparando `tipo = 'DESPESA'`/`tipo = 'RECEITA'` maiúsculo — nenhuma linha gravada depois de 16/08 batia com o filtro, então o "CFO de bolso" respondia sempre com balanço zerado, sem erro algum. | 2026-09-01 | Nova migration `20260901130000_fix_balanco_ia_tipo_case_mismatch.sql`, recriando `rpc_get_balanco_ia` com `LOWER(tipo) = 'despesa'`/`'receita'` nas três agregações (receitas, despesas, top 3 despesas por categoria) — cobre tanto as linhas novas (minúsculas) quanto qualquer linha antiga gravada em maiúsculo. Confirmado ao vivo contra o Supabase local: inserida uma despesa de R$123.456,78 via `rpc_registrar_transacao_com_rateio`, `rpc_get_balanco_ia` retornava `despesas: 0` antes da correção e `despesas: 123456.78` depois. **Aplicado em produção** (`hejewayflbuemnffrhae`) no mesmo dia, via MCP do Supabase (`apply_migration`, escopado só a esta função — não um `db push` genérico, para não arrastar outras migrations locais pendentes/divergentes do remoto). Verificado após aplicar: `pg_proc.prosrc` da função em produção contém `LOWER(tipo)`. Checado também que nenhuma transação financeira foi gravada em produção desde 16/08 até o momento da correção — ou seja, o bug nunca chegou a esconder um balanço real de nenhum produtor, só teria afetado a primeira consulta a partir de agora. |
| DT-66 | **A suíte de "testes de multitenancy" não testava isolamento entre tenants, e `MULTITENANCY.md` declarava validação sem esse lastro.** `TestIsolation_CrossPMOWrite_ArgsInjectionIgnored` montava um mapa de args com `pmo_id: 999` e nunca chamava nenhum handler; `TestRead_WithRealDB_PMOACannotSeePMOBData` comparava só `profileA.PmoAtivoID != profileB.PmoAtivoID` — nenhum dos dois de fato lia/escrevia um dado de um tenant para verificar se vazava para outro. | 2026-09-01 | Novo `internal/mcp/cross_tenancy_real_postgres_test.go`, `TestFinanceiroBalanco_CrossTenantIsolation_RealPostgreSQL`: registra uma despesa real (R$123.456,78, ano 2099 para isolar de dados reais) sob a propriedade 1 pelo handler de produção `handleRegistrarDespesa`, depois confirma — pelo handler de produção `handleConsultarBalancoFinanceiro` — que a propriedade 2 não a enxerga (controle negativo, `despesas == 0`) e que a propriedade 1 enxerga exatamente o valor gravado (controle positivo — sem ele, um teste que sempre lesse zero passaria mesmo com o pipeline quebrado, que é exatamente o defeito que este teste descobriu de verdade: ver **DT-69**). Segue o padrão de `internal/queue/message_buffer_real_postgres_test.go` (DT-68): roda contra `supabase start` local por padrão, aceita `SUPABASE_TEST_URL`/`SUPABASE_TEST_SERVICE_KEY` para staging, e **falha (não pula) sem Postgres real disponível** — o oposto do padrão antigo de "pula sem `DATABASE_URL`" que era parte do próprio problema do DT-66. Reaproveita propriedades/pmos já existentes no seed (id 1 e 2) para não lidar com a FK de `propriedades.user_id` para `auth.users`. `MULTITENANCY.md` atualizado: exemplos de código trocados de `*supabase.Profile` para `TenantCtx` (refletindo o DT-67), checklist de code review ganhou os dois itens novos de teste, e a alegação "validado" agora aponta para este teste real em vez da suíte antiga. |
| DT-46 | **Funções `SECURITY DEFINER` sem checagem de autorização, e defeitos vizinhos.** Levantado ao versionar as funções órfãs (DT-21). Das 9 `SECURITY DEFINER` do lote, 7 não checavam nada e tinham `EXECUTE` aberto para `anon`. | 2026-09-01 | **Núcleo de autorização** (migration `20260901150000_fix_dt46_security_definer_functions.sql`): `registrar_atividade_pmo`/`criar_infraestrutura_pmo` já tinham sido fechadas pelo DT-65; `get_dashboard_stats` ganhou checagem `is_admin()`; `get_propriedade_metrics` (IDOR puro) ganhou checagem de posse; `increment_usage_stats` (órfã, gravava contadores de uso de qualquer usuário) foi revogada de todo mundo; `get_traceability_data` parou de vazar `endereco_cadastral` no QR público de rastreabilidade; hygiene sem mudança de comportamento em `get_admin_user_details`/`get_recent_bot_activities`/`farm_documents`. **Achados vizinhos** (migration `20260901160000_cleanup_dt46_neighbor_findings.sql`): as 3 sobrecargas de `match_chunks` (uma delas referenciava `k.pmo_id`, coluna inexistente em `knowledge_chunks`, e estourava quando chamada) removidas — grep confirmou zero chamadores reais em todo o repo, `match_documents_with_context` é quem de fato serve `consultar_base_conhecimento` hoje, com escopo por PMO; as 3 sobrecargas de `criar_infraestrutura_pmo` (já sem `GRANT` a ninguém desde o DT-65, decisão adiada lá) removidas por decisão tomada agora; `is_chemical_input` rebaixada de `IMMUTABLE` para `STABLE` (chama `unaccent()`, que o próprio Postgres marca `STABLE`); `validate_file_extension` perdeu o `SECURITY DEFINER` desnecessário (corpo só manipula string, não toca tabela). Tudo validado em local antes e reconfirmado ao vivo em produção depois de cada aplicação (com confirmação explícita do responsável): as funções perigosas devolvem `401` para `anon`, as removidas devolvem `PGRST202` (não encontrada), `is_chemical_input`/`validate_file_extension` seguem funcionando idênticas para quem já as chamava. |
| DT-22 | **Schema Drift Crítico (Local vs Prod)**: O ambiente remoto (Staging/Prod) possuía alterações não versionadas em migrations locais: (1) colunas extras em `profiles`, (2) FK nativa para `pmos`, (3) RLS recursivo infinito na policy de Admin e (4) coluna `canteiro_id` + FK ausentes em `caderno_campo_canteiros`. A falta de paridade quebrou testes locais. | 2026-09-01 | **Incidente original (2026-08-17) já estava corrigido** — migrations `20260817212500`/`20260817213000`/`20260817214500` restabeleceram o rastreio, confirmado hoje via `supabase migration list` (local e remoto batendo desde aquela data). **Drift NOVO encontrado e corrigido hoje, com uma causa raiz recorrente:** toda vez que uma migration é aplicada em produção via `apply_migration` do MCP do Supabase (usado nesta mesma sessão, deliberadamente, para não arrastar migrations locais pendentes/divergentes com um `db push` genérico), o histórico remoto grava a versão sob um **timestamp gerado automaticamente na hora da chamada**, diferente do nome do arquivo local — mesmo quando o conteúdo SQL é idêntico. `supabase migration list` mostrava 7 pares divergentes: as 4 migrations desta sessão (DT-69, DT-62 reforço, DT-46 núcleo, DT-46 limpeza) e 2 do DT-68 (aplicadas numa sessão anterior, antes desta compactação). Pior: um índice único (`idx_mq_msg_id` em `message_queue`, também do DT-68) existia em produção **sem nenhum arquivo local correspondente** — aplicado ao vivo e nunca formalizado em git. **Corrigido:** conteúdo de cada par conferido byte a byte antes de agir (`prosrc`/`reloptions`/`indexdef` batendo entre produção e o arquivo local correspondente); arquivo novo `20260901115000_add_message_queue_msg_id_unique.sql` criado para recuperar o índice órfão; os 7 pares (+ o índice recuperado) reconciliados via `supabase migration repair <versão> --status reverted/applied --linked` — comando que só corrige a tabela de bookkeeping (`supabase_migrations.schema_migrations`), não executa SQL nenhum, confirmado depois checando que o schema real (`idx_mq_msg_id`, `reloptions` da view do DT-62) não mudou. **Achado colateral, corrigido à parte:** o DT-62 (bypass de RLS na view de conversas) já tinha sido corrigido em produção em **2026-08-24**, exatamente por essa mesma causa de drift — o board nunca foi atualizado, então uma sessão posterior (hoje) o leu como aberto e "corrigiu" de novo (sem risco, mas redundante); registro do DT-62 corrigido para refletir a data real. **Lição para o futuro:** depois de qualquer `apply_migration` via MCP, rodar `supabase migration repair <versão-real-do-arquivo-local> --status applied --linked` (e reverter a versão automática) na mesma sessão, antes de considerar o trabalho terminado — ou preferir `supabase db push`/`migration up --linked` quando não houver outras migrations pendentes divergentes no caminho. E, de forma mais geral: conferir o estado AO VIVO do banco (não só o board) antes de tratar um item de segurança como ainda aberto. |
| DT-19 | Investigar drift histórico de migrações anteriores à Fase 2.2. O `supabase migration list --linked` revelou pares com nomes dessincronizados entre local e remoto (datas 20260525, 20260526, 20260609, 20260610). | 2026-09-01 | **Confirmado resolvido**, verificação de bônus feita ao investigar o DT-22 (mesma categoria de drift, incidente diferente). `supabase migration list` mostra as 4 versões batendo 1:1 entre local e remoto (nenhuma marcada como par divergente), `list_migrations` confirma que o NOME remoto de cada uma bate exatamente com o arquivo local (`20260525_create_financial_ledger.sql`, `20260526_create_financeiro_transactions_rpc.sql`, `20260609_create_limites_seguranca.sql`, `20260610_add_bonus_credits_to_profiles.sql`). Spot-check de conteúdo na menor delas (`bonus_credits`/`bonus_expires_at` em `profiles`): colunas em produção batem exatamente com o que o arquivo local declara. Sem schema divergente, só nome já sincronizado antes desta sessão — não ficou claro quando/como, mas o estado atual está correto. |
| DT-21 | **Separação de Ambientes (Staging vs Prod): manter o staging fiel à produção.** O projeto `pmo-staging`/`srboqpxrzejtxjfgodnc` existe desde 2026-08-19. Diagnóstico de 2026-08-23 já tinha achado 19 funções e 3 tabelas só em produção; resolvido então por `20260823110000`/`20260823110100`. Causa raiz identificada: `apply_migration` via API/MCP gera timestamp próprio em vez de usar a versão do arquivo, criando drift toda vez que é usado. | 2026-09-01 | **Investigação completa e paridade restaurada de fato (2026-09-02).** Staging estava **17 migrations atrás** de produção desde 24/08, com 8 entradas de histórico próprias sem arquivo local. Das 8: 6 eram drift puro de bookkeeping (mesmo nome/conteúdo, versão errada — `revoke_dt65_gen1_rpc_grants`, `create_conversations_channel_abstraction`, `add_conversations_pmo_id_index`, e as 3 do DT-68); 2 (`fix_pmo_mutation_rpcs`, aplicada 2x em 19/08) foram investigadas a fundo — a função que tocam (`upsert_pmo_relacoes`) está **funcionalmente idêntica** entre staging e produção, a diferença é só um bloco de comentários de debug que ficou em produção e nunca foi commitado (achado sem risco, deixado como está). Além disso, 6 migrations de feature **nunca tinham sido aplicadas em staging** (não só nome errado — ausentes de verdade): `add_cache_tokens_and_fix_logs_processamento`, `fix_view_conversas_recentes_security_invoker` (a correção original do DT-62, nunca chegou a staging), `create_basic_profile_rpc`, `add_onboarding_fields` (x2), `add_slug_organizacoes`. Todos os 5 arquivos não-triviais lidos e confirmados idempotentes/aditivos antes de aplicar. **Ação:** as 6 migrations de feature aplicadas em staging pela primeira vez; as 4 correções de hoje (DT-69, DT-62, DT-46 núcleo, DT-46 limpeza) também aplicadas em staging, restaurando a paridade que essas mesmas correções (aplicadas só em produção mais cedo no mesmo dia) tinham quebrado; todas as 15 versões novas reconciliadas via `supabase migration repair --project-ref srboqpxrzejtxjfgodnc` (bookkeeping, sem re-executar SQL). Confirmado ao vivo depois: `security_invoker=true` na view, `anon` sem acesso, `match_chunks` removida, balanço com `LOWER(tipo)` — staging agora reflete produção em tudo que é relevante para segurança/correção. **Deixado de propósito sem tocar:** `20260830_create_complete_onboarding_rpc.sql`, arquivo local não commitado e nunca aplicado nem em produção — não é drift, é trabalho pendente de outra frente. **Lição estrutural, ainda sem solução de processo:** toda vez que `apply_migration` for usado (nesta sessão ou em qualquer outra), o drift volta — o hábito de rodar `migration repair` logo em seguida, nos dois ambientes, precisa virar parte do fluxo, não uma auditoria ocasional. |
| DT-02 | Remover ou marcar como `deprecated` o método `InsertFarmDocument` (órfão, grava só na coluna legada 3072d/Gemini) | 2026-09-02 | Confirmado zero chamadores em todo o repo (só a própria definição). `UpsertFarmDocumentChunks`, logo abaixo no mesmo arquivo, já é a versão moderna (upsert com `chunk_hash`, suporte a lote) que o substituiu. Removido por completo, junto dos imports `crypto/sha256`/`encoding/hex` que só ele usava. `go build`/`go vet` limpos. |
| DT-03 | Corrigir filename hardcoded (`"audio.ogg"`) no pipeline legado de áudio | 2026-09-02 | Já existia um `deriveFileName(mimeType)` testado (5 casos) em `internal/groq/audio_adapter.go`, usado pelo `AudioTranscriberAdapter` (rota nova, `internal/domain`) — mas os dois call sites do pipeline legado (`fsm.go:174`, `media_worker.go:217`) ainda hardcodavam `"audio.ogg"` mesmo já capturando `audioMimeType` da chamada anterior, com um TODO explícito registrando a lacuna. Função exportada (`DeriveFileName`) e os dois call sites passaram a chamá-la com o mime type real; TODOs obsoletos removidos. `go build`/`go vet`/`go test ./internal/groq/... ./internal/state/... ./internal/queue/...` limpos. |
| DT-05 | Atualizar/padronizar model IDs e tabela de custos — resta limpar IDs defasados nos utilitários de benchmark | 2026-09-02 | `cmd/tester/rag/main.go` tinha `gemini-2.0-flash` como default quando `GEMINI_MODEL` não está setado — atualizado para `gemini-3.1-flash-lite` (o primário real desde o DT-32). `internal/gemini/fallback_simulation_test.go` removido por completo: `TestFallbackLogic` só imprimia uma mensagem sem testar nada, e `TestCalculateAICost` montava uma tabela de casos mas nunca chamava `CalculateAICost` (comentário no próprio arquivo: "would need to be exposed if not in same package") — os dois eram inertes, sempre passavam independente de qualquer coisa, e os valores/IDs que continham já estavam substituídos pelo catálogo multi-fornecedor do DT-43 (`internal/pricing`, com sua própria suíte de testes real). `go build`/`go vet`/`go test ./internal/gemini/...` limpos. |
| DT-06 | Remover diretório `scratch/` e node_modules do laboratório de embeddings offline (Transformers.js) | 2026-09-02 | O laboratório original (Transformers.js/node_modules) já não estava mais lá — o diretório tinha só 2 scripts Go soltos e não commitados (`test_gee.go`, `test_rag_tool.go`, já cobertos por `.gitignore` em dois níveis), causando a quebra conhecida de `go build ./...` na raiz (`main redeclared`). Removidos (conteúdo local, gitignorado — zero impacto em git/outros ambientes). `go build ./...` na raiz do módulo funciona pela primeira vez nesta sessão. |
| DT-48 | **Decidir o sentido staging → produção** — achado maior do que o item original descrevia | 2026-09-02 | **A premissa original ("existem só no staging e em nenhum arquivo") estava desatualizada** — os 5 objetos (`rag_run_judgments`, `set_updated_at_rag_run_judgments`, `maintain_single_latest_judgment`, `upsert_arena_models`, `get_rastreabilidade_publica`) já tinham migration própria no repo (`20260503_public_traceability.sql`, `20260721230000`, `20260721234700`, `20260723120000`) — só nunca tinham sido aplicados em **produção** (confirmado: contagem zero para todos antes da correção). **Achado urgente no caminho:** `get_rastreabilidade_publica` é chamada ao vivo por `pmo-frontend/src/pages/PublicTraceabilityPage.tsx`, que aponta para produção — a página pública de rastreabilidade (QR code de embalagem) estava **quebrada para qualquer visitante** até esta correção. Verificadas todas as dependências (`rag_experiment_runs`, `rag_arena_models`, `caderno_campo`, `propriedades`, `organizacoes` etc.) já existentes em produção antes de aplicar. As 4 migrations aplicadas em produção com confirmação explícita do responsável; todas as 5 confirmadas presentes ao vivo depois. Reconciliado o bookkeeping (`supabase migration repair`) — achado extra: a versão `20260503` já constava como "aplicada" no histórico de produção mesmo com a função ausente, ou seja, algo a removeu depois de maio sem deixar rastro em migration nenhuma. **Item à parte, também resolvido:** a sobrecarga obsoleta de `rpc_registrar_compra_insumo` com 12 argumentos (sem `propriedade_id` nem `idempotency_key`) — risco real de reabrir a duplicação que o DT-09 fechou, se alguma chamada resolvesse para ela — removida de staging com confirmação explícita; produção já tinha só a de 17 argumentos. |
| DT-04 | Descomissionar interface legada `llm.LLMProvider` (FSM) em favor de `ports.LLMProvider` | 2026-09-02 | **Não é limpeza de código legado — decidido não fazer, por custo.** `llm.LLMProvider` (FSM) e `ports.LLMProvider` (`internal/domain`) são interfaces deliberadamente separadas por design, cada uma com "Boundary Rule" documentado no próprio código. `domain.ProcessAudioMessage` não é um substituto do FSM inteiro — é só uma estratégia alternativa para UMA etapa (transformar áudio em texto antes do pipeline de intent/NER, que continuaria idêntico depois): tenta entendimento nativo de áudio do Gemini primeiro, cai pro Groq Whisper só em falha técnica ou resultado "unclear". Todas as peças já existem prontas e testadas (`gemini.LLMProviderAdapter`, `groq.AudioTranscriberAdapter`, o próprio `domain.ProcessAudioMessage`) — a integração seria pequena: trocar a chamada direta ao Groq em `fsm.go`/`media_worker.go` (DT-03) por uma chamada a `domain.ProcessAudioMessage`, usando só o `FinalTranscript` resultante. **Decisão do responsável: não fazer.** Groq Whisper é gratuito (ou muito mais barato); a rota nativa do Gemini tenta primeiro por padrão e cobra por token de áudio em **toda** mensagem — trocar significaria pagar, em todo áudio recebido, por uma capacidade que hoje já funciona sem custo. `llm.LLMProvider` continua sendo a interface certa para o FSM enquanto essa análise de custo não mudar; sem ação de código necessária. |
| DT-59 | **Centralização do Backend em Go (Evitar Vendor Lock-in)** — frontend acessava o banco direto via SDK do Supabase, descentralizando as regras de negócio em relação ao bot. | 2026-09-02 | **Decidido em 2026-08-24: opção GATEWAY, não remoção do PostgREST** — o Go vira a porta de entrada (valida JWT, autoriza, audita) e chama as mesmas RPCs que já existem, sem contradizer o ADR-002 (Fat Database). `internal/gateway/rpc_proxy.go` encaminha o JWT do próprio produtor (nunca a service key) com allowlist fechado de 10 RPCs (`create/update/delete_talhao`, `create/update/delete_caderno_registro`, `rpc_update_propriedade`, `create/update/delete_pmo`); `auth.uid()` resolve igual a uma chamada direta do frontend. Rota `POST /api/v1/rpc/:name`, `RequireAuth`. **Fatia 1 (autenticação das rotas admin)** e **Fatia 3 (gateway RPC)** entregues em 2026-08-24. **Migração das 8 chamadas concluída em 2026-09-02** — 5 já tinham sido migradas antes desta sessão (registro estava desatualizado); migradas as 3 restantes (`update_talhao`, `create_caderno_registro`, `update_caderno_registro`). **Achado não previsto:** `locationService.ts`, usado por componentes reais e ativos (`PropertyMap.tsx`, `MapaPropriedade.tsx`), duplicava chamadas diretas ao PostgREST para talhão que `talhaoService.ts` já tinha migrado — corrigido também. **Achado colateral:** `propriedadeService.test.ts` mockava o caminho antigo (`supabase.rpc`), já quebrado antes desta sessão desde que `updatePropriedade` migrou para `goApiRpc` — corrigido para mockar `goApiClient`. **ADR-003 (Offline-First) verificado, preocupação resolvida:** `useSyncEngine.ts` só sincroniza offline via `cadernoService`/`pmoService`, cujas funções já preservam o contrato `{data, error}` através do gateway — nenhum ajuste adicional necessário; talhão não passa pela fila offline. **Verificado:** parâmetros conferidos contra as RPCs reais em produção; allowlist do Go já cobria os nomes novos; `tsc`/testes/build sem regressão nova. **Deploy confirmado desnecessário no backend** — `curl` contra o container de produção real (`localhost:8080`) confirmou que o binário já reconhece os 10 nomes desde antes desta sessão; deploy do frontend é automático ao mesclar a branch. Não testado em navegador real (exigiria apontar o `pmo-bot-go` local para o Supabase local em vez de produção). |
| DT-61 | **Rotas administrativas do Go estavam sem autenticação nenhuma.** Achado em 2026-08-24 ao dimensionar o DT-59. O middleware global era só RequestID/CORS/Logger/Recovery e `r.Group("/api/v1/admin")` não recebia nada: 9 rotas abertas, entre elas `DELETE /knowledge/documents/:id` e `POST /knowledge/playground/rag` (gasta crédito de LLM por chamada), mais `POST /admin/reload-knowledge`. O próprio frontend documentava o buraco (`// Note: In a real scenario, attach the JWT token here`). Só não vazou porque a stack roda em localhost. | 2026-09-02 | `internal/middleware/auth.go`: verificação de JWT do Supabase por **JWKS/ES256** — o projeto usa chaves assimétricas, não o segredo HS256 legado, então **não entrou segredo novo no `.env`**, basta o `SUPABASE_URL`. Cache do JWKS com rebusca única em `kid` desconhecido e intervalo mínimo entre rebuscas para o `kid` inválido não virar vetor de DoS. Algoritmo fixado em ES256 (fecha confusão de algoritmo — `alg=none` e HS256-com-chave-pública). `RequireAdmin` lê `profiles.role`, **não** o claim `role` do token: o Supabase manda `"authenticated"` para todo usuário logado, inclusive produtor comum. Falha fechada em todos os caminhos. **CORS corrigido junto:** `Allow-Origin: *` com `Allow-Credentials: true` é combinação inválida pela especificação; agora vem de `ALLOWED_ORIGINS`. **Frontend:** duas chamadas mandavam `Bearer ${localStorage.getItem('token')}`, chave que o Supabase nunca escreve — centralizado em `services/goApiClient.ts` lendo `supabase.auth.getSession()`. 9 testes novos. **Prova de ponta feita em 2026-09-02:** `curl` sem header de auth contra `localhost:8080` (container de produção real) devolveu `401` como esperado — confirmado ao vivo. |
| DT-01 | Rotacionar `service_role` key do Supabase (crítico pois a tabela `mutation_drafts` depende de service_role e trafega dados sensíveis). Durante a Fase 2.2, o `SUPABASE_ACCESS_TOKEN` também foi exposto em texto plano e revogado. | 2026-09-01 | Supabase migrou para um sistema de chaves novo (`sb_publishable_...`/`sb_secret_...`, substituindo os antigos `anon`/`service_role` JWT) — gerada uma `secret key` nova no dashboard de produção (`hejewayflbuemnffrhae`), confirmada funcionando (`Blacklist carregada: 3 itens`, dado real via a chave nova). Bot confirmado saudável via `docker logs` depois de cada etapa. **`service_role` legada desativada** na aba "Legacy anon, service_role API keys" do mesmo painel — sem qualquer erro subsequente, como esperado, já que o código já usava só `SUPABASE_KEY` (a chave nova). `pmo-bot-go/.env` (dev local) não foi atualizado, só importa para quem rodar o bot localmente fora do Docker. **Nota da Sessão original**: adotar o hábito para toda a equipe de NUNCA colar segredos em comandos visíveis no terminal (usar prompts interativos ou variáveis lidas com input mascarado). |
| DT-45 | 🛑 **BLOQUEADOR DO DT-38 — a chave da Evolution API estava em texto puro em `cmd/test_audio/main.go:20`, num repositório público, desde 2026-07-30** (commit `163ce12`) — credencial viva da instância de produção do WhatsApp, ~5 semanas de exposição até a rotação. | 2026-09-01 | **(a) e (b):** chave rotacionada (`EVOLUTION_API_KEY`/`GLOBAL_API_KEY` em `.env.prod`, valor hexadecimal puro — a primeira tentativa com um `$` no valor foi cortada pelo Docker Compose ao interpretar como variável dentro do `env_file`, corrigido gerando um valor sem símbolos), containers `pmo-bot-go` e `evolution-go` recriados e confirmados saudáveis (WhatsApp reconectou sozinho, sem QR novo). Achado e corrigido no mesmo fluxo: **DT-57** (vazamento do token nos logs do `evolution-go`, que reapareceria a cada rotação futura). **(c) — histórico do git — decisão explícita: NÃO reescrever.** Antes de decidir, confirmado que as duas worktrees ativas no repositório (`claude/modest-jang-88f9cb`, investigação de dedup; `claude/magical-shamir-a00988`, seed.sql/testes) tinham terminado e não dependiam do histórico permanecer como está — não era isso que pesou contra o rewrite. O que pesou: a chave já está rotacionada e inválida, então o que resta no histórico não é mais um segredo explorável, só registro; `git filter-repo` não está instalado; é repositório público no GitHub, onde um rewrite bem-sucedido não garante remoção de caches do GitHub nem de forks; e existem múltiplas outras branches locais (`feat/*`, `dt-*/*`) mais o `claude/magical-shamir-a00988` recém enviado ao GitHub, todas divergiriam de `main` após o rewrite. Decisão: manter o histórico como está — a rotação já eliminou o risco real. **`pmo-bot-go/.env` (dev local) não foi atualizado** com a chave nova, o que só importa para quem rodar o bot localmente fora do Docker. **Deixou de bloquear o DT-38.** **Nota (2026-09-08):** esta entrada tinha uma cópia esquecida em "A Fazer" (removida), que fez uma sessão posterior tratar o DT-45 como ainda aberto e rotacionar a chave **de novo**, sem necessidade real — a chave `89a03b94...` gerada nesta correção **não era** a originalmente vazada, já era segura. Camada extra de segurança, sem risco: nova chave gerada, `EVOLUTION_API_KEY`/`GLOBAL_API_KEY` atualizadas em `.env.prod`, e — achado importante nessa segunda rodada — a coluna `instances.token` na SQLite do `evolution-go` é um segredo **separado** do `GLOBAL_API_KEY` (a primeira protege rotas de instância como envio de mensagem/webhook via `middleware.Auth`, a segunda só rotas admin via `middleware.AuthAdmin`) que **coincidentemente tinha o mesmo valor** e não tem endpoint de API para ser trocado — só via `UPDATE` direto na SQLite (feito com backup prévio do arquivo, container `evolution-go` parado durante a escrita) ou recriando a instância (que exigiria novo QR, evitado). Containers recriados, WhatsApp reconectou sem novo QR, `ConfigureWebhooks`/heartbeat confirmados autenticando com a chave nova nos logs. Lição de processo: board com entradas duplicadas gera retrabalho real, não só ruído — checar as duas seções antes de tratar algo como pendente. |
| DT-57 | **`evolution-go` imprimia a API key de produção em texto puro no log a cada desconexão** — `whatsmeow.go` (7 ocorrências de `"UserInfo cache cleared for token: %s"`/`"Clearing instance cache - Token: %s"`, não só as 2 linhas identificadas originalmente), mais um vetor não documentado até então: o logger padrão do GORM (`&gorm.Config{}` em `config.go`) imprime a query SQL inteira com parâmetros interpolados sempre que uma consulta não encontra registro — e o fallback de auto-cura de token em `GetInstanceByToken` (`instance_service.go`) dispara exatamente esse caso a **cada rotação de chave**, vazando o valor novo no `docker logs` mesmo com toda a cautela de nunca digitá-lo em outro lugar (achado ao vivo durante a rotação real do DT-45/DT-01 nesta sessão). | 2026-09-01 | Dois fixes em `evolution-go-source`: (1) `newSafeGormLogger()` em `config.go`, configurando `IgnoreRecordNotFoundError: true` nos dois `gorm.Open` de `CreateUsersDB` — suprime o log da query só para o caso "não encontrado" (o próprio fallback de auto-cura), mantendo warnings de queries lentas e erros genuínos; (2) helper `maskToken()` em `whatsmeow.go`, aplicado nas 7 ocorrências que logavam o token cru (mais uma do que o item original documentava), preservando um prefixo de 8 caracteres para correlacionar linhas do mesmo token em debugging sem expor o valor inteiro. Teste unitário novo (`mask_token_test.go`, 5 casos incluindo limites de tamanho). `go build`/`go vet` limpos nos dois pacotes tocados (`pkg/config`, `pkg/whatsmeow/service` — o build do módulo inteiro segue com a limitação pré-existente de cgo do pacote `webp` em ambiente Windows local, já documentada no DT-53, sem relação com esta correção). **Deployado**: imagem reconstruída e `evolution-go` recriado em produção — reconectou sozinho, sem QR, confirmado saudável via `docker logs`. Não foi possível reproduzir ao vivo o caminho exato do vazamento pós-fix (o restart não disparou o fallback de auto-cura, já que o token já estava correto no banco desde a rotação anterior) — a correção é validada por revisão de código + teste unitário + build/vet limpos, não por observação direta do log suprimido em produção. |
| DT-68 | **Mensagens picotadas geram uma resposta por fragmento** — o produtor escreve como fala ("plantei alface hoje" / "no talhão 3" / "umas 200 mudas" em ~4 segundos) e cada mensagem virava um job independente na `message_queue`, com raciocínio de IA e resposta próprios (3× custo de LLM/tool calls/TTS, confusão para o produtor, risco de FSM registrar um plantio incompleto). | 2026-09-01 | Implementado o dreno de coalescência em `claim_next_message_job`: sob o mesmo advisory lock por telefone (`pg_try_advisory_xact_lock`), agrupa fragmentos `ai_pending` elegíveis do mesmo produtor num único turno antes do AI Worker processar. Plano completo em [PLAN-message-buffer-coalescing.md](./PLAN-message-buffer-coalescing.md). **Achado real de concorrência, pego pelo próprio teste e corrigido antes de ir a produção:** a primeira versão da RPC escolhia o "pai" por `SKIP LOCKED` sem noção de telefone — dois workers concorrentes podiam vencer sobre linhas diferentes da MESMA rajada, fatiando um turno de 5 fragmentos em 2-3 "pais" (medido: 5 workers × 5 fragmentos → 3 pais). Corrigido trocando a unidade de contenção de linha para telefone; histórico completo da versão que falhou documentado de propósito no comentário da migration `20260901120100_add_message_queue_drain_on_claim.sql`. **Validação:** 6/6 execuções estáveis contra Postgres local (`-count=1`, sem cache) e 5/5 contra staging real (`srboqpxrzejtxjfgodnc`) via testes parametrizáveis (`SUPABASE_TEST_URL`/`SUPABASE_TEST_SERVICE_KEY`, novo em `message_buffer_real_postgres_test.go`) — dois problemas de higiene de teste corrigidos no caminho (colisão de `msg_id` fixo com índice único aplicado por tarefa paralela; contaminação cruzada entre subtestes por telefone reciclado). **Rollout em produção** (`hejewayflbuemnffrhae`), com confirmação explícita do responsável em cada etapa de risco: schema aplicado (inerte, confirmado via `docker logs` que o bot seguiu saudável), depois o binário publicado em duas fases — primeiro com `MESSAGE_BUFFER_WINDOW=0` (kill-switch, para validar o deploy sem mudar comportamento), depois com os valores reais (`4s`/`12s`). Dois efeitos colaterais descobertos e resolvidos sem incidente: editar `.env.prod` recria também o `evolution-go` (mesmo `env_file`) — reconectou sozinho ao WhatsApp duas vezes, sem QR novo, sessão persistida no volume `evolution_data`; e o classificador de auto mode bloqueou um `docker compose up` repetido, passando na segunda tentativa após reconfirmação em chat. **Pendente, não bloqueante:** observar `message_buffer_parts_per_turn`/`merged_total`/`added_latency_seconds` por um dia de uso real antes de considerar o rollout plenamente encerrado (seção 6.5 passo 5 do PLAN). Achados fora de escopo sinalizados como tarefas separadas, não corrigidos aqui: `seed.sql` quebrado contra o schema atual, e dois testes real-Postgres pré-existentes (`mutation_drafts`, idempotência de cotas) que nunca tinham rodado sua lógica de verdade neste ambiente. |
| DT-65 | RPCs `SECURITY DEFINER` de mutação (geração `*_arg`) sem checagem de posse, IDOR explorável via PostgREST | 2026-08-24 | Corrigido em três frentes, aplicadas primeiro em staging (`pmo-staging`) e validadas antes de ir para produção (`pmo-inteligente`): (1) `registrar_atividade_pmo`, `rpc_registrar_compra_insumo`, `rpc_registrar_operacao_campo` — só o bot as chamava, então `REVOKE ALL FROM PUBLIC, anon, authenticated` + `GRANT EXECUTE TO service_role`, mesmo padrão já usado em `create_basic_profile` (`20260824130000_create_basic_profile_rpc.sql`); (2) `criar_infraestrutura_pmo` (3 sobrecargas) e `rpc_registrar_cota_produtor` — órfãs, sem chamador vivo no bot nem no frontend, bloqueadas sem `GRANT` a ninguém (destino final fica com o DT-46c); (3) `rpc_registrar_transacao_com_rateio` — única com dois chamadores reais (bot via `service_role` **e** frontend via `financeiroService.ts`, que usa a sessão do próprio usuário), teve o corpo reescrito para derivar `user_id` de `auth.uid()` e validar posse de `propriedade_id` quando o chamador não é `service_role`, em vez de confiar no `user_id` do payload. Drift do DT-48 tratado à parte: staging tinha uma sétima sobrecarga de `rpc_registrar_compra_insumo` (12 args, sem idempotência) que não existe em produção, revogada só lá. Verificado ao vivo: `has_function_privilege` confirmando os grants nos dois ambientes, teste negativo (`authenticated` chamando RPC do grupo 1 → `42501 permission denied`), teste do grupo 3 com `propriedade_id` alheio e `user_id` forjado no payload → rejeitado com `"Propriedade inválida ou não pertence ao usuário"`, e teste positivo real em produção via WhatsApp (colheita, compra de insumo e despesa financeira registradas com sucesso pelo bot depois da migration). Migration: `supabase/migrations/20260824140000_revoke_dt65_gen1_rpc_grants.sql`. |
| DT-63 | **Canal de alerta do self-heal (DT-53 Estágio 0) estava mudo em produção — `canal=nenhum` em todo alerta.** Descoberto ao investigar duas quedas reais do WhatsApp percebidas pelo responsável (2026-08-24, ~14:36–14:55): logs confirmaram DNS transitório do Docker (`127.0.0.11:53 i/o timeout`) derrubando o WebSocket do `evolution-go` em loop por ~19min, com `telemetry event=self_heal_alert` disparando corretamente (`severidade=critico` às 14:41) — só que sem `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` no `.env.prod`, o alerta nunca saía do container. O serviço voltou sozinho (auto-restart de tiro único do `evolution-go`, mesmo padrão do DT-56), mas ninguém foi notificado — só se percebeu por teste manual. | 2026-08-24 | Criado bot no Telegram (`@manorg_bot` via BotFather) e obtido `chat_id` via `GET /getUpdates`. Adicionadas `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID` ao `.env.prod`, `pmo-bot-go` recriado (`docker compose -f docker-compose.prod.yml up -d --force-recreate pmo-bot-go`) para reler o env — confirmado saudável nos logs pós-restart. **Nota de segurança:** o token do bot passou em texto puro pelo chat da sessão; recomendado revogar via BotFather (`/revoke`) e trocar por um novo assim que possível. **Atualização no mesmo dia — `SELF_HEAL_ENABLED=true` ligado** a pedido do responsável: heartbeat confirmado enviando o alerta de teste ponta a ponta (`SELF_HEAL_TEST_ALERT=true` numa subida, log `status=ok latency_ms=888`, depois removido do `.env.prod` para não repetir a cada restart). **Segue em `SELF_HEAL_DRY_RUN=true` (default)** — o Estágio 1 já roda a máquina de estados e emitiria o alerta de uma queda real, mas ainda não chama `forcereconnect` de verdade; desligar o dry-run é decisão à parte, deliberadamente represada até observar uma classificação correta ao vivo (ver `DT-53`). **Ping de uptime criado junto** (`cmd/server/main.go`, funções `pingDeUptime`/`formatarDuracaoPing`, chave nova `ports.ChavePingUptime`): manda uma confirmação "online há Xh" pelo Telegram em marcos de 1h/3h/6h/12h/24h após o boot, repetindo a cada 24h depois disso — sinal positivo periódico, independente do alerta de queda (que continua disparando a qualquer momento via `ChaveWhatsAppCaiu`). `go build ./...` limpo; sem teste unitário dedicado (a função é só agendamento sobre `notify.Disparar`, já coberto). |
| DT-60 | **Ordem de montagem do system prompt sabotava o cache implícito do Gemini.** `BuildSystemInstruction` concatenava os blocos DINÂMICOS primeiro (contexto do usuário com talhões, memórias vetoriais top-3, intent do turno) e só depois o conteúdo estável (`.md` de identidade/regras, OKF, instrução de RAG). Como o Gemini 2.5+ casa cache por **prefixo comum** entre chamadas consecutivas, o prefixo nunca se repetia — nem o cache gratuito e automático tinha chance de bater, antes mesmo de qualquer Context Caching explícito entrar em jogo. | 2026-08-24 | Ordem invertida em `internal/state/prompt_manager.go`: estável primeiro (`<IDENTIDADE_E_REGRAS_LLM>` → `<REGRAS_NEGOCIO_OKF>` → `<INSTRUCAO_RAG>`), dinâmico depois (intent + contexto do usuário), e `<MEMORIA_DO_PRODUTOR>` por último por ser a fatia mais volátil. Nenhuma string literal ou condicional mudou — só a ordem de concatenação. Alvo do ganho: o par estável de ~29,6KB de ferramentas (medido no DT-37) + ~12–18KB de prompt/OKF, hoje reenviado a cada turno e cada retry. **Nota de numeração:** o commit `f460c77` saiu rotulado `refactor(DT-59)` por engano — DT-59 já pertencia à centralização do backend em Go. O histórico não foi reescrito (pior que o erro); o comentário no código foi corrigido para DT-60 e o registro passa a ser este. **Ainda não medido em tráfego real:** a telemetria que provaria o ganho é a do DT-37/`CostWithCache`, entregue no mesmo dia — falta rodar com volume para ver `cached_tokens` subir. |
| DT-54 | **Roteador de TTS híbrido com limite de concorrência e fallback gracioso para texto (Fase 1).** O Piper processava requisições concorrentes sem controle algum, arriscando CPU starvation numa VPS de poucos vCPUs — exatamente o que o teste de capacidade do DT-38/DT-53 comprovou (5 requisições simultâneas: spread de 27,4s). Não havia também camada de roteamento entre cache, TTS local e TTS em nuvem — só uma chamada direta ao Piper. **Nota de proveniência:** feito por fora desta sessão (agente Antigravity, commit `c7aca18`, 2026-08-23) e só descoberto ao revisar o repositório durante o DT-53. Os dois documentos que a IA gerou como registro (`PLAN-tts-hybrid-architecture.md`, `WALKTHROUGH-tts-hybrid-architecture.md`) caíam silenciosamente em regras já existentes do `.gitignore` (`PLAN-*.md`, `*ARCHITECTURE.md`) e nunca seriam versionados apesar da intenção declarada de serem "a versão permanente" — o código estava seguro (já commitado), só a narrativa ficava órfã. Conteúdo absorvido aqui e os dois arquivos removidos, para não conviver duas fontes de verdade. | 2026-08-23 | Nova interface `ports.Synthesizer` (`Synthesize(ctx, req) (AudioArtifact, error)`, cancelável por `context.Context`). `tts.Router` encadeia Cache → Local (Piper) → Cloud → Fallback texto. `tts.ConcurrencyLimiter` decora o Piper com fila de capacidade 1 (`chan struct{}`) — quem aborta a espera é o contexto com timeout do chamador, não o limitador, evitando fila infinita. Ao esgotar o timeout, propaga `ErrSynthesizerSaturated` até `Delivery`/`Worker`, que ignora o áudio sem travar a thread e completa a resposta só em texto, com contador Prometheus `tts_fallback_starvation_total` e log `⚠️ TTS Fallback triggered due to starvation` para a degradação não ficar invisível na operação. Injeções ajustadas em `fsm.go`, `ai_worker.go`, `delivery.go`, `utils.go`, `main.go`; `MediaWorker` migrado de `ports.TTSProvider` para `ports.Synthesizer`. Testes unitários cobrindo os 4 ramos do roteador (caminho feliz + falhas em cascata) e a fila unifilar do limitador; suíte `go test ./...` completa passando. Também removido o mapeamento de porta `5000:5000` do `piper` em `docker-compose.prod.yml` — **mas o container em produção ainda está rodando com a porta antiga mapeada**, porque não foi recriado após a edição; ver **DT-55**. |
| DT-55 | **Container de produção do Piper expunha a porta 5000 publicamente** — o `docker-compose.prod.yml` já não mapeava porta pro serviço `piper` desde o DT-54 ("higiene pós-teste de carga"), mas o container em execução continuava com `0.0.0.0:5000->5000/tcp` porque editar o YAML não recria sozinho um container já em pé. Piper não tem autenticação própria: enquanto exposto, qualquer host que alcançasse a porta 5000 desta máquina sintetizava áudio de graça, consumindo a mesma CPU que o produtor precisa. Achado ao montar o teste de capacidade do DT-53 (conflito de porta contra o `docker-compose.captest.yml`). Mesma classe de defeito do DT-25 (edição no arquivo sem efeito prático até recriar o container). | 2026-08-23 | `docker compose -f docker-compose.prod.yml up -d piper` recriou o container aplicando a config atual. Confirmado via `docker ps`: porta agora aparece só como `5000/tcp` (sem publicação no host). Verificado que o resto da stack não foi afetado — `pmo-bot-go` continuou reportando `Heartbeat: WhatsApp CONNECTED` nos logs durante e depois da recriação, já que a comunicação `pmo-bot-go` → `piper` usa a rede Docker interna por nome de serviço, independente de porta publicada no host. |
| DT-56 | **Incidente: WhatsApp fora do ar 36min35s (17:29:09–18:05:44), causa distinta do DT-52.** Cronologia: `[Client/Socket ERROR] failed to read frame header: EOF` → `evolution-go` detecta `Disconnected` e dispara auto-restart → `Already logged in with JID: 5534...` (sessão válida, sem risco de QR) → `Failed to connect: ... dial tcp: lookup web.whatsapp.com on 127.0.0.11:53: i/o timeout` — falha de DNS **transitória** dentro do Docker. O auto-restart do `evolution-go` é **tiro único**: `StartClient` loga a falha e retorna sem retentativa (`whatsmeow.go:517-519`), então um soluço de DNS matou a sessão permanentemente. Um `docker restart pmo-prod-stack-evolution-go-1` sozinho **não bastou** — o boot do `evolution-go` só religa instâncias com `connected=true` no banco (log: `Found 0 connected instances`), e a coluna tinha ficado `false`. Foi preciso um `POST /instance/reconnect` manual pra religar de fato. **Achado colateral de segurança:** o `evolution-go` imprime a API key de produção em texto puro no log a cada desconexão (`whatsmeow.go:1641`, `UserInfo cache cleared for token: <chave>`) — registrado à parte como **DT-57**. | 2026-08-23 | Diagnosticado ao vivo cruzando `docker logs` do `evolution-go` e do `pmo-bot-go` durante a própria sessão de implementação do DT-53 (o incidente aconteceu enquanto o self-heal ainda não estava pronto — prova de necessidade em tempo real). Recuperado com `POST /instance/reconnect` (autorizado pelo responsável, já que o classificador de auto mode bloqueia `docker exec`/chamadas HTTP contra produção por padrão). Confirma **quatro** caminhos de falha distintos já mapeados no board para o mesmo sintoma de superfície (WhatsApp fora do ar sem reação): `StreamReplaced` silencioso (DT-52), auto-restart de tiro único (este incidente), `POST /instance/reconnect` recusando o caso comum (achado ao planejar o DT-53), e `connected=false` impedindo religar no boot (este incidente). Motivou a implementação dos Estágios 0-2 do **DT-53** na mesma sessão. |
| DT-49 | **26 registros de caderno de campo perdidos por vocabulário.** A RPC `rpc_registrar_operacao_campo` valida `tipo_arg` contra sete valores e recusa o resto. Quatorze dos quinze call sites passam literal válido; um passava `ext.Atividade` cru, texto livre do LLM. Sintoma em produção: 26 mensagens com "❌ Erro no Registro: Tipo de operação inválido: aplicação de composto orgânico" — um assistente de produção ORGÂNICA recusando aplicação de composto orgânico. | 2026-08-23 | `domain.NormalizeTipoOperacao` traduz por palavra-chave, seguindo o padrão do `ClassifyHITLResponse` (DT-11). **Verbos antes de substantivos**, descoberto por teste: "lavei as caixas de colheita" caía em Colheita porque a palavra descrevia as CAIXAS, não a atividade. Preserva a distinção compostar (Compostagem) vs aplicar composto (Manejo). **Não chuta quando não reconhece** — pergunta ao produtor, porque arquivar uma venda como manejo corromperia prova perante certificadora. O texto original continua em `metodo_aplicacao`. |
| DT-50 | **Jobs presos para sempre em processamento, e silêncio no dead letter.** `claim_next_message_job` marca o job como em processamento e só o próprio worker o tira dali; se o worker morre (deploy, crash, container reiniciado), ninguém devolve, e o `Claim` só busca `pending`/`ai_pending`. Medido: **8 jobs presos, o mais antigo desde 2026-06-07**. Somado a isso, 6 jobs de áudio morreram por erro 500 do Evolution sem que o produtor fosse avisado de nada. | 2026-08-23 | `queue.StuckJobReaper`, ticker em Go (mesma decisão do DT-42 sobre não usar `pg_cron`). Job de IA volta para `ai_pending`, não `pending`, para não refazer transcrição e gastar cota da Groq de novo. Primeira varredura na SUBIDA, que é quando existem órfãos do processo que acabou de morrer. Regra de dead letter unificada em `IsDeadLetter`/`MaxRetryAttempts`, antes duplicada. No dead letter de mídia o produtor recebe pedido para reenviar ou escrever, em vez de silêncio. |
| DT-52 | **Incidente: `cmd/loadtest.go` (versão antiga, solta na raiz) derrubou a sessão real do WhatsApp por 24 minutos.** Cronologia (2026-08-23, horário local): **13:20:44** — rajada de 70 webhooks `STRESS_*` chega em `/webhook/evolution`, fabricando mensagens de áudio com ID inexistente e o telefone real (`5534997317545`), disparada por alguém rodando o `loadtest.go` antigo (fora do git, com o default perigoso `-url http://localhost:8080/...`, que nesta máquina É a produção real — DT-38). **13:20:48** — o `evolution-go` tenta baixar mídia para essas mensagens fantasmas; a sessão do `whatsmeow` nunca as recebeu de verdade, o protocolo Signal dessincroniza (`Unable to get or create message keys: received message with old counter`), e a Meta derruba a conexão com `StreamReplaced`. **13:20:49 – 13:44:46** — zero tentativa de reconexão; o heartbeat do bot reporta `WhatsApp DISCONNECTED` a cada minuto sem que nada aja — a lacuna de self-healing já registrada no DT-30. Qualquer produtor real que mandasse mensagem nesse intervalo não recebia resposta nenhuma. **13:44:46** — restart manual do container (`docker restart pmo-prod-stack-evolution-go-1`). **13:44:50** — reconectado, `Successfully authenticated`, sem necessidade de novo QR. | 2026-08-23 | **Diagnosticado e mitigado na hora**, cruzando `docker logs` do `evolution-go` com a fila do Supabase. Correção estrutural em `cmd/loadtest`: `-url` perdeu o valor padrão (obrigava a escrever o alvo, mas isso sozinho não bastava — a URL de produção era o único alvo que funciona nesta máquina, então seria digitada de qualquer forma); a correção real é `confirmarAlvo`, que exige o operador digitar `CONFIRMO` depois de ler um aviso citando este incidente, e só pula a pergunta para alvos reconhecidos como staging/CI. README reescrito recomendando `cmd/loadtest_piper` (não toca o WhatsApp) como a escolha padrão para medir capacidade. Os três arquivos soltos na raiz que causaram o incidente (`loadtest.go`, `loadtest_piper.go`, `payload_audio_stress.json` — nunca versionados, com o telefone real) foram removidos; a cópia segura já estava versionada em `cmd/loadtest/`. **Nota:** o restart resolveu porque a sessão em si não corrompeu, só a conexão — se tivesse corrompido, o board já registrava (DT-38) que reconectar por QR é o passo mais arriscado da migração para VPS; este incidente é evidência de primeira mão de por que isso é levado a sério. **Ainda em aberto:** DT-30 continua sem self-healing — se isto acontecer sem ninguém olhando o log na hora, o bot fica fora do ar indefinidamente. |
| DT-00 | Fase 2.1 — Idempotência e migrações no PostgreSQL Staging | 2026-08-16 | Validada com testes unitários e de integração real |
| DT-09 | Two-Phase Commit com tabela `mutation_drafts` no PostgreSQL e TTL de 45min | 2026-08-17 | Fase 2.2 deployada em staging com sucesso (2026-08-16). As 3 migrações (000000, 010000, 020000) confirmadas idênticas entre local e remoto via `supabase migration list --linked`. |
| DT-10 | Tool única polimórfica `propose_batch_mutations(operations: [...])` | 2026-08-17 | Unio discriminada por `type` (`caderno_campo`, `compra_insumo`, `transacoes_com_rateio`, `cotas_produtores`) |
| DT-11 | Normalização determinística de respostas de confirmação HITL por palavras-chave | 2026-08-17 | `ClassifyHITLResponse` antes de chamada a LLM com fallback robusto |
| DT-12 | Substituição atômica via `create_or_supersede_mutation_draft` e terminalidade de rascunhos com erro | 2026-08-17 | Prevenção de race condition sob `FOR UPDATE` e índice único parcial `idx_mutation_drafts_one_pending` |
| DT-25 | **Container de produção defasado (stale build)** — imagem buildada em 2026-08-16 vs. último commit em 2026-08-21 (`1b1b341`). O binário não tinha 5 dias de fixes. Sintoma: `consultar_previsao_tempo` falhando 100% com `parâmetro 'propriedade_id' é obrigatório`, string que já não existia no código-fonte — o que confirmou o diagnóstico. | 2026-08-22 | Rebuild executado (`docker compose -f docker-compose.prod.yml up -d --build pmo-bot-go piper`). Imagem agora 2026-08-22T12:03Z. Levou junto **DT-27** (SendVoice multipart) e **DT-28** (TTS agnóstico + Piper + áudio&texto). Verificado em produção: bot loga `🔊 [TTS] Provider: Piper (auto-hospedado) voz=pt_BR-faber-medium`, container `piper` de pé (Uvicorn na :5000) e síntese testada ponta a ponta (138KB de WAV válido, magic bytes `RIFF....WAVE`). **Causa raiz não resolvida** — rastreada em `DT-30`: não há pipeline que faça deploy do container, então a defasagem volta a acontecer. |
| DT-31 | **TTS lia formatação em voz alta + áudio perdido por timeout**. Validação em produção do DT-28 revelou três defeitos: (1) o texto ia cru para o motor, que narrava "asterisco asterisco Consulta Técnica" e o nome de cada emoji (⏳, 🌿, ☀️); (2) a resposta longa da previsão do tempo falhava com `context deadline exceeded` — o WAV de 554KB estourava o timeout de 30s; (3) o áudio era enviado ANTES do texto, então uma síntese de 25s deixava o produtor sem resposta nenhuma nesse intervalo, e sem nada quando o TTS falhava. | 2026-08-22 | (1) Criado `utils.SanitizeForSpeech` — remove emoji/pictogramas, marcadores de lista, ênfase, cabeçalhos, blocos de código e URLs, preservando letras acentuadas, dígitos, pontuação e `° º ª %`. A mensagem **escrita** mantém a formatação intacta; só o áudio usa a versão limpa. 11 casos de teste. (2) Piper passou a devolver **MP3 em vez de WAV**: medido 92KB contra 554KB (~6x menor) na mesma resposta; timeouts elevados para 120s (contexto) e 150s (cliente HTTP). (3) Ordem invertida para **texto primeiro, áudio depois**, nos dois caminhos (`queue/delivery.go` e `state/utils.go`), com teste de invariante de ordem. Corrigido também o comentário do contrato em `ports.TTSProvider`, que afirmava erroneamente que o texto chegava pré-sanitizado. |
| DT-32 | **Cadeia de fallback do Gemini estava morta em três camadas** (~47% das dúvidas terminando em "erro interno"). (a) `CallGoogle` fixava `c.Config.Model` internamente e **ignorava** o modelo recebido por parâmetro — ao "escalar", `withFallback` voltava a chamar o mesmo primário que acabara de falhar; evidência: a URL do erro citava o primário enquanto o log dizia estar no fallback. (b) O modelo de fallback `gemini-1.5-flash` **foi descontinuado** — confirmado com 0 ocorrências em `/v1beta/models`; estava também hardcoded em `DescribeImage` (descrição de imagem quebrada em silêncio) e como default em `main.go`. (c) A escalada herdava o **contexto já expirado** pelo primário e retornava em ~0,55ms sem abrir conexão — medido pelos timestamps de log (`09:27:04.843084` → `09:27:04.843639`). | 2026-08-22 | (a) `CallGoogle` passou a receber `modelName` obrigatório, propagado em todos os call sites. (b) Primário migrado do canal *preview* para o GA `gemini-3.1-flash-lite`; default morto removido de `main.go` (vazio agora é intencional: escala para **outro provedor**, OpenRouter, que é o que resolve falha de infraestrutura); `OPENROUTER_MODEL` alinhado ao **mesmo** modelo (`google/gemini-3.1-flash-lite`, verificado como existente na OpenRouter). (c) Cada tentativa recebe deadline próprio de 25s via `context.WithoutCancel`, para a escalada não nascer sem prazo. Adicionado vocabulário controlado `FallbackReason` (`timeout`, `rate_limit`, `erro_5xx`, `requisicao_invalida`, `outro`) emitido como `telemetry event=llm_fallback motivo_do_fallback=...`, separando falha de **infraestrutura** de falha de **capacidade**. 9 testes. **Nota de método:** a troca do preview pelo GA é uma hipótese ainda não confirmada como causa dos timeouts — a instrumentação foi adicionada justamente para medir isso em vez de supor (ver DT-33). |
| DT-35 | **Output Judge bloqueava respostas legítimas como `ALUCINACAO_DADOS`** (falso positivo). O `buildJudgePrompt` só marcava `FONTE_RAG=sim` para ferramentas cujo nome contivesse `document`/`rag`, e a regra do juiz só mandava relaxar quando `FONTE_RAG=sim`. Resultado: `consultar_previsao_tempo` executava de fato contra a API meteorológica, mas o juiz recebia `FONTE_RAG=não`, via números específicos (16°C, UV 7,4, evapotranspiração 5,88mm) e acusava alucinação — bloqueando **toda** resposta de previsão do tempo e notificando um especialista à toa. Verificado nos logs que a ferramenta rodou (`09:43:58 handleConsultarPrevisaoTempo executado`) antes do bloqueio (`09:44:02 Judge BLOQUEADA`). **Bug secundário achado pelo próprio teste de regressão:** a checagem usava `strings.Contains(t, "rag")` minúsculo, mas a ferramenta real é `ConsultarLeiOrganica_RAG` — então `FONTE_RAG` era **sempre** "não", deixando o juiz rigoroso demais também nas consultas RAG legítimas. | 2026-08-22 | Introduzido o sinal `FONTE_EXTERNA`, marcado por qualquer ferramenta de consulta executada (prefixo `consultar`, RAG ou documentos), separado do `FONTE_RAG` específico de documentos. Ferramentas de **escrita** (`registrar_*`) deliberadamente não contam como fonte. A regra `ALUCINACAO_DADOS` passou a declarar que dados retornados por qualquer ferramenta em `[FERRAMENTAS USADAS]` são fonte legítima — essas ferramentas foram realmente executadas — e que previsão do tempo não é "registro da fazenda do produtor". Comparação agora case-insensitive. 4 testes de regressão. |
| DT-36 | **Unidades e datas lidas como abreviação no áudio**. Com o pipeline já funcionando ponta a ponta, o motor lia "28°C" como "vinte e oito grau cê" e "(22/08)" como "vinte e dois barra zero oito". Tecnicamente correto, mas soa robótico para quem ouve no campo. | 2026-08-22 | `SanitizeForSpeech` ganhou `expandUnitsForSpeech` (°C/°F/°, mm, mm/dia, cm, km, km/h, kg, ha, m², %) e `expandDatesForSpeech` (dd/mm e dd/mm/aaaa → "22 de agosto"). A expansão roda **antes** de `stripSymbols`, que descartaria `°` e `%` sem deixar leitura; a ordem das regras é significativa (`mm/dia` antes de `mm`). Mês inválido é deixado intacto, evitando estragar razões numéricas. 13 testes novos, incluindo a mensagem real de previsão ponta a ponta. Três asserções anteriores foram atualizadas: afirmavam que `°C` e `%` permaneciam como símbolo, o que era a intenção antiga. |
| DT-26 | **Clock drift** — o sidecar `clockwork` tentava corrigir o relógio de dentro do contêiner e logava "setting time" a cada 30s, mas o offset só crescia (+4,85s → +5,46s em 3 minutos): o relógio autoritativo é o do Windows e o Hyper-V TimeSync reimpõe esse horário na VM, desfazendo o ajuste. O log dava falsa sensação de saúde e mascarou a causa por dias — foi este drift que derrubou 100% dos downloads de mídia do WhatsApp (403) na manhã de 2026-08-22. | 2026-08-22 | **Causa raiz: o serviço `W32Time` do Windows estava PARADO.** A configuração estava correta (`time.windows.com`), mas o serviço é *trigger-started* por ingresso em domínio e a máquina não está em domínio — o gatilho nunca disparava e o Windows jamais sincronizava. Corrigido no host (`Set-Service W32Time -StartupType Automatic` + `Start-Service` + `w32tm /resync /force`), executado manualmente pelo responsável por exigir Administrador. Validado: `Status: Running`, `StartType: Automatic` (sobrevive a reboot), `Camada 5 sincronizado por SNTP`, sondagem a cada 1024s. Drift medido após a correção: **média 0,403s, máximo 0,424s** — dentro da tolerância. O `clockwork` foi reescrito de corretor para **sentinela**: só mede (`ntpd -w`), sem `privileged` nem `SYS_TIME`, e a classificação por limiar saiu do YAML para `scripts/analisar_telemetria.sh`. |
| DT-33 | **Medir a causa real dos timeouts de LLM** — hipótese inicial (canal *preview* do modelo) e hipótese secundária (payload de 29 ferramentas) ambas **REFUTADAS PELO DADO**. | 2026-08-22 | Instrumentação em 3 níveis (`llm_call` no orquestrador, `llm_provider_call` no adapter cobrindo os 6 call sites, `tool_invoked` para a matriz intent×ferramenta) + simulador de carga (`scripts/simular_carga.sh`) + agregador (`scripts/analisar_telemetria.sh`). **Amostra: 121 chamadas.** Conclusões: **(1)** A distribuição é **bimodal** — 69% abaixo de 3s, 85% abaixo de 10s, e **7% travam e são mortas pelo timeout**, com **ZERO chamadas na faixa 24–24,9s**. Essa lacuna é a assinatura de **conexão travada**, não de processamento lento: carga computacional produziria distribuição contínua (chamadas em 15s, 20s, 23s), não um vão. **(2)** O payload **não** explica: chamadas **SEM ferramentas** (roteador, payload mínimo) tiveram média **6.477ms e 23% acima de 10s**, contra **3.749ms e 9%** das chamadas **COM 29 ferramentas** (44KB) — as leves são MAIS lentas que as pesadas. Com payload idêntico (44KB), a latência variou de 789ms a 35.261ms (45x). **(3)** O fix do fallback (DT-32) está funcionando: 20 escaladas para a OpenRouter, **0% de erro para o usuário final** — antes essas 7% viravam "erro interno". Ação restante movida para DT-41 (fail-fast em stall). |
| DT-41 | **Conexão travada era retentada 3x no mesmo endpoint mudo** — custo de ~79s por stall. `isOverloadedError` tratava `context deadline exceeded` como erro transitório de sobrecarga, então um stall disparava a política de retry do primário: 25s (trava) + 2s + 25s (trava) + 2s + 25s (trava) **antes de sequer tentar outro provedor**. Repetir a mesma requisição no endpoint que acabou de ficar mudo por 25s é justamente o que não resolve stall — e contraria a orientação do time de que falha de infraestrutura se resolve trocando de **provedor**, não repetindo no mesmo. | 2026-08-22 | Criado `isStallError`, separado de `isOverloadedError`: stall (deadline/`Client.Timeout`) **escala imediatamente** para a OpenRouter, enquanto 429/503/`overloaded` — que são o servidor declarando carga — mantêm o retry com backoff, onde ele faz sentido. Custo do stall cai de **~79s para ~25s**, afetando os 7% de chamadas medidos no DT-33. 8 casos de teste, incluindo regressão explícita de que timeout não volta a ser classificado como overload. **Nota de método:** a proposta original deste débito era baixar o `attemptTimeout` para 12s; ao inspecionar a faixa de 8–25s antes de aplicar, verificou-se que existem respostas legítimas em 17s, 19s, 22s e 23s — o corte mataria 7 das 13 chamadas dessa faixa. A separação limpa que se supunha não existe (o vão real é só de 23,3s a 24,9s), e a proposta foi descartada em favor desta. Refinamento por TTFT segue em DT-41-STREAM. |
| DT-43 | **Tabela de custos por LLM era ficção** — preços digitados à mão num `switch`, só de Gemini, e **custo ZERO para qualquer modelo não reconhecido**. Como o sistema passou a escalar para a OpenRouter, todo o gasto do fallback ficava invisível. Os valores também estavam defasados: cobrava `gemini-3.1-flash-lite` a US$0,075/US$0,30 por 1M quando o real é US$0,250/US$1,500 — subestimando entrada em 3,3x e saída em 5x. | 2026-08-22 | Criado o pacote `internal/pricing` com catálogo **multi-fornecedor** (319 modelos com suporte a tool calling, de Google, Anthropic, OpenAI, Meta, DeepSeek, Qwen, Mistral e outros), gerado por `cmd/pricing-refresh` a partir de fonte pública e verificável, embutido via `go:embed` e consultado offline. Modelo desconhecido passa a receber estimativa **conservadora (cara)**, nunca zero: subestimar custo desconhecido induz decisão errada de arquitetura, superestimar apenas provoca conferência. O `Lookup` tolera as variações de nome entre fornecedores (`gemini-x` vs `google/gemini-x`, sufixos de variante). Inclui `Compare(entrada, saida, n)`, que ordena por perfil de uso real — a ordem muda conforme a proporção entrada/saída, servindo de insumo objetivo ao roteador do DT-34. **Ganho de resiliência:** o refresh captura `expiration_date`, e `cmd/pricing-refresh -check` falha se um modelo em uso estiver ausente ou expirando — exatamente o cenário que quebrou a cadeia de fallback quando o `gemini-1.5-flash` foi descontinuado sem aviso. **Limitação registrada:** `pricing.overrides` (preço de contexto longo e janelas de horário) não está modelado; o custo assume condições padrão. 7 testes. |
| DT-39 | **Monitor ao Vivo exibia 21:00 em toda mensagem e listava a conversa fora de ordem.** A coluna `timestamp` de `public.messages` era `text`, sem DEFAULT, e o struct `MessageInsert` do bot **não tinha campo de timestamp** — nunca foi enviado. No frontend, `new Date(msg.timestamp)` com NULL vira epoch, que em UTC-3 renderiza **21:00 de 31/12/1969**: uma hora fabricada, com aparência de real. **Segundo defeito, mais grave:** tanto a consulta do painel quanto a view `view_conversas_recentes` ordenam por essa coluna; com ela nula, a ordem das mensagens e a escolha da "última mensagem" de cada conversa eram **indefinidas**. Um painel de auditoria fora de sequência é pior que não ter painel. | 2026-08-22 | Migration `20260822150000`: coluna convertida para `timestamptz`, `DEFAULT now()` como defesa em profundidade, índice `(phone, timestamp DESC)` e a view recriada com `NULLS LAST`. O bot passa a enviar o instante explicitamente (`MessageInsert.Timestamp`) — o que importa para auditoria é a hora do evento, não a do INSERT, que ocorre numa goroutine assíncrona e pode atrasar. No frontend, `formatarHoraMensagem` exibe `--:--` em vez de fabricar horário. **Decisão deliberada:** as 307 linhas históricas seguem NULL. Preenchê-las com `now()` "limparia" a tela, mas fabricaria dado de auditoria num sistema que serve de prova perante certificadora — admitir a lacuna é mais honesto. **Percalços registrados:** (a) o levantamento inicial concluiu "100% NULL"; a verificação antes de aplicar mostrou 30 linhas COM valor, em dois formatos distintos, todos testados como convertíveis; (b) a primeira tentativa falhou e reverteu inteira — a view dependia da coluna (`cannot alter type of a column used by a view`), exigindo DROP/CREATE e restauração explícita dos 20 GRANTs. Verificado após aplicar: tipo correto, DEFAULT ativo, 337 linhas e os 30 timestamps preservados, view funcionando e grants restaurados. |
| DT-20 | Hotfix IDOR Crítico: `setup_initial_profile` mitigado | 2026-08-17 | Vulnerabilidade de sequestro fechada (Staging). Nota: Auditoria manual de baixo volume (N=6), sem cruzamento sistemático de created_at/duplicidade — aceitável devido à volumetria, mas não é garantia formal. |
| DT-28 | **TTS agnóstico + voz natural + resposta em áudio E texto**. (a) A voz robótica vinha de `tts.NewGoogleOrchestrator()` hardcoded em `main.go` — endpoint **não-oficial** do Google Tradutor. (b) O pacote `internal/tts` era um `Orchestrator` único com `switch` por `Provider string`, acoplando a lógica de negócio ao fornecedor: 40+ assinaturas recebiam `*tts.Orchestrator` concreto. (c) A resposta era *ou* áudio *ou* texto, deixando sem saída o produtor que não pode ouvir. | 2026-08-22 | Criada a fronteira `ports.TTSProvider` (mesma estratégia de `LLMProvider`): `GenerateSpeech(ctx, text) (audio, mimeType, err)` + `Name()`. `internal/tts` dividido em `openai_compat.go` (cobre Piper, Groq e OpenRouter — todos falam `/v1/audio/speech`), `google_translate.go` (legado) e `factory.go` (`NewFromEnv`, único ponto que conhece fornecedor concreto). Todas as assinaturas migradas para a interface — os erros de "import não usado" que surgiram foram a prova do desacoplamento. Adotado **Piper auto-hospedado** (serviço no compose + volume `piper_data`, voz `pt_BR-faber-medium`, gratuito e sem cota). Entrega passou a ser **áudio + texto**, com o texto como canal garantido e o áudio best-effort enviado uma única vez (não se repete em retry). Adicionado `sniffAudioMIME` para rejeitar HTTP 200 com corpo não-áudio. Cobertura: 8 testes em `internal/tts` + 5 em `internal/queue/delivery_test.go`. **Requer rebuild (ver DT-25).** |
| DT-27 | **`SendVoice` incompatível com o contrato do `evolution-go`** — bot nunca respondia em áudio. O adapter enviava JSON com `media: "data:audio/ogg;base64,..."` + `mediatype` (contrato da Evolution API v2 em Node), mas o fork `evolution-go` usado aqui expõe `/send/media` com dois caminhos: (a) JSON exigindo `url` (`data.Url`, consumido via `http.Get` — não aceita base64 nem data URI) e (b) multipart aceitando os bytes em `file` + `type`. Resultado: todo envio retornava `400 {"error":"URL is required"}` e caía no fallback para texto. | 2026-08-22 | Fix: `SendVoice` reescrito para `multipart/form-data` com campos `number`, `type=audio` e `file` (bytes decodificados do base64). O `evolution-go` converte para Opus automaticamente e envia com `PTT=true`, então o parâmetro `isPtt` passou a ser redundante nesse caminho. Compila e passa `go vet`. **Requer rebuild da imagem para ter efeito em produção (ver DT-25).** |
| DT-23 | **Sidecar `clockwork` (NTP) quebrado silenciosamente** — causa raiz de "hora errada" + 100% dos downloads de áudio do WhatsApp falhando com 403. `ntpdate` foi removido dos repositórios do `alpine:latest` (`apk add ntpdate` falhava sempre, erro engolido por `2>/dev/null \|\| true`). Sem correção de drift, o relógio do host derivou (2.94s medido às 07:15:52 do dia do incidente) e a Evolution API passou a rejeitar a assinatura das chamadas à WhatsApp API com 401/403, derrubando todo job de download de áudio para dead-letter após 3 tentativas. | 2026-08-22 | Fix: `command` do serviço `clockwork` em `docker-compose.prod.yml` trocado para usar o applet `ntpd` embutido no busybox do Alpine (`busybox ntpd -n -q -p time.google.com`), que não depende de nenhum pacote via `apk` e não silencia erros. Container recriado em 2026-08-22 e o `ntpd` voltou a executar de fato. **PORÉM: conclusão parcial** — a correção não persiste (o relógio segue ~4s atrasado). O download de mídia voltou a funcionar (403 → 200), mas o drift não foi eliminado. Continuação rastreada em **DT-26**. Ver também `state.md` item 9. |
| DT-33-DETALHE | **Telemetria granular por chamada de LLM e deadlines por etapa** — `LLMCallTelemetry` com `request_id, model, input_tokens, output_tokens, latency_ms, time_to_first_token, status_code, timeout_stage, retry_count, retrieved_chunks, prompt_version, cost_estimate`. Deadlines por etapa aplicados: retrieval (2s) e classificação (5s); geração mantida em 25s (respeitando medições legítimas do DT-41) e validação JSON descartada como puramente CPU. Custos calculados via `internal/pricing`. | 2026-09-03 | Instrumentação completa via `telemetry.LogLLMCall` nos 4 call sites de `client.go` (`CallOpenRouter` e `CallGoogle` com sucesso, erro e resposta vazia). Cálculo de custo em USD via catálogo multi-fornecedor do pacote `pricing`. Propagação de `retrieved_chunks` a partir de `tools_rag.go` para chamadas Meta-RAG. `time_to_first_token` mantido em -1 (marcador aguardando streaming em DT-41-STREAM) e `prompt_version` vazio aguardando convenção. |

---

## 🎯 Próxima Frente Ativa

> **Atualização 2026-09-08:** frente ativa agora é o **sprint de segurança
> pré-viagem** (~2 dias) — plano completo, sequenciado e com checklist em
> [`PLAN-sprint-pre-viagem-2026-09-08.md`](./PLAN-sprint-pre-viagem-2026-09-08.md).
> Foco: RPCs/RLS abertas a `anon` (DT-93/94/95/96/106/107/108/111), auth
> sem rate-limit (DT-109/126), integridade de dados no onboarding
> (DT-74/75/77/88), hardening básico do servidor Go (DT-79/80) e
> credenciais/portas do RabbitMQ (DT-122). O restante deste documento
> abaixo desta nota é histórico de frentes anteriores (VPS, DT-58/59) —
> mantido por registro, não é mais a prioridade corrente.

> **Mudança de prioridade decidida em 2026-08-24:** **DT-58** (cadastro 100% via WhatsApp) e **DT-59** (centralização do backend em Go) assumiram a frente ativa, a pedido do responsável. A cadeia da VPS descrita abaixo (`DT-45` → `DT-38`, prazo ~2026-09-05) foi **conscientemente despriorizada** — não esquecida. Registrado aqui de propósito: o prazo de 2026-09-05 continua de pé no calendário e ninguém o cancelou; o que mudou foi a ordem de ataque. Se a data se aproximar sem o corte para VPS, a decisão a rever é esta.
>
> **Ordem de execução acordada para a nova frente:**
> 1. ✅ **Fatia 1 — autenticação no Go** (`DT-61`, concluída em 2026-08-24). Era pré-requisito das outras duas *e* fechava um buraco aberto.
> 2. **Fatia 2 — DT-58**, o fluxo de onboarding em `fsm.go:196`. As peças de banco (`CreateAuthUserByPhone`, `SetupInitialProfile`) já estão prontas.
> 3. **Fatia 3 — DT-59**, o grupo `/api/v1/*` para as rotas de escrita, mais o `ADR-009`.
>
> **Atualização 2026-09-01:** o `DT-45` (chave da Evolution exposta em repositório público desde 2026-07-30) foi rotacionado — não é mais exposição ativa. O parágrafo abaixo descreve o estado em que essa decisão de despriorização foi tomada, mantido por registro histórico.

---

**Cadeia real de bloqueio da VPS (`DT-38`, prazo ~2026-09-05) — despriorizada em 2026-08-24, mantida aqui para referência:**
1. ✅ **DT-45** — concluído em 2026-09-01 (rotacionar a chave da Evolution exposta publicamente desde 2026-07-30). Era o único item que o documento amarrava como pré-requisito **real** do DT-38.
2. **DT-38** — corte real (reconexão do WhatsApp via novo QR, o passo mais arriscado): pré-requisito de segurança já satisfeito, segue dependendo só de decidir provisionar a VPS.

**🔥 Alta que NÃO bloqueiam a VPS** — prioritários, mas independentes de qual máquina roda o container: `DT-01`, `DT-46`, `DT-42`, `DT-18`, `DT-21` (residual). Atenção especial ao **DT-30**: parece pré-requisito mas é o contrário — é a VPS que desbloqueia o DT-30 ("um host sempre ligado destrava o DT-30", texto do próprio DT-38), não o inverso.

**Pode ser feito hoje, sem esperar o DT-45:** teste de capacidade isolado numa VPS enxuta descartável, com `cmd/loadtest_piper` (não toca WhatsApp nem a chave real) para validar contra hardware de nuvem de verdade os números já medidos localmente no DT-38 (~5,2s + 0,037s/caractere, 1 vCPU, Piper como único gargalo de CPU). Isso é dimensionamento, não é o corte de produção — não precisa do DT-45 nem do DT-38 andarem primeiro.

**`DT-53`** (self-healing) fica mais crítico depois do DT-38 fechar, não antes — ver observação no próprio item.

---

## 📜 Histórico de Mudanças

| Data | Alteração |
|------|-----------|
| 2026-09-09 | **DT-74 e DT-75 fechados juntos — `complete_onboarding` ganhou idempotência e validação explícita de modalidade.** Migration `20260909030000_dt74_75_complete_onboarding_idempotencia_modalidade.sql`: fast-path via `profiles.propriedade_ativa_id` (DT-74) combinado com dois índices únicos parciais + captura de `unique_violation` como rede de segurança contra corrida de chamadas concorrentes, e erro explícito para modalidade fora do enum conhecido em vez do fallback silencioso para `CONVENCIONAL` (DT-75). Verificado sem duplicatas em produção antes de escrever a migration; testado em staging (idempotência e erro de modalidade); aplicado em staging e produção. **Nota de processo:** este débito foi trabalhado por duas sessões em paralelo no mesmo diretório — uma escreveu a checagem de idempotência + DT-75, a outra (esta) adicionou os índices únicos/tratamento de corrida; a versão final no repositório mescla as duas. A aplicação manual anterior de uma versão intermediária em produção/staging (fora do arquivo final) foi revertida do histórico de migrations (`supabase_migrations.schema_migrations`) para não deixar entrada sem arquivo correspondente. |
| 2026-09-09 | **DT-82 fechado — cota de ingestão de documentos não é mais burlável.** `handleKnowledgeUpload` (`internal/webhook/handler.go`) tornou `pmo_id` obrigatório: retorna 400 se ausente ou ≤ 0, eliminando o bypass que permitia criar `ingestion_jobs` com `pmo_id = 0` e pular a cota de 3 docs para non-pro. A rota não usa `RequireAuth` (usa token estático compartilhado), então a extração de `pmo_id` do JWT não era viável — decidiu-se por tornar o campo obrigatório. Testes novos: `TestKnowledgeUpload_RejectsMissingPmoID` e `TestKnowledgeUpload_RejectsZeroPmoID` em `internal/webhook/knowledge_upload_test.go`. `go build ./...`, `go vet ./...` e `go test ./internal/webhook/... -v` verdes. Branch: `fix/dt82-quota-bypass`. |
| 2026-09-08 | **DT-30 fechado por completo — pipeline de migrations rodando ponta a ponta contra produção pela primeira vez.** Trocado `--db-url`/`PROD_DB_URL` (nunca configurado) por `supabase link` + `SUPABASE_ACCESS_TOKEN`/`SUPABASE_DB_PASSWORD`. Isso destravou o `Verify Schema Parity`, que por sua vez revelou 3 bugs reais de migration nunca testados contra o banco real (policy duplicada do DT-70, `conversations.tenant_id` com tipo errado, `profiles.tenant_id`/`pmos.organizacao_id` nunca criadas) e uma migration fantasma (`20260825020000`, marcada como aplicada sem nenhum efeito real — nem a tabela `conversations` existia). Histórico de produção corrigido e migration reaplicada de verdade via `db push --include-all` (flag removida no commit seguinte). Achado no caminho: um **segundo** gitlink órfão do `mcp/`, mesmo bug já corrigido em 02/09 mas só na branch `feature-multicanal` (nunca mergeada até hoje) — retrabalho duplicado pela falta desse merge. A branch inteira (62 commits, incluindo DT-46/62/70/59/61) foi reconciliada com o `main` nesta sessão, com build/vet/testes/typecheck verificados antes do push. Abertos DT-128 (build/publish do container ainda manual) e DT-129 (secrets de E2E faltando) para o que ficou de fora do escopo original do DT-30. |
| 2026-09-06 | **DT-44 resolvido — validação de dígito verificador antes de redigir PII.** Aplicado via cherry-pick do `7029433` (= commit `3b0f5cc` da `fix/bigpickle-bugfix-loop`): `filter_pii.go` ganhou `isValidCPF`/`isValidCNPJ` (cálculo de DV, rejeita sequências repetidas) como `validator` das regras `pii_cpf`/`pii_cnpj`; o cherry-pick exigiu correção acompanhante — a regex de telefone nunca casava DDD compacto (`11999998888`) e o teste de telefone só "passava" antes porque o falso-positivo do CPF redigia a sequência de 11 dígitos (exatamente o bug do DT-44). Ajustada a regex para `(?:\+?55\s?|\b|\()(?:\(?\d{2}\)?\s?)?\d{4,5}-?\d{4}\b` (âncora/`(` antes do DDD, final exigindo limite de palavra → não re-casa dentro de lotes/CNPJ longos) e `sensitivity_test.go` atualizado (CPF válido `529.982.247-25` → `identificador_direto`; lote `DT391787418354` → `nao_sensivel`). Cobertura nova em `filter_pii_test.go` (`TestIsValidCPF`, `TestIsValidCNPJ`, `TestPIIScrubber_FalsePositives`, `TestHasPII`, `TestRedactPII`). `go build`/`go vet`/`go test ./...` verdes. Linha do DT-44 marcada como resolvida in loco (mesmo padrão do DT-62). |
| 2026-09-06 | **Decisão registrada: caminho Azure abandonado — `azure-infra/` removido do repo.** Por diretriz do responsável ("não vamos por esse caminho"), a pasta `azure-infra/` (main.bicep, azuredeploy.parameters.json, docker-compose.azure.yml, README.md) foi removida via `git rm` e a entrada correspondente do `DOCUMENTATION-INDEX.md` retirada. A topologia de gateway único (só o 8080 público, evolution-go privado na bridge) tinha sido referenciada no DT-122 como prova de intenção — a menção foi reescrita sem citar arquivo deletado (o DT-125 segue de pé, autossustentado pela exposição real das portas no host). Mantidos por NÃO serem Azure: `deploy/` (Prometheus/Grafana/load-test), `docs/deployment/` (docker local), `evolution-go-source/docs/wiki/deploy-producao/`, `mcp/` (SDK MCP da Microsoft vendorizado), `.github/workflows/` (sem workflow Azure — `deploy-aci.yml` já fora removido no DT-124). |
| 2026-09-06 | **2ª onda da Auditoria de Defesa Profunda (infra/compose) — aberto DT-125 (par do DT-122).** `evolution-go` publica `8082:8082` em 0.0.0.0 no `docker-compose.prod.yml:131-132` sem necessidade funcional (o bot alcança pela rede interna `evolution-go:8082`): a API completa do WhatsApp fica alcançável por quem atingir a porta. Mitigação real confirmada: todas as rotas de dados exigem `apikey` (instância ou `GLOBAL_API_KEY` admin) em `pkg/routes/routes.go`; públicas só `/swagger`/`/manager`/`/server/ok`. Registrado como 🟡 Média ⚡ condicional: escala a Alta se o token de instância ou a `GLOBAL_API_KEY` (`.env.prod`) vazar — vira controle total do WhatsApp do produtor; middleware de auth sem rate limit (força bruta contra porta pública). Sugestão: remover o `ports:` (rede interna resolve) ou bind localhost. |
| 2026-09-06 | **DT-124 concluído — poda do histórico git: binários, sessão WhatsApp (LGPD) e segredos removidos; .git caiu de 365 para 163,72 MiB (-55%).** Etapa 1: `.gitignore` da raiz + `git rm --cached pmo-bot-go/cmd/server/seu-bot` (42,5 MB commitado), push na branch `fix/bigpickle-bugfix-loop`. Etapa 2: `git filter-repo` (4 rodadas) removeu do histórico `tmp_test_bin`, `tmp_bot`, `tmp_server`, `seu-bot`, `pmo_bot/wppconnect-profiles/` (~75 MB de sessão WhatsApp), `temp_benchmark.exe`, `scratch/`, `repomix-output.xml`, `.skills_temp/`, `test_scanned.pdf`, `.env.prod`/`.env` (chave Groq), 3 scripts JS com `sb_secret_*`/`gsk_*`, `deploy-aci.yml` (Segredos Supabase/Groq) e `.vscode/mcp.json` (PAT Azure DevOps) — estes 3 últimos descobertos porque o **GitHub Push Protection (GH013)** bloqueou o push; removidos em vez de "unblock-secret". Após cada rodada: re-add do `origin`, restauração do WIP (snapshot `manejo-org-wip-20260906`), `git reflog expire` + `git gc --prune=now --aggressive`. **Force-push de todas as branches e tags** (decisão explícita do responsável) — 0 branches divergentes, 0 segredos restantes em todo o histórico (verificado com `git grep`), tags reescritas. Backup mirror pré-operação em `manejo-org-backup-20260906.git` (366 MB). 3 worktrees `.claude/` deixadas como estão (apeitam SHAs reescritos, seguem funcionando). Detalhes e métricas no item DT-124. Sem ação pendente a não ser a rotina da Etapa 1. |
| 2026-09-06 | **2ª onda da Auditoria de Defesa Profunda (infra/compose) — abertos DT-122 e DT-123.** RabbitMQ de produção com credenciais `admin/admin_password` hardcoded no repo público e portas 5672/15672 publicadas em 0.0.0.0, transportando payloads completos do WhatsApp e permitindo injeção de webhooks forjados (DT-122, 🔥 Alta ⚡ condicional à rede); `cap_add: SYS_TIME` desnecessário após o DT-26 (DT-123). Descartados com evidência: `worker.go:44` (spec cron constante), token de webhook em query (design do evolution, comparação em tempo constante em `handler.go:849`), compose de dev (Prometheus/Grafana locais). |
| 2026-09-06 | **2ª onda da Auditoria de Defesa Profunda — abertos DT-119 a DT-121 (observabilidade, abuso autenticado, goroutines).** Revisão dirigida a áreas ainda não varridas: `/metrics` aberto sem auth quando `PROMETHEUS_AUTH_TOKEN` ausente (DT-119, ⚡ condicional), gateway `/api/v1/rpc/*` sem rate limit por usuário vs. webhook/mapa que têm (DT-120), e goroutines sem `recover` fora do webhook — playground `knowledge/handler.go`, `logger_supabase.go`, greeting-guard `fsm.go`, consumer RabbitMQ (DT-121, extensão do padrão DT-113). Descartados com evidência como corretos: `map_handler` (semáforo+wg), CORS allowlist, JWKS (ES256 fixo + curva + refetch mínimo), cleanup de `setInterval`/`setTimeout` no frontend, `style.innerHTML` (CSS estático), janitor do embedcache, filtro de realtime por `propriedade_id`, SW sem cache de API. |
| 2026-09-06 | **Auditoria de Defesa Profunda — abertos DT-106 a DT-118 (migrations do harness, storage, concorrência/input do Go).** Revisão da primeira vez de `pmo-bot-go/migrations/` + storage + segurança de frontend + Go: 1 Crítica condicional (`user_memory_profiles` sem RLS + `match_user_memory` `EXECUTE` público, DT-106), 3 Altas (policies `USING (true)` do harness DT-107, bucket `avatars` sem dono DT-108, `CONECTAR` sem brute-force/expiry DT-109), 5 Médias (views sem `security_invoker` DT-110, bucket `comprovantes` DT-111, `sessionMu` sem eviction DT-112 — CSP e rotas `/lab`·`/teste-mapa` registrados como F22/F23), 6 Baixas (DT-113 a DT-118). Itens de frontend registrados como **F21 a F27** no `TECHNICAL_DEBT.md`. Relatório completo em [`docs/audit-defesa-profunda-2026-09-06.md`](../../docs/audit-defesa-profunda-2026-09-06.md). Confirmado ao verificar: `message_queue`, `guardrail_events`, `hitl_pending` e `ingestion_jobs` **JÁ têm RLS no schema canônico** (`20260402120000_create_operational_tables.sql:187,309-311,329,331-333` e `20260721180000_knowledge_ops_panel.sql:159,225,232`) — o risco real está na aplicação parcial das migrations do harness em produção (mesma root cause dos DT-22/DT-70). |
| 2026-09-05 | **Auditoria de RLS completa — abertos DT-93 a DT-105.** Revisão dirigida de `supabase/migrations/` (137 `CREATE POLICY`, 55+ `SECURITY DEFINER`): 4 Críticas (funções `SECURITY DEFINER` com `EXECUTE` aberto a `PUBLIC`/anon — `save_pmo_memory_cache`/`match_pmo_memory_cache` DT-93, `create_or_supersede_mutation_draft`/`commit_mutation_draft` DT-94, RPCs de Knowledge Ops + `upsert_arena_models` DT-95, fallback a `user_metadata` em `get_knowledge_role()` DT-96), 4 Altas (`profiles`/`FOR ALL` sem `WITH CHECK` DT-97, `knowledge_chunks` `USING (true)` DT-98, policy admin por claim JWT DT-99), 4 Médias (grants a anon nunca revogados DT-100/DT-101, corpo IDOR de `rpc_registrar_compra_insumo` DT-102, `seed.sql` quebrado DT-103) e 2 Baixas (experimentos sem escopo por pmo DT-104, bucket de áudios público DT-105). Relatório completo em [`docs/audit-supabase-rls-2026-09-05.md`](../../docs/audit-supabase-rls-2026-09-05.md). Todos os achados confirmados em arquivo:linha antes de registrar. |
| 2026-09-04 | **Auditado o estado do re-ranking e decidida a questão do Langfuse — abertos DT-71, DT-72 e DT-73.** O rerank Cohere está implementado e instrumentado, mas só é exercitado pelo binário de arena; produção nunca passa por ele, embora a wiki afirme o contrário (DT-71). A função também não tem timeout nem fallback próprio — o fallback vive no chamador de teste, então promovê-la a produção hoje introduziria um travamento de worker (DT-72). Sobre observabilidade: **decidido não adotar Langfuse**. Os três trilhos existentes (slog estruturado com `LLMCallTelemetry`, Prometheus com `RagLatencyMS` por fase em `/metrics`, e as tabelas `rag_*` com o painel Knowledge Ops) já cobrem o que se usaria a ferramenta para fazer; os gaps reais são persistir a telemetria de LLM numa tabela consultável e propagar um `trace_id` entre as camadas (DT-73), ambos resolvíveis na stack atual. Observado de passagem, sem débito próprio por falta de confirmação: `docker-compose.prod.yml` não sobe Prometheus nem Grafana, então o endpoint `/metrics` pode não estar sendo raspado por ninguém — verificar se há scraper externo antes de abrir item. |
| 2026-09-03 | **DT-33-DETALHE concluído** — Instrumentação rica por chamada de LLM em `internal/gemini/client.go` e `internal/telemetry/tracker.go`, integrando catálogo de custos (`internal/pricing`), contadores de chunks pós-retrieval (`internal/mcp/tools_rag.go`), deadlines específicos por etapa (5s classificação, 2s retrieval) e rastreamento de timeouts granulares. |
| 2026-09-03 | **DT-18 reinvestigado — escopo original majoritariamente já resolvido, board estava defasado desde agosto.** O REVOKE global (`20260816030000`) já estava aplicado, e 4 migrations de 18/08 já criaram a maioria das RPCs `SECURITY DEFINER` pedidas. Escopo real restante, bem menor: `Secao9.tsx` (+ bug de `pmo_id` ausente no insert), 3 funções de `pmoService.ts` (`logs_treinamento`), e `PropertyProfilePage.tsx` (`limites_seguranca`, nem citado na entrada original). Plano completo em [`PLAN-dt18-frontend-write-migration.md`](./PLAN-dt18-frontend-write-migration.md). Achado colateral corrigido à parte: **DT-70**. |
| 2026-09-03 | **DT-70 aberto e fechado no mesmo dia — IDOR de leitura/escrita ao vivo em produção, achado ao investigar o DT-18.** `canteiros`, `analises_solo`, `culturas_anuais` e `pmo_propagacao` tinham policy RLS permissiva (`auth.role() = 'authenticated'` sem checar dono) que expunha dados de qualquer produtor a qualquer usuário autenticado — sem migration correspondente no repo, criadas fora de banda direto em produção. Corrigido via `20260903100000_fix_dt70_permissive_rls_policies.sql`, validado em transação local com rollback antes de aplicar, confirmado ao vivo em produção. |
| 2026-09-02 | **DT-07: fallback automático de OCR adicionado, os 17/17 PDFs agora indexados.** `L10831.pdf` (PDF puramente escaneado, 0 caracteres extraíveis) tinha ficado de fora da ingestão inicial. `scripts/rag_ingest.py` ganhou `ocr_pdf_to_text()`: quando a extração normal por PyMuPDF volta vazia, roda `ocrmypdf`/Tesseract (idioma Português) via Docker (`jbarlow83/ocrmypdf`) numa cópia temporária do arquivo antes de desistir — cobre esse caso e qualquer PDF escaneado futuro, sem exigir intervenção manual (só Docker Desktop rodando). Testado isoladamente primeiro (extração via OCR confirmada legível e correta), depois via o pipeline completo contra produção: OCR extraiu 7.353 caracteres, 7 chunks gravados em `farm_documents`, retrieval confirmado via `match_documents_with_context` (similaridade 1.0 no próprio chunk). **Total final: 17 de 17 documentos, 847 chunks em produção.** |
| 2026-09-02 | **DT-07 implementado e fechado, movido para 🟢.** Plano de [`PLAN-rag-ingestion-unification.md`](./PLAN-rag-ingestion-unification.md) executado: `scripts/rag_ingest.py` adaptado para gerar embeddings via OpenRouter/bge-m3 (mesmo contrato de `GetEmbedding`) e gravar em `farm_documents` (mesmo contrato de `UpsertFarmDocumentChunks`), mantendo o extrator PyMuPDF da ADR-007. Validado em Postgres local antes de tocar produção (2 PDFs de teste, retrieval real confirmado via `match_documents_with_context`). Rodado contra produção para os 17 PDFs reais: 16 documentos / 840 chunks ingeridos com sucesso, `L10831.pdf` sem texto extraível (PDF escaneado, gap de OCR conhecido desde a ADR-007 original, não bloqueante). Retrieval real re-confirmado em produção. `pmo-bot-go/scripts/rag_ingest.py` (cópia antiga em Docling) e `pmo-bot-go/cmd/ingestor/` removidos como código morto; `go build ./...` limpo. ADR-007 e `docs/guides/rag-ingestion-lessons-learned.md` atualizados com addendum apontando para o pipeline atual. |
| 2026-09-02 | **DT-07 investigado a fundo, sem implementação (a pedido) — achado bem maior que a pergunta original.** `rag_ingest.py` (Docling) e `cmd/ingestor` não são alternativas, são estágios do mesmo pipeline. O problema real: duas linhas de ingestão de RAG paralelas e desconectadas, usando embeddings diferentes — a que funciona hoje (Docling → `cmd/ingestor` → OpenRouter/bge-m3 1024d → `farm_documents`, a tabela que o bot realmente consulta) e a "nova" do ADR-007 (PyMuPDF, sem o bug de acentuação → Gemini 3072d → `knowledge_chunks`, que nada no bot consulta, zero linhas hoje). As únicas linhas em `farm_documents` eram dados de teste — **nenhum dos 17 PDFs da base de conhecimento é pesquisável pelo bot.** Plano recomendado registrado no item: unificar em `farm_documents`, adaptando o extrator bom (PyMuPDF) para o espaço vetorial que já está em uso (OpenRouter/bge-m3), depois ingerir os 17 documentos reais e remover o pipeline antigo como código morto. |
| 2026-09-02 | **DT-59 concluído por completo e movido para 🟢** — confirmado ao vivo que o deploy do backend não é necessário (o container de produção real já reconhecia os 10 nomes do gateway antes desta sessão, via `curl` contra `localhost:8080`) e que o deploy do frontend é automático ao mesclar a branch. Resolvida também a preocupação do ADR-003 (offline-first) que estava marcada como não verificada: o motor de sync só passa pelas funções já migradas, sem risco. **DT-61 também fechado de vez:** a prova de ponta que faltava (`curl` sem auth → 401 contra o servidor real) foi feita como parte desta verificação. |
| 2026-09-02 | **DT-59: migração das 8 chamadas ao gateway concluída** (5 já estavam feitas, registro desatualizado; migradas as 3 restantes). **Achado não previsto:** `locationService.ts`, um segundo serviço de talhão usado por componentes reais (`PropertyMap.tsx`, `MapaPropriedade.tsx`), duplicava chamadas diretas ao PostgREST que `talhaoService.ts` já tinha migrado — também corrigido. Achado e corrigido de passagem: `propriedadeService.test.ts` mockava o caminho antigo, já quebrado antes desta sessão. Verificado sem regressão (testes, build, assinaturas de RPC), mas sem teste em navegador real. **Deploy do backend confirmado desnecessário** — o gateway já roda em produção desde ontem (`curl` local contra o container confirmou os nomes novos no allowlist); deploy do frontend é automático ao mesclar a branch. DT-59 code-complete. |
| 2026-09-02 | **DT-04 fechado por completo e movido para 🟢, com decisão final.** Revisão de hoje corrigiu a leitura de ontem: `domain.ProcessAudioMessage` não exige substituir o FSM inteiro, só trocaria UMA etapa (transcrição de áudio) por uma alternativa já pronta (Gemini nativo com fallback automático pro Groq Whisper) — a integração seria pequena, todas as peças já existem e são testadas. **Decisão do responsável: não fazer, por custo** — Groq Whisper é gratuito/muito mais barato, e a rota do Gemini tentaria primeiro em toda mensagem de áudio, cobrando por isso. `llm.LLMProvider` segue sendo a interface certa para o FSM enquanto essa análise de custo não mudar. |
| 2026-09-02 | **DT-02, DT-03, DT-05, DT-06 fechados (limpeza de código legado); DT-04 reformulado; DT-48 resolvido com achado urgente; DT-47 reconfirmado, não executado por decisão do responsável.** DT-02: `InsertFarmDocument` (zero chamadores) removido, já substituído por `UpsertFarmDocumentChunks`. DT-03: os 2 call sites do pipeline legado de áudio passaram a usar o `DeriveFileName(mimeType)` já testado, em vez de hardcodar `"audio.ogg"`. DT-05: model ID defasado em `cmd/tester/rag` atualizado; teste inerte `fallback_simulation_test.go` (nunca chamava a função que dizia testar) removido. DT-06: 2 scripts soltos e não commitados removidos de `scratch/`, corrigindo a quebra histórica de `go build ./...` na raiz. DT-04: investigado e reformulado — `llm.LLMProvider`/`ports.LLMProvider` são interfaces deliberadamente separadas por design (documentado no próprio código), não legado esquecido; "descomissionar" dependeria de completar uma troca de arquitetura (FSM → `internal/domain`) que nunca foi ligada ao fluxo real. **DT-48: a premissa original estava desatualizada** (os 5 objetos já tinham migration no repo, só nunca tinham sido aplicados em produção) — e um deles, `get_rastreabilidade_publica`, é usado pela página pública de rastreabilidade do frontend: **estava quebrada em produção** até a correção de hoje. 4 migrations aplicadas em produção com confirmação explícita; sobrecarga obsoleta de `rpc_registrar_compra_insumo` (12 args, sem idempotência) removida de staging. DT-47 reconfirmado ao vivo como seguro de remover, mas deixado como está por decisão do responsável. |
| 2026-09-02 | **DT-37/DT-33 revisados — sem novo corte de payload possível com segurança, board consolidado.** As entradas de DT-33/DT-37 estavam duplicadas em três seções (A Fazer, Em Andamento, Concluído) com textos parcialmente contraditórios entre si — consolidado numa entrada por seção. DT-33 (medição de timeout) já estava de fato concluído desde 22/08, só não tinha sido movido; DT-37 confirmado ainda bloqueado por falta de telemetria real: a amostra disponível (`logs/.telemetria_acumulada.log`) continua sendo a mesma simulação sintética de 22/08, sem tráfego real acumulado desde então — cortar ferramentas por intent sem esse dado arriscaria quebrar em silêncio um registro legítimo sob o intent errado. Verificado de novo que a limpeza de fragmentos de system prompt vazados em descrições de tool (feita em 22/08) não deixou nenhuma outra instância nas 29 ferramentas. |
| 2026-09-02 | **DT-30 — `Tests & Coverage` corrigido e confirmado verde (`33587950367`), primeira vez em semanas.** 5 causas empilhadas: testes que exigem Postgres real sem infraestrutura para isso em CI (build tag `real_postgres`); um `internal/state` quebrado desde o refactor do DT-67, nunca pego porque o `go vet` daquele dia só rodou escopado ao pacote `mcp`; um gate de cobertura de 80% que a base real (19,3%) nunca atingiu; `go.sum` faltando entradas de `golang.org/x/oauth2` (só funcionava localmente porque `go.mod`/`go.sum` já estavam modificados sem commit); e `setup-go` instalando Go 1.21 e deixando o toolchain automático baixar 1.25 por cima, faltando a ferramenta `covdata`. Achado no caminho: um `mcp/` na raiz do repo era um clone não relacionado do SDK MCP da Microsoft, commitado como gitlink quebrado por engano em março — removido. Restam abertos, aguardando decisão: secrets de `Backend E2E`/`E2E` (qual ambiente usar) e o pipeline de deploy do container em si (represado até a migração para VPS, DT-38). |
| 2026-09-02 | **DT-21 concluído e movido para 🟢** — staging estava 17 migrations atrás de produção (desde 24/08), com 8 entradas de histórico próprias sem arquivo local (6 drift puro de bookkeeping, 2 investigadas e confirmadas sem risco — só comentários de debug). 6 migrations de feature nunca tinham sido aplicadas em staging, incluindo a correção original do DT-62. Todas aplicadas agora, mais as 4 correções de hoje (DT-69/DT-62/DT-46 x2) que eu tinha aplicado só em produção mais cedo — restaurando a paridade que eu mesmo tinha quebrado. 15 versões reconciliadas via `migration repair`. Confirmado ao vivo que staging reflete produção em tudo relevante para segurança. |
| 2026-09-01 | **DT-19 concluído e movido para 🟢** — bônus da investigação do DT-22 (mesma categoria de drift, incidente mais antigo). As 4 migrations que o item apontava como desincronizadas (20260525, 20260526, 20260609, 20260610) batem 1:1 entre local e remoto, tanto em versão quanto em nome; spot-check de conteúdo confirmou sem divergência de schema. |
| 2026-09-01 | **DT-22 concluído e movido para 🟢.** O incidente original (2026-08-17) já estava corrigido. Achado hoje: `apply_migration` do MCP do Supabase grava migrations em produção sob timestamps auto-gerados, diferentes do arquivo local — causou drift em 7 migrations desta sessão e da anterior (DT-68), mais um índice (`idx_mq_msg_id`) que só existia em produção, sem arquivo local. Conteúdo conferido byte a byte, arquivo recuperado, e os 7 pares reconciliados via `supabase migration repair --linked` (só bookkeeping, sem executar SQL — confirmado que o schema real não mudou). **Achado colateral:** o DT-62 já tinha sido corrigido em produção em 2026-08-24 por essa mesma causa de drift; o board nunca foi atualizado e uma sessão posterior o "corrigiu" de novo sem necessidade (sem risco, só redundante) — registro corrigido. |
| 2026-09-01 | **DT-46 concluído por completo e movido para 🟢** — os achados vizinhos que ficaram de fora da correção anterior (núcleo de autorização) foram resolvidos: as 3 sobrecargas de `match_chunks` (uma delas quebrada, referenciando coluna inexistente) e as 3 de `criar_infraestrutura_pmo` (já sem `GRANT` a ninguém desde o DT-65) removidas por serem código morto confirmado (grep em todo o repo, zero chamadores reais); `is_chemical_input` corrigida de `IMMUTABLE` para `STABLE` (chama `unaccent()`, que é `STABLE`); `validate_file_extension` perdeu o `SECURITY DEFINER` desnecessário. Aplicado em produção com confirmação explícita, reconfirmado ao vivo. |
| 2026-09-01 | **DT-46 (núcleo de autorização) corrigido e aplicado em produção, movido para 🟡 Em Andamento** — `get_dashboard_stats` e `get_propriedade_metrics` (esta era um IDOR puro) ganharam checagem de admin/posse; `increment_usage_stats`, órfã e perigosa (gravava contadores de uso de qualquer usuário), revogada de todo mundo; `get_traceability_data` parou de vazar endereço completo do produtor no QR público de rastreabilidade. `registrar_atividade_pmo`/`criar_infraestrutura_pmo` já tinham sido fechadas pelo DT-65. Restam só achados de correção de código (coluna inexistente em `match_chunks`, rótulo `IMMUTABLE` incorreto, overloads mortas) — sem risco de autorização, cada um fica para revisão própria. |
| 2026-09-01 | **DT-62 concluído e movido para 🟢** — `view_conversas_recentes` deixava qualquer chamador anônimo (sem sessão) ler telefone e última mensagem de todos os produtores, porque a view rodava com as permissões do dono (`postgres`) e ignorava a RLS de `messages`. Corrigido com `security_invoker = true` na view + `REVOKE ALL ... FROM anon`, validado em local (anon passou de ler tudo para `42501`) e **aplicado em produção** com confirmação explícita, reconfirmado ao vivo contra o endpoint REST real (`anon` → `401`, `authenticated` mantém acesso para o Monitor ao Vivo). |
| 2026-09-01 | **DT-66 concluído e movido para 🟢** — novo `TestFinanceiroBalanco_CrossTenantIsolation_RealPostgreSQL` prova isolamento de verdade (escreve despesa real numa propriedade, confirma pelo handler de produção que outra propriedade não a vê), corrigindo a suíte antiga que nunca lia/escrevia dado real. `MULTITENANCY.md` atualizado para refletir `TenantCtx` (DT-67) e apontar para o teste real em vez da alegação sem lastro. **Achado no processo e corrigido como DT-69:** `rpc_get_balanco_ia` comparava `tipo` em maiúsculo contra dados gravados em minúsculo desde 2026-08-16 — `consultar_balanco_financeiro` retornava zero para tudo em produção, silenciosamente, há mais de duas semanas. Corrigido via nova migration (`20260901130000_fix_balanco_ia_tipo_case_mismatch.sql`), validada em local e **aplicada em produção** (`hejewayflbuemnffrhae`) no mesmo dia com confirmação explícita do responsável, via MCP do Supabase escopado só a esta função. Confirmado que nenhuma transação real foi gravada em produção entre 16/08 e a correção — o bug nunca chegou a esconder balanço de ninguém. |
| 2026-09-01 | **DT-01 fechado por completo:** `service_role` legada desativada no painel do Supabase (aba "Legacy anon, service_role API keys"), confirmado o bot saudável logo depois (`docker logs`, sem erro) — esperado, já que o código só usava a chave nova desde a rotação anterior no mesmo dia. Move para 🟢 Concluído. |
| 2026-09-01 | **DT-45 e DT-01 rotacionados (Evolution API key/GLOBAL_API_KEY e Supabase service_role/secret key de produção), e DT-57 corrigido no mesmo fluxo.** Rotação feita com o valor novo gerado sem eu (Claude) precisar digitá-lo — usuário editou `.env.prod` diretamente após eu levantar todos os arquivos/variáveis envolvidos sem ler os valores. Primeira tentativa da chave da Evolution usava um valor com `$`, que o Docker Compose interpretou como referência de variável dentro do `env_file` e cortou um pedaço ao carregar — corrigido gerando um valor hexadecimal puro (sem símbolos) via `openssl rand -hex 32`, escrito no arquivo por indireção de variável de shell para nunca aparecer em texto nas minhas próprias mensagens. Ao verificar a saúde pós-rotação, descoberto que o `evolution-go` vazava o token novo em texto puro no `docker logs` por DOIS caminhos — não só a linha já documentada no DT-57, mas também o logger padrão do GORM, que imprime a query SQL completa (com o token interpolado) toda vez que o fallback de auto-cura de token (`GetInstanceByToken`) dispara um "record not found", o que acontece a cada rotação por design. Corrigidos os dois no mesmo commit: `IgnoreRecordNotFoundError: true` no logger do GORM (`config.go`) e `maskToken()` aplicado nas 7 ocorrências (uma a mais do que o DT-57 original documentava) que logavam o token cru em `whatsmeow.go`. Teste unitário novo, build/vet limpos, imagem reconstruída e `evolution-go` recriado em produção — reconectou sozinho, sem QR, confirmado saudável. Chave da Supabase confirmada funcionando com dado real (`Blacklist carregada: 3 itens`). Restam: desativar a `service_role` legada no painel do Supabase, decidir sobre reescrita do histórico do git (chave antiga da Evolution ainda legível no commit `163ce12`, mas já inválida), e atualizar `pmo-bot-go/.env` (dev local) se quiser consistência — nenhum dos três é urgência de segurança agora. |
| 2026-09-01 | **DT-68 concluído e movido para 🟢.** Valores reais (`MESSAGE_BUFFER_WINDOW=4s`/`MESSAGE_BUFFER_MAX=12s`) ativados em produção com confirmação explícita ("faça"), depois do deploy anterior com kill-switch confirmar o binário saudável. `docker compose up -d pmo-bot-go` bloqueado pelo classificador de auto mode por ser repetição de um comando de produção já executado na sessão — passou após reconfirmação. `evolution-go` recriado de novo junto (mesmo `env_file`), reconectou sozinho sem QR. Env vars confirmadas via `docker exec printenv`, stack inteiro saudável via `docker ps`. Único item pendente, não bloqueante: observar as métricas de coalescência por um dia de uso real antes de dar o rollout por plenamente maduro. |
| 2026-09-01 | **DT-68 — binário novo publicado em PRODUÇÃO** com `MESSAGE_BUFFER_WINDOW=0` (kill-switch), confirmação explícita do responsável antes do rebuild/recreate do container ao vivo. Descoberto e resolvido na hora: `evolution-go` também lê `.env.prod`, então editá-lo recriou os dois serviços, não só o pretendido — verificado (`docker inspect` + logs) que a sessão do WhatsApp, persistida no volume `evolution_data`, reconectou sozinha sem pedir QR novo. Stack confirmado saudável (`docker ps`, `printenv` nas env vars). Fica pendente, por decisão separada: trocar para os valores reais do buffer (4s/12s) e observar as métricas de coalescência em uso real. |
| 2026-09-01 | **DT-68 — schema aplicado em PRODUÇÃO** (`hejewayflbuemnffrhae`), com confirmação explícita do responsável depois do classificador de auto mode bloquear a primeira tentativa via MCP do Supabase (comportamento correto: mutação em banco de produção real). Verificado pós-apply (colunas, função com `pg_try_advisory_xact_lock`, `cleanup_message_queue`) e confirmado o bot ao vivo saudável via `docker logs` (heartbeat normal, WhatsApp CONNECTED) — mudança inerte, o binário em produção ainda não sabe usar as colunas/lógica novas. Fica pendente, condicionado a nova decisão explícita: o rollout do binário em si (Fases 6.5-6.6 do PLAN), que aí sim muda comportamento observável do bot para produtores reais. |
| 2026-09-01 | **DT-68 aplicado e validado em staging** (`srboqpxrzejtxjfgodnc`), via MCP do Supabase, com autorização explícita do usuário antes de aplicar qualquer coisa no ambiente compartilhado. 5/5 execuções estáveis da bateria de testes contra staging, depois de corrigir uma colisão de `msg_id` fixo com o índice único que uma tarefa paralela aplicou lá, e de descobrir (e não ser afetado por) um `supabase db reset` de outra sessão que zerou o schema DT-68 do Postgres local compartilhado — lição registrada no PLAN sobre múltiplas sessões/worktrees disputando o mesmo container Docker local. Confirmado `get_advisors` sem regressão de segurança nova. Falta produção e o rollout do binário. |
| 2026-09-01 | **DT-68 validado ponta a ponta contra Postgres real, com um bug de concorrência real pego e corrigido.** Rodado `npx supabase db reset` local para destravar os grants que bloqueavam os testes (ver entrada anterior); com o banco funcional, o teste de concorrência da Fase 5 (5 workers × 5 fragmentos) expôs que a primeira versão de `claim_next_message_job` escolhia o "pai" por `SKIP LOCKED` sem noção de telefone — dois workers concorrentes podiam vencer sobre linhas diferentes da mesma rajada, fatiando um turno em 2-3 "pais". Corrigido com `pg_try_advisory_xact_lock` por telefone, serializando o dreno inteiro antes de tocar qualquer linha. Também corrigido um falso positivo intermitente no próprio teste (órfãs de subteste vizinho sendo reivindicadas por workers sobrando) via limpeza e filtro por telefone — higiene de teste, sem relação com o comportamento de produção. Resultado: 6/6 execuções completas estáveis (`-count=1`) contra o Postgres local. Nada aplicado a staging/produção. Achados à parte sinalizados como sugestões separadas: `seed.sql` referencia `profiles.user_id`, coluna que não existe no schema atual (`db reset` aplica todas as migrations com sucesso mas falha ao semear); e dois testes real-Postgres pré-existentes (`mutation_drafts`, idempotência de cotas) falham por razões próprias assim que finalmente conseguem rodar sua lógica de verdade neste ambiente — não investigado, fora do escopo do DT-68. |
| 2026-09-01 | **DT-68 avançado — Fases 1-4 do plano de coalescência de mensagens escritas.** Duas migrations novas (colunas `merged_into_job_id`/`parts_count` + reescrita de `claim_next_message_job` com dreno transacional dos irmãos `ai_pending` do mesmo telefone, e `cleanup_message_queue` estendido para o status `merged`); `Manager.MarkAIPending` passou a calcular `next_retry_at` a partir de `MESSAGE_BUFFER_WINDOW`/`MESSAGE_BUFFER_MAX` (`parseEnvDuration` em `main.go`, mesmo padrão de `AUDIT_GC_INTERVAL`); `Job` ganhou `CreatedAt`/`PartsCount`; `ai_worker.go` loga `parts_count` e alimenta 3 métricas Prometheus novas; `media_worker.go` dispara `composing` já na entrada em `ai_pending`. `go build`/`go vet`/`go test` verdes em tudo que não depende de Postgres real. **Nada aplicado a nenhum ambiente.** Escrito também um teste de integração real (`message_buffer_real_postgres_test.go`) que não pôde ser validado: o Postgres local desta máquina subiu durante a sessão (stack `supabase_kong_manejo-org-app-clean`, sem relação com produção — confirmado via `docker inspect` que o bot de produção real aponta para o projeto hospedado `hejewayflbuemnffrhae.supabase.co`) mas `service_role` não tem GRANT de INSERT em `message_queue` nele (`has_table_privilege` retornou `f`); confirmado que é um gap pré-existente do ambiente local e não deste trabalho, reproduzindo a mesma falha em `TestMutationDrafts_RealPostgreSQL_Integration`, teste já existente e sem relação com o DT-68. Achado à parte, fora do escopo deste item, sinalizado como sugestão separada (não corrigido): `message_queue.msg_id` não tem UNIQUE constraint em nenhuma migration, então o `Prefer: resolution=ignore-duplicates` do `Manager.Enqueue` não tem alvo de conflito e a deduplicação por `msg_id` pode não estar realmente ativa no banco. |
| 2026-09-01 | **DT-67 concluído por completo (itens 2-4) e movido para 🟢.** `ToolHandler` trocou `profile *supabase.Profile` por `tenant TenantCtx` na assinatura de todos os handlers de tools MCP, com `buildTenantCtx` como único ponto de validação/construção antes de qualquer handler rodar. Novo `internal/mcp/tenant_guard_test.go` varre via `go/ast` todo `tools_*.go` proibindo leitura de identificadores de tenant de `args`, mais um teste de sanidade sobre `GetAllMCPTools()`. `RegisterTool` passou a aplicar o middleware incondicionalmente. `go build`/`go vet`/`go test` verdes em `internal/mcp`. Ver linha do DT-67 em Concluído para o detalhamento completo. |
| 2026-09-01 | **Item (1) do DT-67 corrigido:** `handleConsultarBalancoFinanceiro` (`internal/mcp/tools_financeiro.go`) parava de derivar `propriedade_id` de `args` do LLM e passou a usar `profile.PropriedadeAtivaID` direto, com erro amigável quando a propriedade ativa é zero. Varredura confirmou ser o único handler com esse padrão. `go build ./internal/... ./cmd/server/...`, `go vet ./internal/mcp/...` e `go test ./internal/mcp/...` verdes. Itens (2)-(4) do DT-67 (contexto de tenant tipado, teste de guarda genérico, middleware incondicional) seguem em aberto. |
| 2026-09-01 | Levantamento da arquitetura do agente (modelos/tools, WhatsApp+painel, multimodal, base de conhecimento) contra o código. Abertos **DT-67** e **DT-68**. O DT-67 corrige de passagem uma leitura errada que circulava: o dispatcher **já** valida a sessão em `validateProfile`, então os `if profile == nil` espalhados pelos handlers são redundância, não a proteção — o furo real é `handleConsultarBalancoFinanceiro` derivar a propriedade dos args do LLM e consultar com a service_role key, que ignora RLS. Instância concreta do que o **DT-66** aponta que a suíte de multitenancy não testa. O DT-68 ganhou plano próprio em `PLAN-message-buffer-coalescing.md`, encurtado por dois achados no código: o portão `next_retry_at` já existe e já é aplicado pela RPC de claim, e a promoção para `ai_pending` tem caminho único. Levantamento também confirmou como **não implementados** o fallback humano no Monitor ao Vivo (a tela é somente-leitura) e a janela de 24h/templates (bloqueada por decisão de gateway: a stack roda Evolution, não a Cloud API oficial). |
| 2026-08-24 | DT-65 corrigido e movido para Concluído: `REVOKE`/`GRANT` aplicados em staging e produção, corpo de `rpc_registrar_transacao_com_rateio` reescrito para validar posse via `auth.uid()`, testado ao vivo no bot em produção via WhatsApp |
| 2026-08-24 | Adicionados DT-65 (RPCs `*_arg` sem checagem de posse, `EXECUTE` público) e DT-66 (suíte de isolamento não testa isolamento) ao levantar o baseline dos [ADR-010](../../docs/architecture/adr/010-multitenancy-por-organizacao.md) e [ADR-011](../../docs/architecture/adr/011-abstracao-de-canal-de-chat.md) |
| 2026-08-16 | Documento criado a partir da consolidação de `state.md` + `estado_tarefa.md` |
| 2026-08-16 | Adicionadas decisões arquiteturais da Fase 2.2 (mutation_drafts/HITL) — DT-09 a DT-12 |
| 2026-08-17 | Concluída Fase 2.2 (DT-09 a DT-12): Migração SQL, RPCs atômicas, tool `propose_batch_mutations`, interceptor webhook HITL e 100% dos testes de concorrência e integração |
| 2026-08-22 | Diagnosticado e corrigido (código) DT-23: sidecar `clockwork` (NTP) inoperante por remoção do pacote `ntpdate` no Alpine — causa raiz de drift de relógio e falhas 403 no download de áudio do WhatsApp. Container recriado; downloads de áudio voltaram a funcionar (403 → 200). |
| 2026-08-22 | Investigação de follow-up sobre os logs pós-fix revelou 3 problemas adicionais: **DT-25** (container de produção defasado em 5 dias, quebrando `consultar_previsao_tempo`), **DT-26** (drift de relógio persiste apesar do DT-23 — correção não fixa no WSL2) e **DT-27** (`SendVoice` incompatível com o contrato do `evolution-go`, bot nunca respondia em áudio). |
| 2026-08-24 | **DT-63** concluído: configurado canal de alerta Telegram do self-heal (DT-53 Estágio 0), mudo desde a implementação. Motivado por duas quedas reais do WhatsApp no dia (DNS transitório do Docker, ~19min), detectadas pelo self-heal mas nunca notificadas. `pmo-bot-go` recriado com `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`, alerta de teste confirmado ponta a ponta. Na mesma sessão: `SELF_HEAL_ENABLED=true` ligado (segue em dry-run, sem reconexão automática de verdade ainda) e criado ping periódico de uptime (1h/3h/6h/12h/24h, depois a cada 24h) para confirmar "estou vivo" mesmo sem incidente. |
| 2026-08-22 | **DT-28** concluído: criada a fronteira `ports.TTSProvider` (mesma estratégia do `LLMProvider`), `internal/tts` dividido por fornecedor, adotado **Piper auto-hospedado** (voz natural, gratuito, sem cota) e entrega passou a ser **áudio + texto**. 13 testes novos. Aberto **DT-29** para a preferência de formato por produtor. |
| 2026-08-22 | **DT-25** concluído: rebuild da imagem de produção, levando junto DT-27 e DT-28. Piper validado em produção. Correção de registro: o `deploy_production.yml` **rodou** (e falhou por secret ausente), ao contrário do que se anotou antes — mas ele só faz `db push` e nunca deployou o container. Causa raiz aberta como **DT-30**. |
| 2026-08-22 | **DT-26** concluído: causa raiz do clock drift era o serviço `W32Time` do Windows parado (trigger-start por ingresso em domínio, máquina fora de domínio). Corrigido no host; drift caiu para ~0,4s estável. O sidecar `clockwork` virou sentinela — mede e alerta em vez de fingir que corrige. |
| 2026-08-22 | **DT-31** concluído: teste real no WhatsApp expôs que o TTS lia a formatação em voz alta, que a resposta longa perdia o áudio por timeout (WAV grande + teto de 30s) e que o áudio ia antes do texto. Criado `utils.SanitizeForSpeech`, Piper migrado para MP3 (~6x menor), timeouts ampliados e ordem invertida para texto→áudio. Lição registrada: os três só apareceram no uso real — o DT-28 tinha build, vet e 13 testes verdes. |
| 2026-08-23 | Aberto **DT-53** (self-healing do heartbeat), planejado após o incidente do DT-52. Reestruturada a "Próxima Frente Ativa": distinguida a cadeia real de bloqueio do DT-38 (só o DT-45) dos demais 🔥 Alta que são prioritários mas independentes da VPS — corrige leitura anterior que tratava DT-30 como pré-requisito quando na verdade é consequência da migração. |
| 2026-08-23 | Teste de capacidade do DT-53 executado localmente (`docker-compose.captest.yml`, Piper isolado a `cpus: 1.0`/`1G`): 1/2/5 workers medidos com `cmd/loadtest_piper`, resultado anexado como adendo ao **DT-38**. Descoberto e absorvido o trabalho de TTS feito por fora da sessão (agente Antigravity, commit `c7aca18`) — registrado como **DT-54** (concluído), com nota de proveniência sobre os dois documentos que caíam silenciosamente em regras do `.gitignore` e nunca seriam versionados. Aberto **DT-55**: o container de produção do Piper ainda expõe a porta 5000 publicamente porque a edição do compose (parte do DT-54) não foi aplicada com um recreate. |
| 2026-08-23 | **Incidente real durante a própria sessão**: WhatsApp fora do ar 36min35s, diagnosticado ao vivo e recuperado com `POST /instance/reconnect` (autorização explícita, já que o classificador de auto mode bloqueia por padrão chamadas contra produção) — registrado como **DT-56**. Achado colateral de segurança (chave de API em texto puro nos logs do `evolution-go`) registrado como **DT-57**. Resolvido o DT-55 (`docker compose up -d piper` recriou o container aplicando a config sem a porta publicada; resto da stack confirmado saudável nos logs). Implementados e testados os **Estágios 0, 1 e 2 do DT-53** (alerta Telegram+e-mail, healer com graça/backoff/teto/cooldown em dry-run, detector por webhook de CONNECTION com disjuntor de QR) — o Estágio 3 (correções no `evolution-go-source`) fica deliberadamente para a janela do DT-38, junto do DT-57. Dois bugs reais achados pelos próprios testes automatizados e corrigidos antes de reportar sucesso: corrida assíncrona no teste do notifier, e um bug de lógica onde o cooldown pós-esgotamento resetava o estado mas nunca chegava a tentar de novo. DT-53 reescrito e movido de 🔴 A Fazer para 🟡 Em Andamento, corrigindo a menção a `POST /instance/reconnect` (mostrou-se inútil pro caso alvo) por `POST /instance/forcereconnect/:id`. |
| 2026-08-24 | **Fatia 3 do DT-59 (gateway REST):** implementado `internal/gateway/rpc_proxy.go` — proxy autenticado com allowlist de 10 RPCs de escrita do produtor. Achado no caminho que confirma a decisão do ADR-009: as RPCs alvo são `SECURITY DEFINER` e derivam o dono do registro de `auth.uid()`; chamar com a chave de serviço as quebraria (`auth.uid()` viria NULL, a própria função levantaria "Não autorizado"). O gateway resolve isso encaminhando o JWT do produtor, não a chave de serviço — zero mudança de comportamento nas RPCs, só um ponto central de log. `goApiRpc()` no frontend, `talhaoService.ts` migrado como referência (`create`/`delete`); as outras 8 chamadas continuam diretas ao PostgREST, mesmo padrão. **ADR-009** escrito. 4 testes novos no gateway. **Ainda não deployado** — ao contrário da Fatia 1 (DT-61) e da Fatia 2 (DT-58), que já foram para produção nesta mesma sessão. |
| 2026-08-24 | **Aberto e parcialmente resolvido DT-64**: onboarding via WhatsApp (DT-58) travava em produção por quatro causas empilhadas — extração de cadastro hallucinando nome a partir de perguntas de domínio, interceptor de HITL sequestrando SIM/NÃO de números sem perfil, greeting guard travando o onboarding em saudações puras, e o guardrail de PII apagando o e-mail digitado no estado `aguardando_email`. Todas as quatro corrigidas e deployadas em produção no mesmo dia, validadas ponta a ponta com webhooks sintéticos contra um número de teste. Achado colateral: template "Magic Link or OTP" do Supabase Auth só mandava link, não o código de 6 dígitos — corrigido no template. **Fica em aberto:** o e-mail de produção depende do serviço padrão/embutido do Supabase (rate limit baixo, dev-only); resolver exige SMTP customizado com domínio próprio, decisão do responsável de deixar para quando a VPS (DT-38/DT-45) for provisionada. |
| 2026-08-24 | **Troca de frente ativa, a pedido do responsável:** DT-58 e DT-59 assumem a prioridade e a cadeia DT-45 → DT-38 (VPS, prazo ~2026-09-05) é conscientemente despriorizada — registrado na "Próxima Frente Ativa" para não parecer esquecimento depois. Ambos saíram de ⚪ Decisão Pendente com as opções escolhidas: **DT-58 = (b)** extração one-shot (a FSM é in-memory e um slot-filling longo perderia o cadastro em deploy); **DT-59 = (a) como gateway**, não remoção do PostgREST, porque a leitura literal contradiz o **ADR-002 "Fat Database" (Aceito)** — que já avaliou e aceitou o lock-in do Postgres com mitigação documentada. Três achados durante o dimensionamento: (1) **as rotas administrativas do Go não tinham autenticação nenhuma** — aberto e já resolvido como **DT-61**; (2) `profiles.id` tem FK para `auth.users(id)`, então cadastro só-WhatsApp exige o Go criar o usuário via Admin API, o que **acopla DT-58 a DT-59** (são a mesma frente, não duas); (3) o frontend mandava `Bearer ${localStorage.getItem('token')}` em duas chamadas — chave que o Supabase nunca escreve, header ia com `null` e nunca funcionou, só não importava porque o servidor não verificava. Registrado também o **DT-60** (ordem do prompt sabotando o cache implícito do Gemini), corrigindo de passagem o rótulo errado do commit `f460c77`, que saiu como `DT-59` quando esse número já pertencia à centralização do backend. |
| 2026-08-23 | Implementado e testado o **Estágio 3 do DT-53** (F1, F2, F3 em `evolution-go-source`) — o retry limitado em `StartClient` (F2) teria evitado o próprio DT-56, e tornar `StreamReplaced` observável (F3) teria evitado o DT-52. Ao commitar o trabalho, descoberto que `evolution-go-source` é um gitlink quebrado sem `.gitmodules` (`git submodule status` falha, diretório sem `.git` próprio) — **nenhuma edição feita ali fica sob controle de versão**, achado que agrava e é acrescentado ao **DT-30**, que já documentava o sintoma no CI mas não a consequência prática de perda de trabalho local. Rebuild/deploy do fork continua adiado pra janela do DT-38, e agora depende também de resolver o DT-30 antes de sequer poder commitar. |
| 2026-09-06 | **DT-64 (Continuação)**: Resolvido o loop infinito de recusa de nomes "não-brasileiros" (ex: Ahmed Mesalam) no Onboarding. O problema era causado por um viés cultural do LLM na ferramenta de extração, que rejeitava nomes estrangeiros ou incomuns mesmo quando o produtor respondia diretamente à pergunta. A solução foi dividida em três frentes: (1) **Fast-Path (Determinístico)** com a heurística `pareceNomeProprio` que avança direto para a confirmação, ignorando a avaliação cega do LLM; (2) **Ajuste Cognitivo** adicionando a variável indicando que "o bot acabou de perguntar o nome" no prompt do LLM; e (3) **Escape Hatch** que, após duas falhas de extração no mesmo estado, pega o input cru do usuário e força uma confirmação explícita (SIM/NÃO). Foram adicionados 11 novos testes unitários (incluindo as exatas strings enviadas pelo produtor) que agora validam essas saídas, além de sanar testes quebrados pré-existentes (`fsm_pending`, `benchmark`). |

---

## Notas de Manutenção

- Ao resolver um item, mova-o para **🟢 Concluído**, registre a data e apague a linha correspondente das colunas anteriores.
- Itens novos entram sempre em **🔴 A Fazer** com um novo `DT-ID` sequencial.
- Revisar este documento a cada marco de fase para evitar que volte a virar rastreio informal espalhado por múltiplos arquivos.
