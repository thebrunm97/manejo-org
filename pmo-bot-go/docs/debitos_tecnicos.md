# 🗂️ Débitos Técnicos & Pendências — pmo-bot-go

> Documento formal de acompanhamento, consolidado a partir de `state.md` e `estado_tarefa.md`.
> Última atualização: **2026-08-17**

---

## Como usar este documento

- Cada item tem um **ID** único (`DT-XX`) para referência em commits, PRs e mensagens (`git commit -m "DT-01: rotaciona service_role key"`).
- Status possível: `🔴 A Fazer` · `🟡 Em Andamento` · `🟢 Concluído` · `⚪ Bloqueado/Decisão Pendente`
- Ao mover um item de coluna, atualize também a seção **Histórico** no final do arquivo.
- Este arquivo substitui o rastreio informal que vinha sendo feito via `state.md` / `estado_tarefa.md` — mantenha-o como fonte única de verdade para débitos.

---

## 🔴 A Fazer

| ID | Item | Categoria | Prioridade | Arquivo(s) |
|----|------|-----------|------------|------------|
| DT-01 | Rotacionar `service_role` key do Supabase (crítico pois a tabela `mutation_drafts` depende de service_role e trafega dados sensíveis). **Nota da Sessão**: Durante a Fase 2.2, o `SUPABASE_ACCESS_TOKEN` também foi exposto em texto plano e revogado. Adotar o hábito para toda a equipe de NUNCA colar segredos em comandos visíveis no terminal (usar prompts interativos ou variáveis lidas com input mascarado). | Segurança | 🔥 Alta | `.env` / Supabase Dashboard |
| DT-02 | Remover ou marcar como `deprecated` o método `InsertFarmDocument` (órfão, grava só na coluna legada 3072d/Gemini) | Código Legado | Média | `internal/supabase/client.go` |
| DT-03 | Corrigir filename hardcoded (`"audio.ogg"`) no pipeline legado de áudio | Código Legado | Média | `internal/state/fsm.go`, `media_worker.go` |
| DT-04 | Descomissionar interface legada `llm.LLMProvider` (FSM) em favor de `ports.LLMProvider` | Código Legado | Média | `internal/state/`, `internal/llm/` |
| DT-05 | ~~Atualizar/padronizar model IDs e tabela de custos~~ — **RESOLVIDO** (primário migrado no DT-32; tabela de custos substituída pelo catálogo multi-fornecedor em DT-43). Resta apenas limpar IDs defasados nos utilitários de benchmark (`cmd/tester/`, `fallback_simulation_test.go`), que não afetam produção. | RAG/Modelos | Baixa | `cmd/tester/` |
| DT-06 | Remover diretório `scratch/` e node_modules do laboratório de embeddings offline (Transformers.js) | Limpeza | Baixa | `scratch/` |
| DT-13 | Suporte opcional a Botões Interativos da Evolution API para confirmação de rascunhos HITL (fallback automático para texto livre) | UX / Webhook | Baixa | `internal/adapter/evolution/`, `internal/webhook/` |
| DT-18 | **ÉPICO**: Migrar escrita direta do `pmo-frontend` para RPCs `SECURITY DEFINER` antes de aplicar REVOKE global de grants. Auditoria confirmou que `pmo-frontend` grava direto em tabelas via SDK do Supabase usando anon/authenticated (arquivos: `cadernoService.ts`, `propriedadeService.ts`, `talhaoService.ts`, `pmoService.ts`, `TabelaDinamica.tsx`, `VegetalImportDialog.tsx`, `Secao9.tsx`). A migração `20260816030000_revoke_broad_grants.sql.pending` está pronta mas NÃO deve ser aplicada até essa migração ser concluída, ou ela quebra essas gravações. | Segurança/Frontend | 🔥 Alta | `pmo-frontend/src/...` |
| DT-38 | **PRAZO — Migrar a stack para VPS antes de ~2026-09-05.** Hoje a produção roda na máquina local do desenvolvedor (Docker Desktop/WSL2), o que já custou caro: foi a origem do clock drift (DT-26), impede qualquer pipeline de deploy (DT-30) e derruba o serviço sempre que a máquina desliga. O responsável viaja para Moçambique em ~2 semanas e o MVP precisa rodar sem depender do desktop. Provedor provável: Hostinger. Escopo mínimo: provisionar VPS Linux, instalar Docker + Compose, transferir `.env.prod` por canal seguro (NUNCA versionado nem colado em terminal — ver DT-01), subir a stack, **reconectar a instância do WhatsApp (exige novo QR e é o passo mais arriscado)**, apontar webhook/DNS, configurar backup do volume `piper_data` e do estado do `evolution-go`, e garantir `restart: always` + boot automático. Ganho colateral: em Linux nativo o NTP funciona por padrão, eliminando a classe de problema do DT-26; e um host sempre ligado destrava o DT-30. **⚠️ CAPACIDADE MEDIDA EM 2026-08-22 — a VPS de 2 vCPUs é apertada.** Do pipeline inteiro, o **Piper (TTS) é o único passo que consome CPU local**; transcrição, LLM e envio são todos limitados por rede. Ou seja, a capacidade da VPS é essencialmente a capacidade do Piper. Medido com o container já limitado a `cpus: 1.0` (mesmo teto da VPS), via `scripts/simular_carga_tts.sh`: **tempo ≈ 5,2s fixos + 0,037s por caractere** (50 chars→7,1s · 150→10,9s · 300→15,2s · 600→27,2s). Sob concorrência, 2 requisições simultâneas levam ~42s contra ~20s de uma só. **Descoberta relevante: concorrência NÃO aumenta vazão** — com 1 vCPU, 2 workers apenas dividem a mesma CPU (≈170 áudios/hora nos dois casos), dobrando a latência individual sem ganho algum. Considerar reduzir AIWorkers para 1 na VPS: mesma vazão, metade da espera. **Alavanca testada e DESCARTADA:** trocar por voz mais leve não ajuda — `pt_BR-edresson-low` (21,3s) não foi mais rápido que `pt_BR-faber-medium` (20,3s); o gargalo é a síntese em si, não o tamanho do modelo. **Alavancas que funcionam:** (a) encurtar as respostas — é o fator dominante e linear; (b) não sintetizar quando o produtor prefere texto (**DT-29**), que elimina a carga em vez de otimizá-la; (c) mais vCPUs. Some-se o teto do tier gratuito da Groq: **2.000 transcrições/dia**. Atenuante importante: o texto já é enviado ANTES do áudio (DT-28), então o produtor recebe a resposta em poucos segundos e o áudio chega depois — a espera de 15-40s degrada o complemento, não a resposta. | DevOps / Prazo | 🔥 Alta | `docker-compose.prod.yml`, infraestrutura |
| DT-41-STREAM | **Detectar stall por time-to-first-token (streaming)** — refinamento futuro do DT-41. A correção aplicada elimina os retries inúteis, mas ainda gasta 25s antes de reconhecer o stall. Só o TTFT distingue "modelo pensando" de "conexão morta" *durante* a chamada; por latência total isso é impossível, pois há respostas legítimas em 17s, 19s, 22s e 23s (verificado na amostra). Exige migrar `CallGoogle` de `GenerateContent` para `GenerateContentStream`, com montagem incremental da resposta e das tool calls — mudança de risco moderado no caminho crítico, por isso não feita junto. | Performance | Média | `internal/gemini/client.go` |
| DT-44 | **Falso positivo do filtro de PII em sequências numéricas.** Observado ao validar o DT-39: a string `DT391787418354` foi persistida como `DT[CPF ocultado]4` — o filtro tratou a sequência de dígitos como CPF. Achado com dois lados: confirma que `filter_pii.go` **está ativo sobre o conteúdo persistido** (informação relevante e positiva para o DT-42), mas também mostra que ele redige por padrão numérico sem validar o dígito verificador do CPF. Em produção isso corromperia dados legítimos do produtor — códigos de lote de rastreabilidade, notas fiscais, identificadores de talhão e números de registro têm exatamente esse formato. Corrigir validando o DV antes de redigir, e cobrir com testes de lote/NF que devem passar intactos. | Dados / Segurança | Média | `internal/guardrails/filter_pii.go` |
| DT-40 | **Markdown do WhatsApp renderizado cru no painel web.** O bot formata para o padrão do WhatsApp (`*negrito*`, `**texto**`), e o Monitor ao Vivo exibe os asteriscos literalmente (`**Dicas de ouro**`, `*Consulta Técnica:*`). O texto é o mesmo dos dois lados, mas a convenção de formatação não é. Converter na exibição do painel (WhatsApp-markdown → HTML), sem alterar o que é enviado ao produtor. | Frontend | Baixa | `pmo-frontend/src/pages/` (Monitor ao Vivo) |
| DT-42 | **LGPD — anonimização e minimização antes de enviar dados a terceiros.** Hoje o pipeline envia a fornecedores externos (Google, OpenRouter e, por tabela, os provedores por trás dela) o conteúdo bruto das mensagens do produtor. Isso inclui, no mínimo: **telefone** (identificador direto, presente no roteamento e às vezes no texto), nome de propriedade e talhões (podem identificar o produtor), localização (`consultar_previsao_tempo` recebe cidade/estado, e há latitude/longitude cadastradas), dados econômicos (vendas, despesas, balanço), e o áudio original da voz — **biometria vocal**, que a LGPD trata como dado sensível (art. 5º, II). Há ainda `internal/guardrails/filter_pii.go` no projeto, mas é preciso auditar se ele cobre o que sai para o LLM ou só o que entra. Escopo: (a) mapear o fluxo de dados pessoais e produzir o registro de operações de tratamento (art. 37); (b) pseudonimizar identificadores antes do envio ao LLM (telefone → id opaco, nomes → tokens reversíveis só localmente); (c) definir base legal e política de retenção — hoje `raw_payloads`, `messages` e `audios_audit` guardam tudo indefinidamente, sem TTL; (d) avaliar retenção nos fornecedores (a OpenRouter roteia para provedores com políticas distintas — relevante para transferência internacional, art. 33); (e) implementar direito de eliminação (art. 18, VI), hoje inexistente. **Atualização 2026-08-22 — a camada OpenRouter é menos crítica do que se supunha.** Segundo a documentação de coleta de dados: a OpenRouter **não armazena** prompts nem respostas, salvo dois opt-ins que são **desligados por padrão** (logging privado de entrada/saída; e uso de entradas/saídas para melhoria do produto, em troca de 1% de desconto). Ela armazena apenas **metadados** (contagem de tokens, latência), sem conteúdo. Há também amostragem anônima de prompts para categorização, feita por modelo com política de retenção zero e não associada à conta. **Ações verificáveis derivadas:** (i) confirmar nas configurações da conta que os dois opt-ins estão desligados — são default-off, mas isso precisa ser checado, não presumido; (ii) o risco relevante desceu um nível, para os **provedores por trás** da OpenRouter, alguns dos quais podem capturar dados para treino — a documentação informa quais, e essa checagem deve entrar no critério de escolha de modelo; (iii) nada disso reduz o risco no lado do **Google direto**, que é o caminho primário e tem política própria. **Vínculo com o DT-05:** a mesma neutralidade de fornecedor que reduz lock-in aumenta a superfície de terceiros com acesso a dado pessoal — a escolha de modelo precisa ponderar política de retenção, não só custo e qualidade. | Jurídico / Segurança | 🔥 Alta | `internal/guardrails/filter_pii.go`, `internal/gemini/`, `supabase/migrations/` |
| DT-19 | Investigar drift histórico de migrações anteriores à Fase 2.2. O `supabase migration list --linked` revelou pares com nomes dessincronizados entre local e remoto (datas 20260525, 20260526, 20260609, 20260610). Não bloqueou o deploy, mas indica que o projeto já tinha histórico de renomeação. Investigar se é apenas dessincronia de nome ou se representa schema realmente divergente entre ambientes. | Manutenção/DB | Média | `supabase/migrations/` |
| DT-21 | **Separação de Ambientes (Staging vs Prod)**: Atualmente, Staging e Produção compartilham o mesmo projeto Supabase (`hejewayflbuemnffrhae`). Criar um segundo projeto dedicado a Staging e utilizar `supabase link` / branching model para isolar testes de alterações críticas (ex: correções de segurança e RLS) antes de impactar dados reais. | DevOps | Média | Supabase Dashboard |
| DT-37 | **Filtro de ferramentas por intent não filtra nada** — ⚠️ **REBAIXADO: o dado do DT-33 mostrou que cortar ferramentas NÃO resolve os timeouts** (chamadas sem ferramentas são MAIS lentas que as com 29). Continua válido como economia de **custo por token** (~7.400 tokens desperdiçados por chamada), não como correção de performance. ATENÇÃO ao cortar: a matriz de uso real registrou `registrar_colheita` e `registrar_limpeza` sendo invocadas sob intent **RAG** — remover DBWrite de RAG quebraria registros legítimos, exatamente o risco que motivou medir antes. Apenas **7 das 29** ferramentas foram usadas na amostra, mas uma única simulação não prova que as outras 22 sejam dispensáveis. `GetToolsForIntent` inclui `CategoryRAG + CategoryDBWrite + CategoryDBRead` tanto para `RAG` quanto para `DATABASE`, ou seja, **29/29 ferramentas** nos dois casos (CHAT leva 25/29). Medido em bancada (`go test ./internal/mcp/ -run PayloadSize -v`): **29.594 bytes ≈ 7.400 tokens** só de definição de ferramentas, enviados em **cada turno** e **cada retry**. Uma pergunta "qual a previsão do tempo?" carrega `registrar_compra_insumo` (2,3KB), `registrar_venda`, `registrar_colheita`, `cadastrar_propriedade`… Principal suspeito dos timeouts que persistiram após a troca do modelo. **Achado colateral:** a descrição de `calcular_recomendacao_adubacao` contém fragmentos de *system prompt* colados por engano (regras numeradas "# 7. NO TECHNICAL IDs", "# 8. EXTRAÇÃO DE CONTEXTO", "# 9. LÓGICA DE ÁREA TOTAL"), inflando o payload e poluindo o contexto do modelo. | Performance / Custo | 🔥 Alta | `internal/mcp/server.go:143`, `internal/mcp/tools_registry.go:17` |
| DT-33 | **Medir a causa real dos timeouts de LLM antes de trocar de modelo**. ⚠️ **PRIMEIRA MEDIÇÃO JÁ REFUTOU A HIPÓTESE INICIAL**: após migrar do canal preview para o GA, a telemetria registrou `motivo_do_fallback=timeout modelo_primario=gemini-3.1-flash-lite tentativas=3` **duas vezes seguidas** (2026-08-22 09:43). Ou seja, o modelo GA também estoura o prazo — o canal preview **não** era a causa, confirmando a orientação de medir antes de concluir. Investigar as causas ainda não descartadas: prompt extenso (29 ferramentas são enviadas a cada turno), legislação inteira no contexto, ausência de streaming, deadline de 30s por turno no orquestrador possivelmente curto demais para a carga real. Orientação original do time: | Observabilidade | 🔥 Alta | `internal/gemini/`, `internal/telemetry/`, `internal/queue/` |
| DT-33-DETALHE | Orientação de método registrada em 2026-08-22: com ~47% de timeout, não se deve declarar o modelo inadequado antes de saber se a falha está no modelo, na API, na rede ou no **desenho do prompt** (prompt extenso, legislação inteira no contexto, documentos sem pré-processamento, chamadas encadeadas, concorrência, ausência de streaming). A instrumentação `motivo_do_fallback` do DT-32 é o primeiro passo, mas falta registrar por chamada: `request_id, model, input_tokens, output_tokens, latency_ms, time_to_first_token, status_code, timeout_stage, retry_count, retrieved_chunks, prompt_version, cost_estimate`. Falta também aplicar **deadline por etapa** (retrieval 2s, classificação 5s, geração 20s, validação JSON 3s, fallback 15s) em vez de um teto único, para identificar QUAL etapa estoura. Métrica-alvo: **custo por caso corretamente resolvido**, não custo por chamada. | Observabilidade | 🔥 Alta | `internal/gemini/`, `internal/telemetry/`, `internal/queue/` |
| DT-34 | **Roteador de modelos por complexidade** (desenho definido em 2026-08-22). Hoje todas as tarefas usam o mesmo modelo, o que é caro para triagem e fraco para casos complexos. Desenho alvo: `gemini-3.1-flash-lite` para classificação de intenção, extração de entidades, triagem e resumo; `gemini-3.7-flash` para interpretar múltiplos dispositivos normativos, analisar exceções, confrontar documentos e produzir relatório preliminar; **motor de regras determinístico + auditor humano** para decisão final de conformidade, vigência de norma, substância autorizada e cálculo de prazo — nunca delegados ao LLM. O roteamento deve usar critérios objetivos (mais de uma norma aplicável, presença de exceções, conflito entre documentos, ausência de evidência, baixa confiança na recuperação), não a autoavaliação do modelo. Distinguir fallback de **infraestrutura** (mesmo modelo, outro provedor) de fallback de **capacidade** (modelo mais forte). | Arquitetura / Custo | Média | `internal/state/`, `internal/llm/`, `internal/gemini/` |
| DT-29 | **Preferência de formato de resposta por produtor**: hoje o formato é decidido **por mensagem** (espelha a entrada: mandou áudio → responde áudio), não por pessoa. Não existe nenhuma coluna de preferência em `profiles` (verificado no banco). Decisão de produto tomada em 2026-08-22: o produtor deve poder escolher entre `audio`, `texto` e `ambos` por **três** caminhos — (1) comando no WhatsApp ("modo texto"), (2) seletor no painel do `pmo-frontend`, (3) botões interativos no primeiro contato (casa com o DT-13). Escopo: migration adicionando `preferencia_resposta` em `profiles`; leitura em `queue/response_mode.go` (`ShouldRespondWithAudio`), que já é o ponto central de decisão; handler do comando no webhook; UI no frontend. **Pré-requisito já concluído**: a entrega áudio+texto do DT-28. | UX / Produto | Média | `supabase/migrations/`, `internal/queue/response_mode.go`, `internal/webhook/`, `pmo-frontend/src/` |
| DT-30 | **Não existe pipeline de deploy do container** — causa raiz do DT-25. O `.github/workflows/deploy_production.yml` roda apenas `supabase db push` (migrations); ele **nunca** buildou ou publicou a imagem do `pmo-bot-go`. Ou seja, mesmo funcionando não teria evitado a defasagem. Além disso ele está quebrado: falha em 16s com `Missing value for flag --db-url` porque o secret `PROD_DB_URL` não está configurado no repositório (run `32539826317`), e o checkout emite `fatal: No url found for submodule path 'evolution-go-source' in .gitmodules`. Agrava: **todos** os demais workflows (`Tests & Coverage`, `Backend E2E`, `E2E`) também estão vermelhos há semanas. Complicador de arquitetura: a stack de produção roda localmente via Docker Desktop/WSL2, então um runner do GitHub não alcança o host — resolver exige registry + pull no host, runner self-hosted, ou assumir o deploy como manual e documentado. | DevOps | 🔥 Alta | `.github/workflows/`, `docker-compose.prod.yml` |
| DT-22 | **Schema Drift Crítico (Local vs Prod)**: O ambiente remoto (Staging/Prod) possuía alterações não versionadas em migrations locais: (1) colunas extras em `profiles`, (2) FK nativa para `pmos`, (3) RLS recursivo infinito na policy de Admin e (4) coluna `canteiro_id` + FK ausentes em `caderno_campo_canteiros`. A falta de paridade quebrou testes locais. Criadas migrations corretivas (finais `212500`, `213000` e `214500`) de forma estritamente idempotente para reestabelecer o rastreio oficial sem quebrar Prod. | Manutenção/DB | 🔥 Alta | `supabase/migrations/` |


## ⚪ Bloqueado / Decisão Pendente

| ID | Item | Decisão Necessária | Opções |
|----|------|---------------------|--------|
| DT-07 | Futuro do `rag_ingest.py` (Docling) | Aposentar em favor do `cmd/ingestor` (Go) ou manter como pré-processador isolado? | (a) Migrar tudo para Go / (b) Manter híbrido |
| DT-08 | Política de acesso ao painel Knowledge Ops | Reabrir para produtores ou manter restrito ao admin? | (a) Abrir para produtores / (b) Restrito a admin |

## 🟡 Em Andamento

| ID | Item | Estado |
|----|------|--------|
| DT-33 | Medir a causa real dos timeouts de LLM | **Instrumentação COMPLETA no ar, aguardando tráfego.** Já emitidos: `event=llm_call` (status, turno, modelo, `latency_ms`, `req_bytes`, ferramentas, msgs de histórico, `input_tokens`, `output_tokens`, `tool_calls`), `event=llm_fallback` (`motivo_do_fallback` em vocabulário controlado) e `event=tool_invoked` (intent × ferramenta). Cobertura fechada em 2026-08-22: além do laço do orquestrador, foram instrumentados o **atalho de CHAT** (que envia ZERO ferramentas e é o grupo de controle natural da hipotese de carga) e o **nivel do provider** em CallGoogle, que captura de uma vez os 6 pontos que chamam o LLM — inclusive o **roteador de intencao**, ja observado levando **19s para classificar Ola, bom dia com zero ferramentas**, dado que contraria a hipotese de que so a carga explica os timeouts. Agregador em scripts/analisar_telemetria.sh, que acumula em .telemetria_acumulada.log para sobreviver a recriacao de container. ATENCAO: evitar docker compose up --build ate a analise, pois recriar o container descarta seus logs. |
| DT-37 | Filtro de ferramentas por intent não filtra nada | **Parcialmente resolvido.** Removidos os fragmentos de system prompt vazados na descrição de `calcular_recomendacao_adubacao` (784 bytes, ~200 tokens — ganho real mas pequeno, 2,6%). O corte de verdade depende do DT-33: apertar `GetToolsForIntent` sem dado é arriscado, pois se o roteador classificar "registra 10kg de tomate" como RAG e RAG tiver perdido as ferramentas de escrita, o registro falha em silêncio. `event=tool_invoked` foi adicionado justamente para montar a matriz intent × ferramenta e permitir o corte com base em uso real. |

## 🟢 Concluído

| ID | Item | Concluído em | Observações |
|----|------|---------------|-------------|
| DT-00 | Fase 2.1 — Idempotência e migrações no PostgreSQL Staging | 2026-08-16 | Validada com testes unitários e de integração real |
| DT-09 | Two-Phase Commit com tabela `mutation_drafts` no PostgreSQL e TTL de 45min | 2026-08-17 | Fase 2.2 deployada em staging com sucesso (2026-08-16). As 3 migrações (000000, 010000, 020000) confirmadas idênticas entre local e remoto via `supabase migration list --linked`. |
| DT-10 | Tool única polimórfica `propose_batch_mutations(operations: [...])` | 2026-08-17 | Unio discriminada por `type` (`caderno_campo`, `compra_insumo`, `transacoes_com_rateio`, `cotas_produtores`) |
| DT-11 | Normalização determinística de respostas de confirmação HITL por palavras-chave | 2026-08-17 | `ClassifyHITLResponse` antes de chamada a LLM com fallback robusto |
| DT-12 | Substituição atômica via `create_or_supersede_mutation_draft` e terminalidade de rascunhos com erro | 2026-08-17 | Prevenção de race condition sob `FOR UPDATE` e índice único parcial `idx_mutation_drafts_one_pending` |
| DT-25 | **Container de produção defasado (stale build)** — imagem buildada em 2026-08-16 vs. último commit em 2026-08-21 (`1b1b341`). O binário não tinha 5 dias de fixes. Sintoma: `consultar_previsao_tempo` falhando 100% com `parâmetro 'propriedade_id' é obrigatório`, string que já não existia no código-fonte — o que confirmou o diagnóstico. | 2026-08-22 | Rebuild executado (`docker compose -f docker-compose.prod.yml up -d --build pmo-bot-go piper`). Imagem agora 2026-08-22T12:03Z. Levou junto **DT-27** (SendVoice multipart) e **DT-28** (TTS agnóstico + Piper + áudio&texto). Verificado em produção: bot loga `🔊 [TTS] Provider: Piper (auto-hospedado) voz=pt_BR-faber-medium`, container `piper` de pé (Uvicorn na :5000) e síntese testada ponta a ponta (138KB de WAV válido, magic bytes `RIFF....WAVE`). **Causa raiz não resolvida** — rastreada em `DT-30`: não há pipeline que faça deploy do container, então a defasagem volta a acontecer. |
| DT-31 | **TTS lia formatação em voz alta + áudio perdido por timeout**. Validação em produção do DT-28 revelou três defeitos: (1) o texto ia cru para o motor, que narrava "asterisco asterisco Consulta Técnica" e o nome de cada emoji (⏳, 🌿, ☀️); (2) a resposta longa da previsão do tempo falhava com `context deadline exceeded` — o WAV de 554KB estourava o timeout de 30s; (3) o áudio era enviado ANTES do texto, então uma síntese de 25s deixava o produtor sem resposta nenhuma nesse intervalo, e sem nada quando o TTS falhava. | 2026-08-22 | (1) Criado `utils.SanitizeForSpeech` — remove emoji/pictogramas, marcadores de lista, ênfase, cabeçalhos, blocos de código e URLs, preservando letras acentuadas, dígitos, pontuação e `° º ª %`. A mensagem **escrita** mantém a formatação intacta; só o áudio usa a versão limpa. 11 casos de teste. (2) Piper passou a devolver **MP3 em vez de WAV**: medido 92KB contra 554KB (~6x menor) na mesma resposta; timeouts elevados para 120s (contexto) e 150s (cliente HTTP). (3) Ordem invertida para **texto primeiro, áudio depois**, nos dois caminhos (`queue/delivery.go` e `state/utils.go`), com teste de invariante de ordem. Corrigido também o comentário do contrato em `ports.TTSProvider`, que afirmava erroneamente que o texto chegava pré-sanitizado. |
| DT-32 | **Cadeia de fallback do Gemini estava morta em três camadas** (~47% das dúvidas terminando em "erro interno"). (a) `CallGoogle` fixava `c.Config.Model` internamente e **ignorava** o modelo recebido por parâmetro — ao "escalar", `withFallback` voltava a chamar o mesmo primário que acabara de falhar; evidência: a URL do erro citava o primário enquanto o log dizia estar no fallback. (b) O modelo de fallback `gemini-1.5-flash` **foi descontinuado** — confirmado com 0 ocorrências em `/v1beta/models`; estava também hardcoded em `DescribeImage` (descrição de imagem quebrada em silêncio) e como default em `main.go`. (c) A escalada herdava o **contexto já expirado** pelo primário e retornava em ~0,55ms sem abrir conexão — medido pelos timestamps de log (`09:27:04.843084` → `09:27:04.843639`). | 2026-08-22 | (a) `CallGoogle` passou a receber `modelName` obrigatório, propagado em todos os call sites. (b) Primário migrado do canal *preview* para o GA `gemini-3.1-flash-lite`; default morto removido de `main.go` (vazio agora é intencional: escala para **outro provedor**, OpenRouter, que é o que resolve falha de infraestrutura); `OPENROUTER_MODEL` alinhado ao **mesmo** modelo (`google/gemini-3.1-flash-lite`, verificado como existente na OpenRouter). (c) Cada tentativa recebe deadline próprio de 25s via `context.WithoutCancel`, para a escalada não nascer sem prazo. Adicionado vocabulário controlado `FallbackReason` (`timeout`, `rate_limit`, `erro_5xx`, `requisicao_invalida`, `outro`) emitido como `telemetry event=llm_fallback motivo_do_fallback=...`, separando falha de **infraestrutura** de falha de **capacidade**. 9 testes. **Nota de método:** a troca do preview pelo GA é uma hipótese ainda não confirmada como causa dos timeouts — a instrumentação foi adicionada justamente para medir isso em vez de supor (ver DT-33). |
| DT-35 | **Output Judge bloqueava respostas legítimas como `ALUCINACAO_DADOS`** (falso positivo). O `buildJudgePrompt` só marcava `FONTE_RAG=sim` para ferramentas cujo nome contivesse `document`/`rag`, e a regra do juiz só mandava relaxar quando `FONTE_RAG=sim`. Resultado: `consultar_previsao_tempo` executava de fato contra a API meteorológica, mas o juiz recebia `FONTE_RAG=não`, via números específicos (16°C, UV 7,4, evapotranspiração 5,88mm) e acusava alucinação — bloqueando **toda** resposta de previsão do tempo e notificando um especialista à toa. Verificado nos logs que a ferramenta rodou (`09:43:58 handleConsultarPrevisaoTempo executado`) antes do bloqueio (`09:44:02 Judge BLOQUEADA`). **Bug secundário achado pelo próprio teste de regressão:** a checagem usava `strings.Contains(t, "rag")` minúsculo, mas a ferramenta real é `ConsultarLeiOrganica_RAG` — então `FONTE_RAG` era **sempre** "não", deixando o juiz rigoroso demais também nas consultas RAG legítimas. | 2026-08-22 | Introduzido o sinal `FONTE_EXTERNA`, marcado por qualquer ferramenta de consulta executada (prefixo `consultar`, RAG ou documentos), separado do `FONTE_RAG` específico de documentos. Ferramentas de **escrita** (`registrar_*`) deliberadamente não contam como fonte. A regra `ALUCINACAO_DADOS` passou a declarar que dados retornados por qualquer ferramenta em `[FERRAMENTAS USADAS]` são fonte legítima — essas ferramentas foram realmente executadas — e que previsão do tempo não é "registro da fazenda do produtor". Comparação agora case-insensitive. 4 testes de regressão. |
| DT-36 | **Unidades e datas lidas como abreviação no áudio**. Com o pipeline já funcionando ponta a ponta, o motor lia "28°C" como "vinte e oito grau cê" e "(22/08)" como "vinte e dois barra zero oito". Tecnicamente correto, mas soa robótico para quem ouve no campo. | 2026-08-22 | `SanitizeForSpeech` ganhou `expandUnitsForSpeech` (°C/°F/°, mm, mm/dia, cm, km, km/h, kg, ha, m², %) e `expandDatesForSpeech` (dd/mm e dd/mm/aaaa → "22 de agosto"). A expansão roda **antes** de `stripSymbols`, que descartaria `°` e `%` sem deixar leitura; a ordem das regras é significativa (`mm/dia` antes de `mm`). Mês inválido é deixado intacto, evitando estragar razões numéricas. 13 testes novos, incluindo a mensagem real de previsão ponta a ponta. Três asserções anteriores foram atualizadas: afirmavam que `°C` e `%` permaneciam como símbolo, o que era a intenção antiga. |
| DT-26 | **Clock drift** — o sidecar `clockwork` tentava corrigir o relógio de dentro do contêiner e logava "setting time" a cada 30s, mas o offset só crescia (+4,85s → +5,46s em 3 minutos): o relógio autoritativo é o do Windows e o Hyper-V TimeSync reimpõe esse horário na VM, desfazendo o ajuste. O log dava falsa sensação de saúde e mascarou a causa por dias — foi este drift que derrubou 100% dos downloads de mídia do WhatsApp (403) na manhã de 2026-08-22. | 2026-08-22 | **Causa raiz: o serviço `W32Time` do Windows estava PARADO.** A configuração estava correta (`time.windows.com`), mas o serviço é *trigger-started* por ingresso em domínio e a máquina não está em domínio — o gatilho nunca disparava e o Windows jamais sincronizava. Corrigido no host (`Set-Service W32Time -StartupType Automatic` + `Start-Service` + `w32tm /resync /force`), executado manualmente pelo responsável por exigir Administrador. Validado: `Status: Running`, `StartType: Automatic` (sobrevive a reboot), `Camada 5 sincronizado por SNTP`, sondagem a cada 1024s. Drift medido após a correção: **média 0,403s, máximo 0,424s** — dentro da tolerância. O `clockwork` foi reescrito de corretor para **sentinela**: só mede (`ntpd -w`), sem `privileged` nem `SYS_TIME`, e a classificação por limiar saiu do YAML para `scripts/analisar_telemetria.sh`. |
| DT-33 | **Medir a causa real dos timeouts de LLM** — hipótese inicial (canal *preview* do modelo) e hipótese secundária (payload de 29 ferramentas) ambas **REFUTADAS PELO DADO**. | 2026-08-22 | Instrumentação em 3 níveis (`llm_call` no orquestrador, `llm_provider_call` no adapter cobrindo os 6 call sites, `tool_invoked` para a matriz intent×ferramenta) + simulador de carga (`scripts/simular_carga.sh`) + agregador (`scripts/analisar_telemetria.sh`). **Amostra: 121 chamadas.** Conclusões: **(1)** A distribuição é **bimodal** — 69% abaixo de 3s, 85% abaixo de 10s, e **7% travam e são mortas pelo timeout**, com **ZERO chamadas na faixa 24–24,9s**. Essa lacuna é a assinatura de **conexão travada**, não de processamento lento: carga computacional produziria distribuição contínua (chamadas em 15s, 20s, 23s), não um vão. **(2)** O payload **não** explica: chamadas **SEM ferramentas** (roteador, payload mínimo) tiveram média **6.477ms e 23% acima de 10s**, contra **3.749ms e 9%** das chamadas **COM 29 ferramentas** (44KB) — as leves são MAIS lentas que as pesadas. Com payload idêntico (44KB), a latência variou de 789ms a 35.261ms (45x). **(3)** O fix do fallback (DT-32) está funcionando: 20 escaladas para a OpenRouter, **0% de erro para o usuário final** — antes essas 7% viravam "erro interno". Ação restante movida para DT-41 (fail-fast em stall). |
| DT-41 | **Conexão travada era retentada 3x no mesmo endpoint mudo** — custo de ~79s por stall. `isOverloadedError` tratava `context deadline exceeded` como erro transitório de sobrecarga, então um stall disparava a política de retry do primário: 25s (trava) + 2s + 25s (trava) + 2s + 25s (trava) **antes de sequer tentar outro provedor**. Repetir a mesma requisição no endpoint que acabou de ficar mudo por 25s é justamente o que não resolve stall — e contraria a orientação do time de que falha de infraestrutura se resolve trocando de **provedor**, não repetindo no mesmo. | 2026-08-22 | Criado `isStallError`, separado de `isOverloadedError`: stall (deadline/`Client.Timeout`) **escala imediatamente** para a OpenRouter, enquanto 429/503/`overloaded` — que são o servidor declarando carga — mantêm o retry com backoff, onde ele faz sentido. Custo do stall cai de **~79s para ~25s**, afetando os 7% de chamadas medidos no DT-33. 8 casos de teste, incluindo regressão explícita de que timeout não volta a ser classificado como overload. **Nota de método:** a proposta original deste débito era baixar o `attemptTimeout` para 12s; ao inspecionar a faixa de 8–25s antes de aplicar, verificou-se que existem respostas legítimas em 17s, 19s, 22s e 23s — o corte mataria 7 das 13 chamadas dessa faixa. A separação limpa que se supunha não existe (o vão real é só de 23,3s a 24,9s), e a proposta foi descartada em favor desta. Refinamento por TTFT segue em DT-41-STREAM. |
| DT-43 | **Tabela de custos por LLM era ficção** — preços digitados à mão num `switch`, só de Gemini, e **custo ZERO para qualquer modelo não reconhecido**. Como o sistema passou a escalar para a OpenRouter, todo o gasto do fallback ficava invisível. Os valores também estavam defasados: cobrava `gemini-3.1-flash-lite` a US$0,075/US$0,30 por 1M quando o real é US$0,250/US$1,500 — subestimando entrada em 3,3x e saída em 5x. | 2026-08-22 | Criado o pacote `internal/pricing` com catálogo **multi-fornecedor** (319 modelos com suporte a tool calling, de Google, Anthropic, OpenAI, Meta, DeepSeek, Qwen, Mistral e outros), gerado por `cmd/pricing-refresh` a partir de fonte pública e verificável, embutido via `go:embed` e consultado offline. Modelo desconhecido passa a receber estimativa **conservadora (cara)**, nunca zero: subestimar custo desconhecido induz decisão errada de arquitetura, superestimar apenas provoca conferência. O `Lookup` tolera as variações de nome entre fornecedores (`gemini-x` vs `google/gemini-x`, sufixos de variante). Inclui `Compare(entrada, saida, n)`, que ordena por perfil de uso real — a ordem muda conforme a proporção entrada/saída, servindo de insumo objetivo ao roteador do DT-34. **Ganho de resiliência:** o refresh captura `expiration_date`, e `cmd/pricing-refresh -check` falha se um modelo em uso estiver ausente ou expirando — exatamente o cenário que quebrou a cadeia de fallback quando o `gemini-1.5-flash` foi descontinuado sem aviso. **Limitação registrada:** `pricing.overrides` (preço de contexto longo e janelas de horário) não está modelado; o custo assume condições padrão. 7 testes. |
| DT-39 | **Monitor ao Vivo exibia 21:00 em toda mensagem e listava a conversa fora de ordem.** A coluna `timestamp` de `public.messages` era `text`, sem DEFAULT, e o struct `MessageInsert` do bot **não tinha campo de timestamp** — nunca foi enviado. No frontend, `new Date(msg.timestamp)` com NULL vira epoch, que em UTC-3 renderiza **21:00 de 31/12/1969**: uma hora fabricada, com aparência de real. **Segundo defeito, mais grave:** tanto a consulta do painel quanto a view `view_conversas_recentes` ordenam por essa coluna; com ela nula, a ordem das mensagens e a escolha da "última mensagem" de cada conversa eram **indefinidas**. Um painel de auditoria fora de sequência é pior que não ter painel. | 2026-08-22 | Migration `20260822150000`: coluna convertida para `timestamptz`, `DEFAULT now()` como defesa em profundidade, índice `(phone, timestamp DESC)` e a view recriada com `NULLS LAST`. O bot passa a enviar o instante explicitamente (`MessageInsert.Timestamp`) — o que importa para auditoria é a hora do evento, não a do INSERT, que ocorre numa goroutine assíncrona e pode atrasar. No frontend, `formatarHoraMensagem` exibe `--:--` em vez de fabricar horário. **Decisão deliberada:** as 307 linhas históricas seguem NULL. Preenchê-las com `now()` "limparia" a tela, mas fabricaria dado de auditoria num sistema que serve de prova perante certificadora — admitir a lacuna é mais honesto. **Percalços registrados:** (a) o levantamento inicial concluiu "100% NULL"; a verificação antes de aplicar mostrou 30 linhas COM valor, em dois formatos distintos, todos testados como convertíveis; (b) a primeira tentativa falhou e reverteu inteira — a view dependia da coluna (`cannot alter type of a column used by a view`), exigindo DROP/CREATE e restauração explícita dos 20 GRANTs. Verificado após aplicar: tipo correto, DEFAULT ativo, 337 linhas e os 30 timestamps preservados, view funcionando e grants restaurados. |
| DT-20 | Hotfix IDOR Crítico: `setup_initial_profile` mitigado | 2026-08-17 | Vulnerabilidade de sequestro fechada (Staging). Nota: Auditoria manual de baixo volume (N=6), sem cruzamento sistemático de created_at/duplicidade — aceitável devido à volumetria, mas não é garantia formal. |
| DT-28 | **TTS agnóstico + voz natural + resposta em áudio E texto**. (a) A voz robótica vinha de `tts.NewGoogleOrchestrator()` hardcoded em `main.go` — endpoint **não-oficial** do Google Tradutor. (b) O pacote `internal/tts` era um `Orchestrator` único com `switch` por `Provider string`, acoplando a lógica de negócio ao fornecedor: 40+ assinaturas recebiam `*tts.Orchestrator` concreto. (c) A resposta era *ou* áudio *ou* texto, deixando sem saída o produtor que não pode ouvir. | 2026-08-22 | Criada a fronteira `ports.TTSProvider` (mesma estratégia de `LLMProvider`): `GenerateSpeech(ctx, text) (audio, mimeType, err)` + `Name()`. `internal/tts` dividido em `openai_compat.go` (cobre Piper, Groq e OpenRouter — todos falam `/v1/audio/speech`), `google_translate.go` (legado) e `factory.go` (`NewFromEnv`, único ponto que conhece fornecedor concreto). Todas as assinaturas migradas para a interface — os erros de "import não usado" que surgiram foram a prova do desacoplamento. Adotado **Piper auto-hospedado** (serviço no compose + volume `piper_data`, voz `pt_BR-faber-medium`, gratuito e sem cota). Entrega passou a ser **áudio + texto**, com o texto como canal garantido e o áudio best-effort enviado uma única vez (não se repete em retry). Adicionado `sniffAudioMIME` para rejeitar HTTP 200 com corpo não-áudio. Cobertura: 8 testes em `internal/tts` + 5 em `internal/queue/delivery_test.go`. **Requer rebuild (ver DT-25).** |
| DT-27 | **`SendVoice` incompatível com o contrato do `evolution-go`** — bot nunca respondia em áudio. O adapter enviava JSON com `media: "data:audio/ogg;base64,..."` + `mediatype` (contrato da Evolution API v2 em Node), mas o fork `evolution-go` usado aqui expõe `/send/media` com dois caminhos: (a) JSON exigindo `url` (`data.Url`, consumido via `http.Get` — não aceita base64 nem data URI) e (b) multipart aceitando os bytes em `file` + `type`. Resultado: todo envio retornava `400 {"error":"URL is required"}` e caía no fallback para texto. | 2026-08-22 | Fix: `SendVoice` reescrito para `multipart/form-data` com campos `number`, `type=audio` e `file` (bytes decodificados do base64). O `evolution-go` converte para Opus automaticamente e envia com `PTT=true`, então o parâmetro `isPtt` passou a ser redundante nesse caminho. Compila e passa `go vet`. **Requer rebuild da imagem para ter efeito em produção (ver DT-25).** |
| DT-23 | **Sidecar `clockwork` (NTP) quebrado silenciosamente** — causa raiz de "hora errada" + 100% dos downloads de áudio do WhatsApp falhando com 403. `ntpdate` foi removido dos repositórios do `alpine:latest` (`apk add ntpdate` falhava sempre, erro engolido por `2>/dev/null \|\| true`). Sem correção de drift, o relógio do host derivou (2.94s medido às 07:15:52 do dia do incidente) e a Evolution API passou a rejeitar a assinatura das chamadas à WhatsApp API com 401/403, derrubando todo job de download de áudio para dead-letter após 3 tentativas. | 2026-08-22 | Fix: `command` do serviço `clockwork` em `docker-compose.prod.yml` trocado para usar o applet `ntpd` embutido no busybox do Alpine (`busybox ntpd -n -q -p time.google.com`), que não depende de nenhum pacote via `apk` e não silencia erros. Container recriado em 2026-08-22 e o `ntpd` voltou a executar de fato. **PORÉM: conclusão parcial** — a correção não persiste (o relógio segue ~4s atrasado). O download de mídia voltou a funcionar (403 → 200), mas o drift não foi eliminado. Continuação rastreada em **DT-26**. Ver também `state.md` item 9. |

---

## 🎯 Próxima Frente Ativa

**Fase 3 — Refinamento do RAG, Rotação de Segredos e Polimento Operacional:**
1. Executar rotação da `service_role` key do Supabase (`DT-01`).
2. Limpeza de código legado (`DT-02`, `DT-03`, `DT-04`, `DT-06`).
3. Decisão sobre `rag_ingest.py` e consolidação definitiva no pipeline Go (`DT-07`).

---

## 📜 Histórico de Mudanças

| Data | Alteração |
|------|-----------|
| 2026-08-16 | Documento criado a partir da consolidação de `state.md` + `estado_tarefa.md` |
| 2026-08-16 | Adicionadas decisões arquiteturais da Fase 2.2 (mutation_drafts/HITL) — DT-09 a DT-12 |
| 2026-08-17 | Concluída Fase 2.2 (DT-09 a DT-12): Migração SQL, RPCs atômicas, tool `propose_batch_mutations`, interceptor webhook HITL e 100% dos testes de concorrência e integração |
| 2026-08-22 | Diagnosticado e corrigido (código) DT-23: sidecar `clockwork` (NTP) inoperante por remoção do pacote `ntpdate` no Alpine — causa raiz de drift de relógio e falhas 403 no download de áudio do WhatsApp. Container recriado; downloads de áudio voltaram a funcionar (403 → 200). |
| 2026-08-22 | Investigação de follow-up sobre os logs pós-fix revelou 3 problemas adicionais: **DT-25** (container de produção defasado em 5 dias, quebrando `consultar_previsao_tempo`), **DT-26** (drift de relógio persiste apesar do DT-23 — correção não fixa no WSL2) e **DT-27** (`SendVoice` incompatível com o contrato do `evolution-go`, bot nunca respondia em áudio). |
| 2026-08-22 | **DT-28** concluído: criada a fronteira `ports.TTSProvider` (mesma estratégia do `LLMProvider`), `internal/tts` dividido por fornecedor, adotado **Piper auto-hospedado** (voz natural, gratuito, sem cota) e entrega passou a ser **áudio + texto**. 13 testes novos. Aberto **DT-29** para a preferência de formato por produtor. |
| 2026-08-22 | **DT-25** concluído: rebuild da imagem de produção, levando junto DT-27 e DT-28. Piper validado em produção. Correção de registro: o `deploy_production.yml` **rodou** (e falhou por secret ausente), ao contrário do que se anotou antes — mas ele só faz `db push` e nunca deployou o container. Causa raiz aberta como **DT-30**. |
| 2026-08-22 | **DT-26** concluído: causa raiz do clock drift era o serviço `W32Time` do Windows parado (trigger-start por ingresso em domínio, máquina fora de domínio). Corrigido no host; drift caiu para ~0,4s estável. O sidecar `clockwork` virou sentinela — mede e alerta em vez de fingir que corrige. |
| 2026-08-22 | **DT-31** concluído: teste real no WhatsApp expôs que o TTS lia a formatação em voz alta, que a resposta longa perdia o áudio por timeout (WAV grande + teto de 30s) e que o áudio ia antes do texto. Criado `utils.SanitizeForSpeech`, Piper migrado para MP3 (~6x menor), timeouts ampliados e ordem invertida para texto→áudio. Lição registrada: os três só apareceram no uso real — o DT-28 tinha build, vet e 13 testes verdes. |

---

## Notas de Manutenção

- Ao resolver um item, mova-o para **🟢 Concluído**, registre a data e apague a linha correspondente das colunas anteriores.
- Itens novos entram sempre em **🔴 A Fazer** com um novo `DT-ID` sequencial.
- Revisar este documento a cada marco de fase para evitar que volte a virar rastreio informal espalhado por múltiplos arquivos.
